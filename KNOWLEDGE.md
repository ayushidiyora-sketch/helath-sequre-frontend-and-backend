# HealthSecure Portal — Project Knowledge Base

> Single source of truth for this project. Update this file at the end of every task per `CLAUDE.md` rules.

---

## 1. Project Overview

- **Name:** HealthSecure Portal — HIPAA-Ready Patient Portal
- **Type:** Web-only multi-tenant healthcare portal (responsive desktop / tablet / mobile browsers)
- **Owner:** Sensussoft Software Pvt Ltd
- **Phase:** Frontend ~100% page-level done; backend started — first slice = Super Admin auth + Tenants CRUD on NestJS + Prisma + PostgreSQL
- **Compliance targets:** HIPAA, GDPR, HL7/FHIR-ready, FDA 21 CFR Part 11, SOC 2 (readiness posture)
- **Repository:** https://github.com/Sensussoft-Software/Health_Secure_Portal.git
- **Current branch:** `ayushi` (main is `main`)

---

## 2. Tech Stack (Frontend, current)

| Layer | Technology | Version |
|---|---|---|
| Framework | Next.js (App Router) | 15.5.18 |
| Language | TypeScript | 5.7.3 |
| UI library | React | 19.0.0 |
| Styling | Tailwind CSS | 4.3.0 |
| PostCSS | postcss | 8.5.15 (override) |
| Icons | lucide-react | 0.469.0 |
| Toasts | sonner | 2.0.7 |
| Date utilities | date-fns | 4.1.0 |
| Auth utility | jose (JWT) | 6.2.3 |
| PDF generation | jspdf + jspdf-autotable | 4.2.1 / 5.0.8 |
| Radix primitives | avatar, dialog, dropdown-menu, popover, progress, scroll-area, separator, slot, switch, tabs, tooltip | various |
| Linting | ESLint + eslint-config-next | 9.39.4 / 15.5.18 |

### Mandated stack from requirement doc — NOT yet installed

These libraries are required per the requirement doc but not yet in `package.json`. Must install before backend integration:

- `react-hook-form` — form management
- `zod` — schema validation
- `@tanstack/react-query` — server state
- `axios` — HTTP client
- `zustand` (or `@reduxjs/toolkit`) — client global state
- `i18next` + `react-i18next` — internationalization
- `husky` + `lint-staged` — pre-commit hooks

---

## 3. Repository Structure

```
d:/helathsecure/                  ← project root
├── CLAUDE.md                     ← Claude Code session rules
├── KNOWLEDGE.md                  ← THIS FILE
├── backend/                      ← NestJS API (in development)
│   ├── prisma/                   ← schema + seed + migrations
│   ├── src/
│   │   ├── main.ts               ← Nest bootstrap
│   │   ├── app.module.ts
│   │   ├── prisma/               ← global PrismaService
│   │   ├── auth/                 ← login/refresh/logout/me + JwtAuthGuard + RoleGuard
│   │   └── super/tenants/        ← Super Admin tenant CRUD
│   └── README.md                 ← setup + smoke-test
├── frontend/                     ← Next.js app
│   ├── app/                      ← App Router routes
│   │   ├── (auth)/               ← Auth pages (login, MFA, etc.)
│   │   ├── (marketing)/          ← Public pages
│   │   ├── api/                  ← API route handlers (auth stubs only)
│   │   ├── patient/              ← Patient portal
│   │   ├── clinician/            ← Clinician workspace
│   │   ├── admin/                ← Org admin console
│   │   ├── compliance/           ← Compliance manager dashboard
│   │   ├── auditor/              ← Auditor (read-only)
│   │   ├── super/                ← Super admin (platform)
│   │   ├── globals.css           ← Tailwind v4 tokens + theme
│   │   ├── layout.tsx            ← Root layout (fonts, Toaster)
│   │   └── page.tsx              ← Landing
│   ├── components/
│   │   ├── auth/                 ← Auth-specific UI
│   │   ├── marketing/            ← Marketing site UI
│   │   ├── patient/              ← Patient-only header/sidebar
│   │   ├── shared/               ← Cross-role shared components
│   │   └── ui/                   ← Base UI primitives (button, dialog, input, etc.)
│   ├── lib/
│   │   ├── auth.ts               ← JWT signing/verification (jose)
│   │   ├── patient-store.tsx     ← Patient role local store (localStorage)
│   │   ├── clinician-store.tsx   ← Clinician role local store
│   │   ├── admin-store.tsx       ← Org admin local store
│   │   ├── demo-users.ts         ← Seed user accounts for demo
│   │   ├── login-href.ts         ← Role → login URL helpers
│   │   ├── report-generators.ts  ← PDF/CSV report builders
│   │   └── utils.ts              ← Tailwind cn() helper
│   ├── middleware.ts             ← Auth gate + role-based redirects
│   ├── next.config.ts            ← Security headers + image config
│   ├── package.json
│   └── tsconfig.json
└── report.txt                    ← Last build report (transient)
```

---

## 4. User Roles

The portal supports **6 distinct roles**. Each has its own route prefix, sidebar, and store (where stateful).

| Role | Route prefix | Store | Purpose |
|---|---|---|---|
| Patient | `/patient/*` | `lib/patient-store.tsx` | Own PHI, appointments, consents, messages |
| Clinician | `/clinician/*` | `lib/clinician-store.tsx` | Assigned patients, schedule, notes, prescriptions |
| Org Admin | `/admin/*` | `lib/admin-store.tsx` | Tenant-scoped user/patient/dept mgmt |
| Compliance Manager | `/compliance/*` | (local component state) | Audit logs, policies, anomalies, approvals |
| Auditor | `/auditor/*` | (local component state) | Read-only audit + consent + reports |
| Super Admin | `/super/*` | (local component state) | Cross-tenant platform mgmt |

Marketing/public pages live under `app/(marketing)/*` (no auth).
Auth pages under `app/(auth)/*` (no auth required).

---

## 5. Module Inventory (frontend pages — current state)

All pages listed are present unless marked otherwise. See changelog for additions.

### 5.1 Authentication (`app/(auth)/*`)

`/login` · `/register` · `/forgot-password` · `/reset-password` · `/verify-email` · `/mfa-setup` · `/mfa-challenge` · `/mfa-prompt` · `/mfa-recovery-codes` · `/invite/[token]` · `/account-locked` · `/staff-login` · `/super-login`

### 5.2 Patient (`app/patient/*`)

`/dashboard` · `/records` (+ `[id]`) · `/appointments` (+ `new`, `[id]`) · `/documents` (+ `upload`) · `/consents` (+ `[id]`, `grant`) · `/messages` (+ `[threadId]`) · `/prescriptions` · `/family` · `/emergency` · `/insurance` · `/vaccinations` · `/notifications` · `/settings`

### 5.3 Clinician (`app/clinician/*`)

`/dashboard` · `/patients` (+ `[id]`, `[id]/records`, `[id]/records/new`, `[id]/documents`, `[id]/timeline`) · `/schedule` · `/appointments` (+ `[id]`) · `/messages` (+ `[threadId]`) · `/notes` (+ `new`) · `/prescriptions/[id]` · `/tasks` · `/notifications` · `/settings`

### 5.4 Org Admin (`app/admin/*`)

`/dashboard` · `/users` (+ `invite`, `bulk`) · `/patients` (+ `invite`, `bulk`, `[id]`, `[id]/edit`) · `/clinicians` (+ `invite`, `[id]`) · `/departments` (+ `new`) · `/appointments` · `/billing` · `/consents/campaign` · `/reports` · `/templates` · `/notifications` · `/settings`

### 5.5 Compliance Manager (`app/compliance/*`)

`/dashboard` · `/audit-logs` (+ `[id]`) · `/consents` (+ `[id]`) · `/consent-policies` (+ `new`, `[version]`, `[version]/edit`) · `/anomalies` (+ `[id]`) · `/approvals` (+ `[id]`) · `/campaigns` (+ `[id]`) · `/deletion-requests` (+ `[id]`) · `/retention` · `/reports` · `/notifications` · `/settings`

### 5.6 Auditor (`app/auditor/*`)

`/dashboard` · `/audit-logs` (+ `[id]`) · `/consents` · `/reports` · `/notifications` · `/settings`

### 5.7 Super Admin (`app/super/*`)

`/dashboard` · `/tenants` (+ `new`) · `/platform` (+ `integrations/[slug]`) · `/health` · `/security` · `/incidents` · `/integrations` · `/break-glass` · `/notifications` · `/settings`

### 5.8 Marketing (`app/(marketing)/*`)

`/` · `/about` · `/services` · `/pricing` · `/hipaa` · `/privacy` · `/terms` · `/contact` · `/checkout` (ahead of plan — billing is out of Phase 1 scope)

---

## 6. Shared Components (`components/shared/`)

| Component | File | Purpose |
|---|---|---|
| `action-button.tsx` | Confirm modal + toast wiring for destructive actions |
| `consent-denied-card.tsx` | PHI access-denied empty state; supports `onRequestAccess` CTA |
| `empty-state.tsx` | Standard empty-list state |
| `form-dialogs.tsx` | Invite patient / staff dialogs with token preview |
| `idle-timeout.tsx` | 15-min inactivity timer + 30s warning countdown |
| `logo.tsx` | HealthSecure brand mark |
| `logout-button.tsx` | Session-clearing logout |
| `nav-search.tsx` | Cmd-K style page search (sidebar-aware) |
| `notifications-list.tsx` | Reusable notification feed |
| `onboarding-tour.tsx` | First-time staff onboarding tour — spotlight walkthrough anchored to `data-tour` elements; per-role steps in `TOURS`; persists per role in `localStorage` (`hs_tour_<role>_v1`); replayable via the `hs:start-tour` window event ("Take a tour" in profile menu) |
| `page-header.tsx` | Standard role-page header (eyebrow, title, description, actions) |
| `report-download-button.tsx` | Trigger PDF/CSV report generation |
| `role-header.tsx` | Top bar with search, MFA chip, theme toggle, notification bell (links to `notificationsHref`), profile menu |
| `role-mobile-nav.tsx` | Hamburger drawer nav for narrow screens |
| `role-sidebar.tsx` | Desktop sidebar (groups + utility items) |
| `security-badge.tsx` | 6 variants: encrypted, audited, consent-bound, PHI, MFA, verified |

### Base UI primitives (`components/ui/`)

`avatar.tsx` · `badge.tsx` · `button.tsx` · `card.tsx` · `dialog.tsx` · `dropdown-menu.tsx` · `input.tsx` (also exports `Textarea`, `Label`) · `progress.tsx` · `separator.tsx` · `switch.tsx` · `tabs.tsx`

---

## 7. State Management (`lib/*-store.tsx`)

Each role-specific store uses **React Context + localStorage**, no third-party state library yet. Stores hydrate on first client render and persist on every mutation.

### Patient store ([lib/patient-store.tsx](frontend/lib/patient-store.tsx))
- Storage key: `hs_patient_store_v2`
- Slices: profile, appointments, records, documents, consents, messages, notifications, prescriptions, security (MFA, sessions), **family**, **emergencyContact**, **insurance**, **vaccinations**
- New actions (v2): `addFamilyMember`, `updateFamilyMember`, `removeFamilyMember`, `updateEmergencyContact`, `addInsurancePlan`, `updateInsurancePlan`, `removeInsurancePlan`, `markPrimaryInsurance`, `addVaccination`, `updateVaccination`, `removeVaccination`
- Hydrated demo data: 2 family members, full emergency contact block, 2 insurance plans, 5 vaccinations

### Clinician store ([lib/clinician-store.tsx](frontend/lib/clinician-store.tsx))
- Storage key: `hs_clinician_store_v3`
- Slices: `assignedPatients`, `appointments`, `notes`, `prescriptions`, `documents`, `scheduleTemplate`, `blockedSlots`, `tasks`, `notifications`, `messageDrafts`, `accessRequests`, `profile`
- Sensitive-access approval workflow: `requestAccess`, `simulateApprovalDecision`, `revokeExpiredAccess`
- Helpers: `hasEffectiveConsent`, `activeApprovedRequest`, `pendingRequest`, `patientHasConsent`
- Expiry sweep: every 30s the provider checks `accessRequests` and demotes expired approvals to `"expired"`

### Admin store ([lib/admin-store.tsx](frontend/lib/admin-store.tsx))
- Storage key: `hs_admin_store_v2`
- Slices: `organization`, `staff`, `patients`, `departments`, `assignments`, `auditLedger`, `notifications`, `onboardingChecklist`, retention defaults, password policy
- Seed patient IDs align with `app/admin/patients/patients-data.ts` slugs (`aarav-mehta`, `neha-bansal`, `vikram-rao`)

---

## 8. Authentication & Security

- **Session:** JWT (HS256) via `jose` in [lib/auth.ts](frontend/lib/auth.ts) — 12-hour TTL
- **Cookie:** httpOnly, Secure, SameSite=Strict (in production)
- **Demo users:** [lib/demo-users.ts](frontend/lib/demo-users.ts) — one account per role
- **API stubs:** `app/api/auth/*` — login, logout, register, verify-email, verify-otp, resend-otp (mocked, no backend)
- **Middleware ([middleware.ts](frontend/middleware.ts)):** redirects unauthenticated users to role-appropriate login (`/login`, `/staff-login`, `/super-login`) with `?next=` preserved
- **Security headers ([next.config.ts](frontend/next.config.ts)):** X-Frame-Options, X-Content-Type-Options, Referrer-Policy, Permissions-Policy, HSTS, X-DNS-Prefetch-Control
- **MFA:** mandatory for staff/admin/compliance/auditor/super; optional for patients
- **Audit toasts:** every destructive action surfaces "audit-logged" message via `sonner`

---

## 9. Known Gaps & Open Items

### Confirmed missing (cross-cutting, not pages)

- ❌ `components/ui/skeleton.tsx` — loading skeleton component
- ✅ ~~First-time onboarding tour for clinician + admin~~ — DONE 2026-06-04 (`components/shared/onboarding-tour.tsx`, wired into all 5 staff layouts: clinician, admin, compliance, auditor, super)
- ❌ Help / docs drawer in `role-header.tsx`
- ❌ Masked-field component with click-to-reveal + audit toast
- ❌ Patient-initiated deletion request submission form
- ❌ i18n scaffolding
- ❌ WCAG 2.1 AA accessibility audit pass
- ❌ CSRF token mechanism (needed before backend wiring)
- ❌ Content Security Policy header (deferred — needs nonce wiring for Tailwind v4)

### Demo-only quirks to remember

- All stores persist seeded PHI to **localStorage** — acceptable for demo only; must be removed once real APIs land (requirement doc Sec 5.5)
- Compliance and Super sides use **hardcoded mock data**, not a store
- `/clinician/appointments` index was added to fix a 404; the requirement doc only mandates `/clinician/appointments/[id]`
- `/checkout` exists for the marketing flow but billing is officially out of Phase 1 scope

---

## 10. Build & Verify

```powershell
cd d:/helathsecure/frontend
npm install
npm run dev          # localhost:3000
npm run build        # production build (must exit 0)
npm run lint         # 0 warnings expected
npx tsc --noEmit     # 0 errors expected
npm audit            # 0 vulnerabilities expected
```

Dev login (any role): credentials in [lib/demo-users.ts](frontend/lib/demo-users.ts). Common demo password: `Demo!Pass1234`.

---

## 11. Conventions to Follow

- **No emojis in code** unless the user explicitly asks.
- **No documentation files (`*.md`) created** unless the user asks — only `CLAUDE.md` + `KNOWLEDGE.md` are auto-managed.
- **Edit existing files** before creating new ones.
- **Comments:** none by default; only the rare "why" for a non-obvious invariant.
- **Type-strictness:** `tsconfig.json` has strict mode on — all new code must satisfy it.
- **Tailwind v4 + design tokens:** use `var(--color-foreground)` etc. from `app/globals.css`, never raw hex.
- **`<Suspense>` for `useSearchParams`:** every client component that reads search params must be wrapped in `<Suspense>` or Next 15 build fails (already enforced on existing pages).
- **Security badges:** any new PHI surface should render a `<SecurityBadge>` (encrypted / consent-bound / audited).
- **Action confirms:** any destructive UI action should use `<ActionButton confirm={...}>` for the modal + toast pattern.

---

## 12. Changelog

> Newest entry on top. Format: `YYYY-MM-DD HH:MM - [ADDED/REMOVED/MODIFIED] - what changed and why`

2026-06-09 18:00 - [MODIFIED] - **Fixed Vercel build failure: Prisma Client now generated during the build.** Vercel caches `node_modules`, so Prisma's install-time auto-generation didn't re-run and the build failed with the "outdated Prisma Client" error (the generated client lives in gitignored `node_modules/.prisma/client`). Added `"postinstall": "prisma generate"` and changed `build` to `"prisma generate && next build"` in `frontend/package.json`. Also added the Vercel Linux engine `rhel-openssl-3.0.x` to `binaryTargets` in `frontend/prisma/schema.prisma` (was `["native", "windows"]`). Local `tsc --noEmit` and `next lint` were clean; `prisma generate` succeeds. Reminder: all `.env.local` keys must also be set in Vercel project env vars.

2026-06-09 08:25 - [MODIFIED] - **Patient record NOTE section now renders SOAP with bold section labels (was one run-on paragraph).** On `/patient/records/[id]`, the Note rendered `record.noteBody` as a raw string in a div, so a SOAP note ("Subjective: … Objective: … Assessment: … Plan: …") collapsed into a single line (the API joins sections with `\n\n`, but HTML collapses whitespace, and free-form `n.body` notes have the labels inline). Added a `NoteBody` component + `parseSoap()` that splits the text on the four canonical labels (`/\b(Subjective|Objective|Assessment|Plan)\b\s*:/`) and renders each as its own paragraph with the **label in bold** — matching the exported PDF (second screenshot). Any preamble before the first label and notes with no SOAP labels (e.g. discharge summaries) render as plain blocks with line breaks preserved (`whitespace-pre-line`). File: `frontend/app/patient/records/[id]/page.tsx`. tsc + lint clean.

2026-06-09 08:10 - [FIXED] - **Patient document upload let a scanner-blocked file stay uploadable.** On `/patient/documents/upload`, `lib/document-scanner.scanFile` only ran *inside* `startUpload`, so a dangerous file (e.g. a PDF with a `/OpenAction` active-content tag, or an EICAR/exe/magic-byte mismatch) could be queued with "Start upload (1)" still enabled. Now the file is **scanned the moment it's selected/dropped**: new `scans` state (per-file, aligned with `files`) holds `"scanning"` → `ScanResult`. Blocked files show inline immediately (red row + AlertOctagon + reason, "· N blocked" in the header) and are **excluded from the upload** — the button now reads `Start upload (cleanCount)` and is disabled while any scan is in flight (`Scanning…`) or when `cleanCount === 0`, so an all-blocked selection can't be submitted. `startUpload` reuses the pre-scan result (no double scan) and still blocks any infected file defensively; mixed selections upload only the clean files. Remove-file now splices `files`/`scans`/`items` together; the all-rejected branch clears all three. File: `frontend/app/patient/documents/upload/page.tsx`. tsc + lint clean; page compiles (307 to login when unauthenticated). (Server-side `app/api/clinician/patients/[id]/documents` and `app/api/patient/documents` still accept a `scanStatus` flag from the client — the authoritative ClamAV scan is a backend TODO; this fix hardens the client gate.)

2026-06-09 07:55 - [FIXED] - **Patient appointment detail page showed "Appointment not found" for real DB appointments.** `app/patient/appointments/[id]/page.tsx` was reading the localStorage demo store (`usePatientStore().state.appointments.find(...)`), but the list page and the dashboard "Manage" link both pass **DB** appointment UUIDs — which never exist in the demo store, so every real appointment 404'd on its detail view. Rewrote the page to be **DB-backed**: it now fetches `GET /api/patient/appointments`, finds the row by `id`, and renders the live clinician/date/time/status/mode/notes/duration. Actions are wired to the same `PATCH /api/patient/appointments` endpoint the list uses — Reschedule (`patient_reschedule` via `RescheduleAppointmentDialog`), Cancel (`cancel`), and the reschedule-proposal banner (`accept_reschedule` / `decline_reschedule`) — replacing the old demo-store mutations. Dropped the demo-only attachments section and `usePatientStore`/`patient-store` imports; the right-rail clinician card now uses DB data. Loading → "Loading…", missing → existing NotFound. tsc + lint clean; page compiles (307 to login when unauthenticated, no 500). File: `frontend/app/patient/appointments/[id]/page.tsx`.

2026-06-09 07:40 - [ADDED] - **Action emails on the Priority-19 lifecycle events (email + in-app).** New central dispatcher `frontend/lib/notify.ts` — `sendActionEmail({orgId,to,slug,vars,fallbackSubject,fallbackText})` renders the tenant's *editable* `notification_templates` row by slug (admins manage copy at /admin/templates), falls back to built-in branded copy when no active template exists, and delivers via the tenant's preferred transport (resolved from `organizations.settings.integrations` → `transportsFromIntegrations`). Best-effort: never throws, never blocks the triggering write. Also exports `sendAppointmentEmail(id,status)`, `orgTransports(orgId)`, `appBaseUrl()`, `scopeLabels()`. The in-app leg needs no new code — the notification feed (`/api/notifications`) is **derived** from domain tables, so it updates automatically when the underlying row changes; these helpers add the missing **email** leg. Wired the ~7 priority actions that previously sent nothing:
  - **Appointment confirmed / rejected / reschedule-requested** → patient. Hooked into `lib/appointment-lifecycle.ts › transitionStatus` (covers clinician + patient flows) for those three statuses; the Org-Admin route `app/api/admin/appointments/[id]/route.ts` (writes via `prisma.update`, not `transitionStatus`) also calls `sendAppointmentEmail` on confirm. Slugs: `appointment-confirmed`, `reschedule-request` (seeded), `appointment-rejected` (fallback copy).
  - **Consent request** → patient, in `app/api/clinician/consent-requests/route.ts` POST (slug `consent-request`, seeded).
  - **Consent revoked** → clinician, in `app/api/patient/consents/route.ts` DELETE (slug `consent-revoked`, fallback copy).
  - **New document available** → patient, in `app/api/clinician/patients/[id]/documents/route.ts` POST (slug `new-record-available`, seeded).
  - **Invoice generated + Payment success** → Org Admin / payer, in `lib/billing.ts › finalizePayment` after the invoice INSERT (single combined email; idempotent — the existing PI early-return prevents double-send; slug `payment-success`, fallback copy).
  Already-wired Priority-19 items left as-is (tenant/user invitation, password reset, login OTP = email verification, T-24h/T-1h reminders, break-glass start, incident create). Admins can create editable templates for the new slugs (`appointment-rejected`, `consent-revoked`, `payment-success`) at /admin/templates; until then the built-in fallback copy is used. tsc + lint clean; changed routes compile (verified via dev server: 401/405, no import errors).

2026-06-09 07:05 - [FIXED] - **Patient header avatar shows the uploaded profile photo (was always initials).** The Settings card showed the saved photo but the top-right header avatar always rendered the "PD" initials. `/api/patient/dashboard` already returns `profile.profilePhotoUrl`, so `components/patient/header.tsx` now adds `avatarUrl` to its user state and renders `<AvatarImage src={user.avatarUrl}/>` (only when set) alongside `<AvatarFallback>{initials}</AvatarFallback>` — Radix shows the photo when available and falls back to initials when there's no image (or it fails to load). Imported `AvatarImage`. File: `frontend/components/patient/header.tsx`. Typecheck + lint clean. (The shared `RoleHeader` for staff/admin/super still shows initials only — `/api/auth/me` doesn't return profilePhotoUrl; extend there the same way if profile photos are wanted in those headers.)

2026-06-09 06:55 - [FIXED] - **Patient dashboard widgets now show real DB data (was localStorage demo seed).** `app/patient/dashboard/page.tsx`: (1) **Next appointment (hero) + Upcoming appointments** now come from `/api/patient/appointments` (mapped DB rows) instead of `state.appointments` — the demo fallback ("Dr. rahul sharma / Annual health check-up") is gone; empty → "No upcoming appointments". The widget's per-item Reschedule/Cancel (which wrote to the demo store) were replaced with a **Manage** link to the DB-backed appointments page. (2) **Recent messages** widget rewritten as a self-fetching `RecentMessages` that reads `/api/messages/threads` (otherName/lastBody/lastSentAt/unread) instead of `state.threads` (demo "Dr. Rohan Iyer · Hello"). (3) **Activity** feed rewritten to fetch `/api/notifications` (the real per-patient event aggregate) and render the top 5 with a category→icon map, instead of the hardcoded 4-item list ("Consent granted to Dr. Shah", "lipid panel PDF", "Signed in from Chrome · Mumbai", "Reviewed policy v2.4"). QuickStats were already DB-backed. Removed now-unused imports (CheckCircle2, RescheduleDialog, CancelAppointmentDialog, MessageThread type) and the `state.appointments/threads` reads. **Verified** (aarav): appointments 0, threads 0, notifications 1 real item ("Your data deletion request was reviewed") → dashboard renders real/empty states, no demo. File: `frontend/app/patient/dashboard/page.tsx`. Typecheck + lint clean.

2026-06-09 06:30 - [FIXED] - **Patient appointments page now shows ONLY real DB rows (demo-seed leak fixed).** A new patient (e.g. Prachi) with `/api/patient/appointments` → `{ok:true,appointments:[]}` still saw seeded demo appointments ("Annual health check-up", Dr. rahul sharma). Cause: `app/patient/appointments/page.tsx` `mergeAppointments(db, local)` returned the localStorage `patient-store` demo list whenever the DB list was empty. Removed that fallback — `allAppointments = dbAppointments` (DB is the only source of truth; booking already goes through `POST /api/patient/appointments`). Empty-state checks switched from `state.appointments.length` to `dbAppointments.length`, so an empty API now renders "No appointments yet". The store handlers (reschedule/cancel for legacy slug ids) remain but never fire since only DB (uuid) rows display. File: `frontend/app/patient/appointments/page.tsx`. Typecheck + lint clean. (Same demo-store pattern may still exist on the patient dashboard's upcoming-appointments widget — fix there too if reported.)

2026-06-09 06:10 - [ADDED] - **Real patient-invite pipeline driven by the editable "Welcome (patient invitation)" template.** The Admin → Patients → Register/Invite form was a stub (toast only, no patient, no email). Now it creates the patient and emails them an invitation rendered from the org's `welcome-invitation` notification template. Parts: (1) **`lib/notification-template.ts`** `renderTemplate(orgId, slug, vars)` — loads the active row from `notification_templates` and substitutes `{{dotted.key}}` placeholders. (2) **`lib/auth.ts`** `signResetToken(claims, ttlSeconds?)` now takes an optional TTL + new `INVITE_TTL_SECONDS` (72h) — invitation links live longer than 30-min resets. (3) **`POST /api/admin/patients/invite`** (NEW, Org-Admin): creates the patient user (`roleKind=patient`, `status=invited`, tenant = admin's org, throwaway passwordHash, mfaRequired=false), mints a 72h reset token → `action_url = /reset-password?token=…`, renders `welcome-invitation` with `{patient.first_name, patient.last_name, organization.name, clinic.name, action_url}`, and `sendMail`s it (falls back to a basic email + returns the link in dev when no mailer). The patient clicks → existing `/reset-password` page → sets password → `/api/auth/reset-password` flips invited→active. (4) **`app/admin/patients/invite/page.tsx`** — submit now POSTs the invite (email required; `name` attrs added to the 6 core fields read via FormData), buttons relabeled "Send invitation", the admin password field disabled (patient sets their own). **Verified** (as komal/city hospital): invite → `templateUsed:true`, `mailSent:true` via SendGrid, patient row created (invited), accept link uses the reset flow (test invitee removed after). Note: the heavy demographics/PHI/clinician-assignment fields on the form remain UI-only (not persisted) — the patient completes their profile after accepting; full demographics persistence is a separate task. Files: `frontend/lib/notification-template.ts` (new), `frontend/lib/auth.ts`, `frontend/app/api/admin/patients/invite/route.ts` (new), `frontend/app/admin/patients/invite/page.tsx`. Typecheck + lint clean.

2026-06-09 05:35 - [ADDED] - **"Current plan" badge on the pricing page for the subscriber's active tier.** When a signed-in Org Admin with an active subscription views `/pricing`, the tier card matching their plan shows a green **Current plan** badge (with a success ring + tinted card) and its CTA becomes **Manage plan → /admin/billing** instead of "Get started". `lib/billing.ts`: new `currentUserActiveTierId()` (reads the session via `cookies()` + `verifySession`, returns the active `tenant_subscriptions.tierId` for the Org Admin's org, else null — null for logged-out/non-admin). `app/(marketing)/pricing/page.tsx` (already `force-dynamic`): resolves it via `Promise.all([getTiers(), currentUserActiveTierId()])` and passes `current={t.id === currentTierId}` to `TierCard`; `TierCard` gained a `current` prop that swaps the badge (Current plan > Most popular) + the CTA. Verified: komal (Hospital sub) → Hospital card shows Current plan + Manage plan; logged-out → no badge. Files: `frontend/lib/billing.ts`, `frontend/app/(marketing)/pricing/page.tsx`. Typecheck + lint clean.

2026-06-09 05:10 - [MODIFIED] - **Fixed dropdown crash "Cannot read properties of null (reading 'focus')" in the header user menu** (all roles). Root cause: `components/shared/logout-button.tsx` `LogoutButton` is rendered as `<DropdownMenuItem asChild><LogoutButton/></DropdownMenuItem>`, but it was a plain function component that **didn't forward a ref** (and dropped Radix's injected props). Radix's focus scope then called `.focus()` on the item's null DOM node when the menu opened → runtime TypeError (surfaced via `RoleHeader` → Radix `DropdownMenuContent`). Fix: rewrote `LogoutButton` as `React.forwardRef<HTMLButtonElement, ComponentPropsWithoutRef<"button">>` that forwards `ref` and spreads `...props` (data-radix-collection-item, role, tabIndex, focus handlers) onto the `<button>`, composing the injected `onClick` with `handleLogout`. Backward-compatible with all call sites (they only passed `className`). Affected every layout's header menu (RoleHeader), not just Super. Verified: `/super/dashboard` serves 200 and the profile menu opens without crashing; tsc + lint clean. File: `frontend/components/shared/logout-button.tsx`.

2026-06-09 04:55 - [ADDED] - **Hard subscription gate on the Org-Admin dashboard.** Previously a tenant admin without a subscription was only soft-redirected to /pricing at login but could still navigate to /admin/* directly. Now they cannot reach the dashboard (or any /admin page) until the tenant has an active subscription. (1) **`/api/me/subscription`** (NEW GET) → `{active}` using `orgHasActiveSubscription(orgId)` (org resolved from the session user); demo (non-UUID) sessions and non-Org-Admin roles report `active:true` (not gated). (2) **`app/admin/layout.tsx`** — on mount fetches the gate; while checking it renders a full-screen loader (no flash of the dashboard); if `active===false` it `router.replace("/pricing")` and shows "Redirecting to plans…"; only renders the sidebar/children when `active===true`. After payment → finalize activates the subscription → the next /admin load passes the gate. **Verified**: komal (city hospital, has sub) → `active:true`; rinokat (hospital org, no sub) → `active:false`. Note: this gates **pages** for ALL DB Org Admins without a subscription (existing seeded tenants like "city"/"hospital" admins are now forced to /pricing until they subscribe); `/api/admin/*` data endpoints keep their existing role checks but are not subscription-gated. Files: `frontend/app/api/me/subscription/route.ts` (new), `frontend/app/admin/layout.tsx`. Typecheck + lint clean for changed files (pre-existing stale eslint-disable in `app/admin/templates/page.tsx:599` is unrelated).

2026-06-09 04:30 - [ADDED] - **"Organization contact" fields on the Provision-a-tenant form.** Super Admin can now enter contact email / phone / address when creating a tenant; they persist to `organizations.settings.contact` (JSONB — no schema change). `app/super/tenants/new/page.tsx`: new "Organization contact" card (3 optional inputs) + state + `contact` in the POST body + a Contact line in the provisioning summary. `app/api/super/tenants/route.ts`: `CreateTenantBody.contact?`, a trimmed `contact = {email,phone,address}`, merged into `settings: { integrations, contact }`. These feed the tenant's own **Admin → Settings → Profile** tab and `/api/admin/tenant` (which already read `settings.contact`), so the tenant starts pre-filled instead of blank. Optional — provisioning still works with them empty. **Verified**: provisioned a tenant with contact → `settings->'contact'` = `{email,phone,address}` exactly as entered (test tenant then removed). Typecheck + lint clean (pre-existing `tenants/route.ts` InputJsonValue baseline unchanged). Other suggested fields offered but not chosen this round: branding, trial toggle, initial security policy, admin phone, seat limit, audit retention, timezone/locale, internal note.

2026-06-09 04:10 - [REMOVED] - **"Subscription tier" field removed from the Provision-a-tenant form.** Tenants now start on Basic and their tier is set when the Org Admin subscribes via the pricing/checkout flow (`finalizePayment` updates `organizations.tier`), so picking a tier at creation was redundant/misleading. `app/super/tenants/new/page.tsx`: removed the tier `<select>` (replaced with a one-line note "New tenants start on Basic. The plan is set when the Org Admin subscribes…"), the `tier` state, the `TIERS` const, the `tier` field in the POST body, and the "Tier" row in the provisioning summary. The API (`/api/super/tenants` POST) already defaults `body.tier ?? "Basic"`, so omitting it is safe — no API change needed. Typecheck + lint clean.

2026-06-09 03:55 - [MODIFIED] - **Invoice PDF redesigned (`lib/invoice-pdf.ts`).** The previous layout was plain (header + flat rows). `renderInvoicePdf` now renders a professional invoice: teal header band with a rounded logo mark + "INVOICE"/number + a rounded **PAID** status pill; a two-column **Billed to** (name/org/email) vs **Invoice details** (Issued, Billing period) block; a real line-items table (Description / Qty / Amount with header + rules); a right-aligned **totals box** that reconstructs **Subtotal + Tax (GST/VAT 9%) + Total paid** from the stored total (`subtotal = round(amount/1.09)`, since only the gross is stored); a Payment method + Transaction ID block; and a footer. Shared by the thank-you page, Admin billing, and Super tenant billing "Download PDF". Typecheck + lint clean.

2026-06-09 03:40 - [ADDED] - **Super Admin "Billing" module — tenant-wise payment overview.** New top-level nav item (Platform group, between Tenants and Configuration) → `/super/billing`. **`GET /api/super/billing`** (Super-Admin) returns every tenant with its current subscription (plan, cycle, status, card brand/last4, next billing) + an invoice rollup (count, total collected, last invoice number/amount/date) via `organizations LEFT JOIN tenant_subscriptions LEFT JOIN LATERAL (invoice aggregate)`, ordered active-first; plus a summary (tenants, active subscriptions, total collected). **`app/super/billing/page.tsx`** renders 3 summary stats + a searchable tenant-wise table (Tenant, Plan, Subscription status, Card, Last payment, Next billing, Total paid) where each row links to the existing per-tenant `/super/tenants/[id]/billing` invoice history. **`app/super/layout.tsx`** gained the `Receipt`-icon Billing nav item. Verified: API returned 4 tenants / 2 active subs / $163,391 collected (city hospital INV-2026-0001, mother care INV-2026-0002), page renders 200. Files: `frontend/app/api/super/billing/route.ts` (new), `frontend/app/super/billing/page.tsx` (new), `frontend/app/super/layout.tsx`. Typecheck clean; lint clean for new files (pre-existing `super/security` useMemo warning untouched).

2026-06-09 03:05 - [MODIFIED] - **Login now trims whitespace on the submitted password (staff onboarding paste fix).** A newly-onboarded tenant admin failed to sign in because the copy-pasted temp password carried a leading TAB (`"\t_LjQX&7Hv33zAZY$"` in the login payload) → bcrypt mismatch → "Email or password is incorrect." `generateTempPassword()` only emits clean chars (no whitespace), so the tab came from the paste. `app/api/auth/login/route.ts` now does `const pw = password.trim()` before both the DB bcrypt compare and the demo-user path (our passwords never have leading/trailing whitespace, so trimming is safe and prevents paste artifacts from breaking onboarding logins). Verified: correct password, and a `"\tTest@123 "` variant, both succeed; a wrong password is still rejected. Also note: onboard links are single-use — after the token is consumed the admin signs in with the temp password directly (or Forgot password). File: `frontend/app/api/auth/login/route.ts`. Lint clean.

2026-06-09 02:45 - [ADDED] - **Tenant subscription + billing/invoices pipeline (post-payment persistence + activation).** Closes the gap where Stripe payment worked but nothing was recorded/activated. (1) **DDL** (local PG18 + Neon): `tenant_subscriptions` (one row/org, `UNIQUE("organizationId")`: tier, cycle, qty, status, amountCents, currency, cardBrand, cardLast4, stripePaymentIntentId, currentPeriodStart/End) + `tenant_invoices` (history: number, tierName, cycle, qty, amountCents, status, cardBrand/last4, stripePaymentIntentId UNIQUE for idempotency, billingName/email, period, createdAt). (2) **`lib/billing.ts`** — `finalizePayment(piId)`: retrieves the PI from Stripe (expand `latest_charge.payment_method_details`), requires `succeeded`, **idempotent** (returns existing invoice if the PI is already recorded), upserts the subscription, **activates the org** (`organizations.status='active'::"TenantStatus"`, `tier=`mapped `::"TenantTier"` — solo→basic/hospital→pro/enterprise→enterprise), inserts a `tenant_invoices` row with a generated `INV-YYYY-####`; also `orgHasActiveSubscription(orgId)`. **Finalize-on-return** (no webhook). (3) **`payment-intent` route** now resolves the signed-in buyer's org and puts `organizationId` in the PI metadata. (4) **Thank-you page** calls `finalizePayment(payment_intent)` server-side then shows the real invoice (plan, amount, card brand/last4, txn id) + a **Download invoice** button (`lib/invoice-pdf.ts` `renderInvoicePdf` — branded jsPDF) + Go to dashboard. (5) **Admin billing** (`/admin/billing`) is now DB-backed via new **`GET /api/admin/billing`** (subscription + invoices + real Download PDF; empty state → /pricing) — removed the hardcoded INVOICES. (6) **Super Admin → Tenants → row menu → "Billing & invoices"** → new page `app/super/tenants/[id]/billing/page.tsx` backed by **`GET /api/super/tenants/[id]/billing`**: plan, status, next billing, card brand/last4, invoice + payment-history table (number, date, txn id, amount, status, Download PDF). (7) **Onboarding**: `verify-otp` redirects an `org_admin` with NO active subscription to `/pricing` (soft redirect; dashboard not hard-gated). **Verified end-to-end** (org admin buy hospital×100 → confirm pm_card_visa → finalize): subscription active (visa 4242), invoice INV-2026-0001 paid, org tier→pro + status active, idempotent on reload, Admin + Super billing APIs return the real data. Files: DDL (both DBs), `frontend/lib/billing.ts` + `frontend/lib/invoice-pdf.ts` (new), `frontend/app/api/checkout/payment-intent/route.ts`, `frontend/app/(marketing)/checkout/thank-you/page.tsx` (+ `download-invoice-button.tsx`), `frontend/app/api/admin/billing/route.ts` (new) + `frontend/app/admin/billing/page.tsx`, `frontend/app/api/super/tenants/[id]/billing/route.ts` (new) + `frontend/app/super/tenants/[id]/billing/page.tsx` (new) + `tenant-row-menu.tsx`, `frontend/app/api/auth/verify-otp/route.ts`. Typecheck + lint clean. Dev writes hit local PG18 only (Neon has the schema, not the dev rows).

2026-06-09 01:45 - [MODIFIED] - **Checkout switched from Stripe hosted-redirect to an EMBEDDED card form (Stripe Payment Element).** User wanted card fields on the page (Cardholder name + Card information), not a third-party redirect. Installed `@stripe/stripe-js` + `@stripe/react-stripe-js`; `lib/stripe-client.ts` loads the publishable key once. New **`/api/checkout/payment-intent`** (POST) recomputes the amount server-side from `subscription_tiers` (same authoritative pricing as the session route) and creates a PaymentIntent with `automatic_payment_methods`, returning the `client_secret`. `app/(marketing)/checkout/page.tsx` refactored: the page content is wrapped in `<Elements>` (deferred mode: `{mode:'payment', amount, currency}`), a new `CheckoutForm` child uses `useStripe`/`useElements`, renders a **Cardholder name** input + **`<PaymentElement>`** ("Card information"), keeps the amount synced via `elements.update({amount})` on cycle/qty change, and on Place order does `elements.submit()` → create PaymentIntent → `stripe.confirmPayment({elements, clientSecret, confirmParams:{ return_url:/checkout/thank-you, payment_method_data:{billing_details}}})`. The `EnterpriseContact` early-return moved to the outer wrapper so no hooks run conditionally. Thank-you page now handles the embedded-flow `?payment_intent=` redirect (retrieve PaymentIntent) in addition to `?session_id=` (hosted). The hosted `/api/checkout/session` route is left in place as an unused alternative. Card data still goes directly to Stripe (PCI-safe via the Element iframe) — never to our server. **Verified**: PaymentIntent created (`pi_…`, $797,880 for hospital 500×annual, matching the live DB price), `/checkout` renders the Cardholder-name + Card-information fields. Files: `frontend/lib/stripe-client.ts` (new), `frontend/app/api/checkout/payment-intent/route.ts` (new), `frontend/app/(marketing)/checkout/page.tsx`, `frontend/app/(marketing)/checkout/thank-you/page.tsx`, `package.json` (2 stripe client deps). Typecheck + lint clean.

2026-06-09 01:15 - [ADDED] - **Real Stripe checkout (test mode) + pre-filled billing + thank-you page** (was a fake on-page card form that just toasted and went to /admin/billing). Now: open `/checkout?tier=…` → billing details pre-filled from the signed-in user → Place order → **Stripe-hosted Checkout** → pay (test card 4242…) → redirect to a **thank-you page** showing the order. Parts: (1) **Stripe SDK** installed (`stripe@22.2.0`); keys in `.env.local` (gitignored) — `STRIPE_SECRET_KEY` (server-only), `NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY`, `NEXT_PUBLIC_APP_URL`. (2) **`lib/stripe.ts`** — server Stripe singleton + `appBaseUrl()`. (3) **`/api/checkout/session`** (NEW POST) — recomputes price **server-side from `subscription_tiers`** (never trusts the client amount; ties into the dynamic-tiers feature), builds a `mode: "payment"` Checkout Session with plan + 9% tax line items, `customer_email`, metadata (tier/cycle/qty/org), `success_url=/checkout/thank-you?session_id={CHECKOUT_SESSION_ID}`, returns the hosted URL. (4) **`/api/me/billing-prefill`** (NEW GET) — name/email from session claims + org name resolved from `organizations`; blanks for prospects. (5) **`app/(marketing)/checkout/page.tsx`** — pre-fills billing on mount, pulls live tier prices from `/api/tiers` so displayed + charged amounts match Super Admin's config, replaced the fake card inputs with a "you'll pay on Stripe" panel, and Place order now POSTs the session and `window.location`-redirects to Stripe (card fields/validation + formatCardNumber/formatExp removed; canSubmit now just needs name/email/org). (6) **`app/(marketing)/checkout/thank-you/page.tsx`** (NEW, force-dynamic) — retrieves the Checkout Session server-side and shows plan, amount paid, receipt email, reference + dashboard CTA. **Verified**: session create returned a real `cs_test_…` checkout URL; prefill returned the logged-in user's name/email/org. Files: `frontend/lib/stripe.ts` (new), `frontend/app/api/checkout/session/route.ts` (new), `frontend/app/api/me/billing-prefill/route.ts` (new), `frontend/app/(marketing)/checkout/page.tsx`, `frontend/app/(marketing)/checkout/thank-you/page.tsx` (new), `.env.local` (keys, gitignored), `package.json` (stripe dep). Typecheck + lint clean. SECURITY: secret key only in `.env.local` (never committed, never sent to client).

2026-06-09 00:30 - [ADDED] - **Subscription tiers are now dynamic + shared between Super Admin and the public pricing page (were two separate hardcoded arrays).** Before: `app/super/platform/page.tsx` had its own `TIERS` (Basic/Pro/Enterprise · $99/$399/Custom) and `app/(marketing)/pricing/page.tsx` had a different `TIERS` (Solo Practice/Hospital/Enterprise · $149/$7/Custom); the super "Edit tier" dialog only toasted. Now both read one DB source and Super Admin edits go live on the marketing site. (1) **DDL** — new `subscription_tiers` table on local PG18 + Neon (`id, name, tagline, icon, monthly, "annualMonthly", unit, "ctaLabel", "ctaHref", featured, bullets jsonb, "sortOrder"`), seeded with the 3 marketing tiers (Solo/Hospital/Enterprise). (2) **`lib/tiers.ts`** — `SubscriptionTier` type + `getTiers()` (prisma read, ordered). (3) **`/api/tiers`** (NEW public GET, `force-dynamic`) for the marketing page + super tab; **`/api/super/tiers/[id]`** (NEW PATCH, Super-Admin-only) merges editable fields onto the current row (name/tagline/monthly/annualMonthly/unit/ctaLabel/featured/bullets — `monthly`/`annual` nullable = "Custom", so merge in JS not COALESCE) and enforces a single `featured` tier. (4) **Marketing `pricing/page.tsx`** — now an async server component (`force-dynamic`) building tiers from `getTiers()` with an `icon` id→lucide map (solo→Stethoscope, hospital→Building2, enterprise→ShieldCheck); the comparison-table headers use the same dynamic tiers. (5) **Super tiers** — new client `tiers-tab.tsx` fetches `/api/tiers` and renders the SAME marketing content (name, tagline, $price/unit, annual, bullets, Most-popular badge); `edit-tier-dialog.tsx` rewritten to PATCH the API and update both surfaces (fields: name, tagline, monthly [blank=Custom], annual, unit, CTA, featured switch, bullets one-per-line). Removed the static `TIERS`/`TierSpec` + Basic/Pro from the super page. **Verified end-to-end**: GET returns the 3 tiers; a Super-Admin edit (solo→$20, hospital featured) persisted and rendered live on `/pricing`. Files: `subscription_tiers` DDL (both DBs), `frontend/lib/tiers.ts` (new), `frontend/app/api/tiers/route.ts` (new), `frontend/app/api/super/tiers/[id]/route.ts` (new), `frontend/app/(marketing)/pricing/page.tsx`, `frontend/app/super/platform/page.tsx`, `frontend/app/super/platform/tiers-tab.tsx` (new), `frontend/app/super/platform/edit-tier-dialog.tsx`. Typecheck + lint clean. NOTE: dev writes hit local PG18 only (dual-write disabled) — Neon has the seed defaults; re-apply edits there when switching back to Neon for prod.

2026-06-08 23:30 - [ADDED] - **Right-to-be-forgotten now does real, retention-aware deletion (was a static matrix that deleted nothing).** Before: the Compliance decision matrix showed hardcoded category counts ("32 clinical notes", "47 threads") and approving only toasted. Now the full GDPR/HIPAA workflow is live: dynamic per-patient counts → retention-aware suggested decision per category → admin override (justification mandatory) → **actual purge** → patient notified with a what-was-deleted/retained summary. Five parts: (1) **`deletion-requests-data.ts`** — extracted a keyed `CATEGORY_TEMPLATES` (the 6 categories: personal_info, recent_records ≤7y, old_records >7y, audit_logs, consent_records, messages) with retention copy + `defaultDisposition` + a `patientExplanation` line; added `CATEGORY_ORDER`. (2) **`/api/compliance/data-requests/[id]/categories`** (NEW GET) — counts the request-patient's real rows (`medical_records`/`prescriptions`/`patient_documents` split by a `NOW() - INTERVAL '7 years'` boundary, `record_access_log` by actorId, `consent_requests`, `messages`) in one `Promise.all`, returns category descriptors with dynamic `held` + suggested disposition (export request → all export-only). (3) **`/api/compliance/data-requests/[id]` PATCH** — now accepts `decisions{categoryKey→delete|keep|export-only}`; for an approved **deletion**, `executePurge()` runs per category: soft-delete (`deletedAt=NOW()`) on medical_records/prescriptions/patient_documents, hard-DELETE on record_access_log/consent_requests/messages (no deletedAt col), and **anonymize** the shared `users` row for personal_info (name→"Deleted Patient", phone/dob/gender/photo→NULL — row kept for FK integrity); builds a "Deleted: … Retained: … (reason)" summary folded into `decisionNote`. (4) **Compliance detail page** fetches `/categories` and feeds real categories into `DecisionMatrix` (which now also POSTs the chosen `decisions` and shows the patient-facing line per row). (5) **Patient** `DataTab` delete card lists the 6 categories (info-only); **`notifications/route.ts` patientFeed** gained a query for the patient's decided `data_requests` → emits a "Your data deletion request was reviewed" notification carrying the summary. **End-to-end verified** (local PG18): categories showed real counts (consent_records "1 consent records"), approving consent+messages=delete dropped Aarav's consent rows 1→0, summary stored, patient notification appeared. Decision = **soft-delete + anonymize** (not hard purge); deletes scoped to the patient; legalHold still blocks. Files: `deletion-requests-data.ts`, `app/api/compliance/data-requests/[id]/categories/route.ts` (new), `app/api/compliance/data-requests/[id]/route.ts`, `app/compliance/deletion-requests/[id]/page.tsx`, `app/compliance/deletion-requests/decision-matrix.tsx`, `app/patient/settings/page.tsx`, `app/api/notifications/route.ts`. Typecheck + lint clean. Test deletes ran on the LOCAL dev DB only (Neon untouched).

2026-06-08 22:30 - [ADDED] - **Patient data requests (deletion/export) now flow to the Compliance queue (was demo-only).** Before: the patient's Settings → Data rights → "Request deletion" only fired a toast, "Download export now" only downloaded a local JSON, and the Compliance "Patient data requests" page (`/compliance/deletion-requests`) rendered a hardcoded `DELETION_REQUESTS` array. Now it's a real round-trip. (1) **DDL** — new `data_requests` table on BOTH local PG18 + Neon (`id, "patientId", "organizationId", type, status, channel, "legalHold", "legalHoldReason", "decisionNote", "decidedById", "decidedByName", "decidedAt", "requestedAt"`, + indexes on org/status and patient). (2) **`/api/patient/data-requests`** (NEW) — `POST {type:'deletion'|'export'}` writes a row stamped with the patient's tenant (own `organizationId`, else the active care-team clinician's org), deduping open same-type requests; deletion starts `pending`, export `in_progress`. `GET` lists the patient's own. (3) **Patient `DataTab`** (`app/patient/settings/page.tsx`) — "Request deletion" `ActionButton` now `onClick`s the API (confirm dialog kept); `exportPhi()` also logs an `export` request after the download. (4) **`/api/compliance/data-requests`** (NEW) `GET` lists the Compliance Manager's tenant requests (joined to the patient user for name/email/derived MRN); **`/api/compliance/data-requests/[id]`** (NEW) `GET` one + `PATCH {status,note}` records the decision (`approved_partial|approved_full|rejected`, stamps `decidedByName`/`decidedAt`, blocks if `legalHold`). IMPORTANT gotcha: `claims.org` is the org **slug**, not the UUID — both compliance routes resolve the real org id via `SELECT "organizationId" FROM users WHERE id = uid` (same pattern the notification feeds use); don't cast `claims.org::uuid`. (5) **Compliance pages** — list `page.tsx` and detail `[id]/page.tsx` converted from static-array server components to **client components fetching the API** (removed `generateStaticParams`); `DecisionMatrix` approve/reject now `PATCH` and refresh. The retention-policy matrix still uses the static `STANDARD_CATEGORIES` template (now exported) — per-category counts are reference, not per-patient. **End-to-end verified** on local PG18: Aarav Mehta (patient) → deletion request → appears in Meera Singh's (compliance, org_city_hospital) Open queue → decided to approved_partial (decidedBy=Meera Singh, moved to Decided). Files: `data_requests` DDL (both DBs), `frontend/app/api/patient/data-requests/route.ts` (new), `frontend/app/api/compliance/data-requests/route.ts` (new), `frontend/app/api/compliance/data-requests/[id]/route.ts` (new), `frontend/app/patient/settings/page.tsx`, `frontend/app/compliance/deletion-requests/page.tsx` + `[id]/page.tsx` + `decision-matrix.tsx` + `deletion-requests-data.ts` (export STANDARD_CATEGORIES). Typecheck + lint clean. Note: Next.js route files may only export HTTP handlers/`runtime` — keep shared helpers local (un-exported).

2026-06-08 21:30 - [MODIFIED] - **API latency fix — slow API calls were Neon-distance latency, not code.** Measured root cause: the dev machine (India) talks to **Neon Postgres in AWS us-east-1 (US East)** → **~254 ms per query** round-trip (even `SELECT 1`) + **~2 s** cold connection (TLS + `channel_binding`). This is geographic; it does NOT exist in production when the app server is co-located with Neon. Two fixes: (1) **Code — parallelized `app/api/notifications/route.ts`.** Every per-role feed (`patientFeed` 5 queries, `clinicianFeed` 3, `adminFeed`/`complianceFeed`/`auditorFeed` 2 each after resolving `orgId`, `superFeed` 2) ran its `$queryRaw` calls **sequentially** (await…await), so the patient feed alone was 5 × 254 ms ≈ 1.3 s. Wrapped the independent queries in `Promise.all` (date windows hoisted above the batch) → one wall-clock round-trip instead of N. Same output/ordering; only latency improves. (2) **Dev infra — switched local dev to a LOCAL PostgreSQL 18 mirror.** Neon is PG **18.4**; the machine only had PG **17.10**, so `pg_dump 17` couldn't dump it. Installed **PG18 portable binaries** at `C:\pg18` (no admin/service), `initdb` a cluster on **port 5433**, mirrored Neon via PG18 `pg_dump`→restore (38 users / 15 docs / 8 access-log rows; auditor login `mebila4127` present), and pointed `frontend/.env.local` (all three DB vars) at `postgresql://postgres:1234@localhost:5433/helath_secure` — dual-write disabled (shadow==primary), so **dev writes stay local and do NOT reach Neon**. Local query latency now **~0.1 ms** vs 254 ms; warm `/api/notifications` dropped **~1.3–4.8 s → ~0.08 s**, `/api/messages/threads` **~9 s → ~0.07 s**. Restart the local DB after a reboot with `C:\pg18\start-local-db.cmd`; to revert to Neon for prod, swap the commented blocks in `.env.local`. NOTE: `.env.local` is gitignored (Neon creds never committed); only the `notifications/route.ts` parallelization is a code change to commit. Production guidance: deploy the app in us-east-1 (co-located with Neon) or move Neon to `ap-south-1` (Mumbai) if hosting from India.

2026-06-08 20:10 - [ADDED] - **Per-user "Login verification (OTP)" toggle on every role's profile/settings** (GitHub-style switch — choose whether sign-in asks for a one-time code). Drives `users.mfaRequired`, the exact flag `app/api/auth/login/route.ts` reads: ON → sign-in challenges with an OTP (authenticator-app code when TOTP enrolled, else emailed code); OFF → a DB user signs straight into a session (the login fast-path). Three parts: (1) **`/api/me/login-otp`** (NEW, session-required) — `GET` returns `{otpRequired, totpEnrolled, demo}` for the signed-in user; `PUT {otpRequired:boolean}` updates `mfaRequired`. Demo (non-UUID) sessions can't persist (they always get an OTP) → GET reports `demo:true` and PUT is rejected, so the UI renders read-only. (2) **`components/shared/login-otp-toggle.tsx`** (NEW) — self-contained card (`<LoginOtpToggle />`): fetches state on mount, optimistic Switch with saving spinner + revert-on-error, On/Off badge, a "uses authenticator app / sends email code" sublabel, and a warning line when OFF. Drop-in (own border/title) so it fits any layout. (3) Wired into all six role settings — `app/patient/settings/page.tsx` + `app/clinician/settings/page.tsx` + `app/compliance/settings/page.tsx` + `app/super/settings/page.tsx` (inside their Security tab), `app/auditor/settings/page.tsx` (in the Security card stack), and `app/admin/settings/page.tsx` (admin has no personal Security tab — added as an account card under the org Profile tab). **End-to-end tested** (auditor account): login → GET `otpRequired:false` → PUT true → PUT false, all persisted. Files: `frontend/app/api/me/login-otp/route.ts` (new), `frontend/components/shared/login-otp-toggle.tsx` (new), + the six settings pages. Typecheck clean; lint clean for changed files (pre-existing warnings in `app/super/security/page.tsx` untouched).

2026-06-08 19:25 - [ADDED] - **Name field validation (2–15 chars) on profile editors.** New shared helper `lib/validate-name.ts` (`validateName(value, label)` → error string | null; `NAME_MIN=2`, `NAME_MAX=15`; trims, also rejects empty). Wired into the only two role profiles with an editable name — **patient** (`app/patient/settings/page.tsx`) and **clinician** (`app/clinician/settings/page.tsx`): the local `Field` component gained an `error?` prop (red border + `aria-invalid` + red helper text under the input), First/Last name show the live error as you type, the **Save button is disabled** while either name is invalid, and `handleSave` re-checks + toasts before submitting. Server-side defense-in-depth: `app/api/patient/profile/route.ts` PATCH now runs the same `validateName` on firstName/lastName (was: non-empty only) so a tampered request is rejected with the same message. The other roles' profiles need no change — auditor shows the name **read-only** (`app/auditor/settings/page.tsx`), and admin/compliance/super settings have **no editable own-name field** (those pages are org/anomaly/platform settings). Clinician profile persists via the localStorage store (no API), patient via the DB. Files: `frontend/lib/validate-name.ts` (new), `frontend/app/patient/settings/page.tsx`, `frontend/app/clinician/settings/page.tsx`, `frontend/app/api/patient/profile/route.ts`. Typecheck + lint clean. NOTE: not yet applied to the registration wizard or admin user create/edit forms — extend there if name validation is wanted on those surfaces too.

2026-06-08 18:55 - [MODIFIED] - **Record-detail document preview hardened (PDF "Failed to load" fix).** A clinician-uploaded "document" whose stored `dataUrl` was placeholder text mislabeled `application/pdf` made the `<iframe>` PDF viewer show "Failed to load PDF document". `DocumentPreview` (in `app/patient/records/[id]/page.tsx`) now: (1) decodes the `data:` URL to raw bytes (`decodeDataUrl`), (2) renders real PDFs from a **Blob/object URL via `<object>`** (more reliable than a large `data:` URL in an iframe, with a download-tile fallback in the `<object>` body for browsers without an inline viewer), (3) validates the `%PDF` magic header (`looksLikePdf`) — a file that *claims* PDF but isn't shows a clean `DownloadTile` with a "not a valid PDF" note instead of the broken viewer. Object URLs are revoked on unmount. Real PDF/image uploads preview inline; bad/placeholder files degrade gracefully. File: `frontend/app/patient/records/[id]/page.tsx`. Typecheck + lint clean.

2026-06-08 18:40 - [ADDED] - **Patient record-detail page is now fully dynamic — real content, real access trail, working Download/preview.** Before: opening a DB record (`/patient/records/[id]`) fetched the whole `/api/patient/records` list and found the record by id (so it never showed the uploaded file for clinician-shared documents — a document record rendered an empty body), the **Access history** panel was hardcoded demo data (fake actors "Aarav Mehta (you)" / "Auditor (regulator)" with invented IPs + relative times), and **Download PDF** produced a generic 3-line stub regardless of the record's actual content. Four changes: (1) **DDL** — new `record_access_log` table applied to BOTH Neon + local Postgres via psql (`id uuid pk default gen_random_uuid(), "recordId" text, "recordKind" text, "actorId" uuid, "actorName" text, "actorRole" text, action text, ip text, "createdAt" timestamptz default now()` + index on `("recordId","createdAt" desc)`). Append-only PHI access ledger keyed by the record's id (text, since records span 3 source tables). (2) **`/api/patient/records/[id]`** (NEW, Patient-gated) — `GET` resolves ONE record across `prescriptions` / `medical_records` / `patient_documents` (all scoped `WHERE id = $id AND patientId = me`), returns the full `RecordDetail` INCLUDING `fileUrl` (base64 data URL) + `mimeType` + `sizeBytes` for documents, derives lifecycle events (record.create / record.finalize / record.upload) from the row's own timestamps, logs a `record.view` event (deduped to 1/actor/min so Strict-Mode double-effects don't spam), then returns `accessLog` = logged events ∪ lifecycle, newest-first. `POST {action}` logs `record.download` / `record.share` (validated against an allow-list). (3) **`app/patient/records/[id]/page.tsx`** — fetches the single-record endpoint instead of the list; new `<DocumentPreview>` renders the uploaded file inline (image `<img>` / PDF `<iframe>` / fallback download tile by MIME); new `<AccessHistory>` renders `record.accessLog` with per-action icon/label/relative-time (replaces the hardcoded `<ol>`); added an empty-state for records with no renderable body. (4) **`app/patient/records/[id]/record-actions.tsx`** — `RecordActions` now takes the whole `record`; `buildRecordPdf()` renders a brand-headed, content-rich PDF from the real fields (clinician/facility/date + prescription/results/findings/impression/note sections); for uploaded documents (`record.fileUrl`) it downloads the **original file** as-is instead of a wrapper PDF; both paths + the Share dialog POST a `record.download` / `record.share` event so the trail reflects the action. `RecordDetail` (in `records-data.ts`) gained `kind?`, `fileUrl?`, `mimeType?`, `sizeBytes?`, and `accessLog?: AccessEvent[]`. **End-to-end**: patient opens a clinician-uploaded document → sees the actual file previewed + a real "Uploaded / Viewed" trail (their own view logged with IP); clicks Download → original file downloads, a `record.download` row appears on next load; opens a prescription/note → content PDF carries the real fields. Demo lab/imaging viewer records (static `RECORDS[]`) still work via the client-side `getRecord()` fallback. Files: `frontend/app/api/patient/records/[id]/route.ts` (new), `frontend/app/patient/records/[id]/page.tsx`, `frontend/app/patient/records/[id]/record-actions.tsx`, `frontend/app/patient/records/records-data.ts`, + `record_access_log` DDL (both DBs). Typecheck + lint clean (baseline `tenants/route.ts` error unchanged).

2026-06-08 17:55 - [ADDED] - **/admin/settings → Channels tab now fully dynamic.** Before: the three channel cards (SendGrid Email · Twilio SMS · In-app push) read/wrote `useAdminStore.channels` in localStorage, and the verified-sender ("ops@citygeneral.health") + sender ID ("CITYGEN · 6 countries") text was hardcoded into the card `desc` literal — different tenants saw the same hardcoded sender details regardless of who they were. Two changes: (1) **`/api/admin/channels`** (NEW, Org-Admin-gated) — GET returns `{channels, defaults}` from `organizations.settings.channels` (same JSONB pattern as policy / schedule); PATCH validates + clamps (`verifiedSender ≤ 254 chars trim`, `senderId ≤ 32 chars trim`, `0 ≤ countries ≤ 250`), merges with current so a partial PATCH doesn't blow away the other channels, then writes back. Server-side: `inApp.enabled` is forced to `true` regardless of payload (alwaysOn platform policy). (2) **`app/admin/settings/page.tsx`** Channels tab rewritten — new `channels / channelsLoading / channelSaving` state alongside the existing tabs. On mount it GETs `/api/admin/channels` with a Loader; the `ChannelCard` helper was rewritten to support editable `fields` (label + value + onSave) and per-card `saving` indicators. Each toggle and each inline-edit input fires a single-key PATCH (so flipping email doesn't grey out SMS, and saving sender ID doesn't block the toggle); on success, the response replaces local state so server clamps (e.g. inApp forced true) are reflected immediately. Inline fields commit on blur or Enter; Escape reverts. Removed the `updateChannels` destructure from `useAdminStore` since channels no longer go through localStorage. The fake hardcoded sender strings are gone — what shows in the card is what the tenant has saved. **End-to-end**: Org Admin opens Channels → enters their actual SendGrid sender (e.g. ops@cityhospital.health) → Tab/Blur → API saves → reload → value persists from DB. SMS senderId + countries work the same way. In-app card has no toggle (alwaysOn). Files: `app/api/admin/channels/route.ts` (new), `app/admin/settings/page.tsx` (channels state + tab JSX + ChannelCard + ChannelFieldRow). Typecheck baseline unchanged.

2026-06-08 17:10 - [MODIFIED] - **/admin/appointments — implemented section 3.5 (Schedule & Slot Configuration) and trimmed out-of-scope UI.** Before: the page showed a clinic-wide schedule + Book/Reassign flow but the in-page "Reminders" button only fake-toasted (no persistence), working hours / slot defaults / reschedule + cancellation windows were not configurable anywhere, conflict detection on booking was missing, and a Telehealth stat tile sat in the header even though 3.5 doesn't mention telehealth tracking. Five changes: (1) **`lib/schedule-config.ts`** (NEW) — shared `ScheduleShape` (workingHours per day × {enabled, open, close}, defaultSlotMinutes, reschedulePolicy, cancellationPolicy, reminders), `SCHEDULE_DEFAULTS` (Mon–Fri 09:00–17:00 / Sat-Sun off, 30-min slots, 24h reschedule, 12h cancel, T-24h + T-1h on), `readSchedule()` clamp/coerce reader off `organizations.settings` JSON, `checkWorkingHours()` returning `{ok:true}` or `{ok:false, reason, dayKey, window}` for a `(start, duration)` pair. Shared between the schedule-config endpoint and the appointment POST so the editor + the conflict-check can never drift. (2) **`/api/admin/schedule-config`** (NEW, Org-Admin-gated) — `GET` returns `{schedule, defaults}` from `organizations.settings.schedule` (extends the same JSONB column already used for `policy`); `PATCH` validates + clamps (`5≤slot≤240`, `0≤hours≤168`, time fields must be `HH:MM`), merges with current so a partial PATCH doesn't blow away other keys, then writes back through the same raw `UPDATE organizations SET settings = ...::jsonb` pattern. (3) **`/api/admin/appointments` POST** — added a conflict + working-hours guard. Loads `org.settings`, runs `checkWorkingHours()` and a raw SQL overlap query (`(start, start+duration)` intersect any existing non-completed appointment for the same clinician), returns `409 {conflict, error}` with the conflict details, OR accepts on the second attempt when the client passes `override: true`. Existing GET unchanged. (4) **`app/admin/appointments/page.tsx`** — loads `/api/admin/schedule-config` on mount, swapped the old static `<ConfigureRemindersDialog />` for a new `<ScheduleSettingsDialog />` with four sections in one scrollable dialog (Working hours per day × 7 / Default slot duration / Reschedule + Cancellation hours / Reminders T-24h + T-1h + No-show follow-up — all driven by the same PATCH). BookAppointmentDialog now starts at `defaultSlotMinutes` from config and handles 409: surfaces the conflict (with the existing slot's time + patient name) inside the dialog and shows an **Override and book anyway** destructive-variant button that re-POSTs with `override:true`. Any edit clears the conflict so the user re-validates. (5) **Removed Telehealth stat tile** — not in 3.5 scope; stats grid is now `sm:grid-cols-3` covering Today total / Confirmed / No-shows. **Stat counts API unchanged** (still returns telehealth in the response, just no longer rendered). The 3.5 requirements now map: working hours = Schedule settings · Working hours section; default slot template = Schedule settings · Default slot template; oversee schedule = main page table; override conflicts = 409 + Override button; reassign = existing AppointmentRow detail dialog (clinician dropdown); reschedule/cancellation windows = Schedule settings · Reschedule + cancellation; reminder timing = Schedule settings · Reminders. Files: `frontend/lib/schedule-config.ts` (new), `frontend/app/api/admin/schedule-config/route.ts` (new), `frontend/app/api/admin/appointments/route.ts` (POST), `frontend/app/admin/appointments/page.tsx`. Typecheck unchanged from baseline.

2026-06-08 15:30 - [ADDED] - **Per-user notification preferences are now DB-backed** (was localStorage). Patient → Settings → Notifications (and the same component on clinician / admin / compliance / auditor / super settings) writes the category × channel matrix (In-app / Email / SMS) to a new `notification_preferences` table instead of `hs_notif_prefs_<role>_v1` in localStorage. Three changes: (1) **DDL** — new `notification_preferences` table applied to BOTH Neon + local Postgres via psql one-shot (SQL file self-deleted): `id uuid pk default gen_random_uuid(), "userId" uuid not null references users(id) on delete cascade, role text not null, prefs jsonb not null default '{}', "updatedAt" timestamp(3) not null default now(), created_at timestamp(3) not null default now(), unique("userId")` plus an index on userId. One row per user (upsert on `userId`); `prefs` is the same `{ [categoryKey]: { inApp, email, sms } }` shape the component already used. (2) **`/api/me/notification-preferences`** (NEW, session-required) — `GET` returns `{ok, prefs, role}` for the signed-in user (empty `{}` when no row exists yet, so the component falls back to its per-role defaults). `PUT` upserts the row with a server-side clamp that forces `inApp + email = true` for critical category keys (`security`, `incidents`) regardless of what the client sends, so a tampered request can't silently disable security alerts. Demo (non-UUID) users bypass the DB and get an in-memory success response so the editor still works on demo logins. (3) **`components/shared/notification-preferences.tsx`** rewritten to use the API: fetches on mount with a Loader spinner, `dirty` flag compares against server-saved snapshot, Save sends `PUT` and reflects server-clamped values back into the form, Reset to defaults still requires an explicit Save. Removed the `loadPrefs/storageKey/VERSION` localStorage helpers. The 6 settings pages that import `<NotificationPreferences role="…" />` (patient/clinician/admin/compliance/auditor/super) need no changes — the prop contract is unchanged. Files: `frontend/app/api/me/notification-preferences/route.ts` (new), `frontend/components/shared/notification-preferences.tsx` (rewired), plus the one-shot SQL migration that was deleted after running. Typecheck baseline unchanged.

2026-06-08 14:55 - [MODIFIED] - **Header "idle in" pill now shows the live countdown driven by tenant policy** (follow-up to 14:20). User reported: with Patient session timeout = 1 min, the patient dashboard pill still showed hardcoded `Session secured · idle in 27:14` and didn't auto-logout after 1 min. Root cause: the pill in `components/patient/header.tsx` was static literal text, the pill in `components/shared/role-header.tsx` rendered a derived expression off a static `sessionMins` prop defaulting to 14 ("MFA · idle in 14:14"), and `<IdleTimeout />` kept its own private `lastActivity` ref — three separate timers with no shared state. Three files added/changed: (1) `lib/idle-timer.ts` (NEW) — module-level singleton that holds the activity timestamp + tenant idle window, binds the mouse/keyboard/scroll/touch listeners ONCE per page, fires a 500 ms tick to all subscribers, exposes `subscribe / resetIdle / setIdleMs / readIdleSnapshot / loadIdlePolicyOnce`. (2) `components/shared/idle-pill.tsx` (NEW) — small client component that subscribes to the singleton and renders `${label} · idle in MM:SS`, used for both the patient pill (label "Session secured") and the clinical pill (label "MFA"). (3) `components/shared/idle-timeout.tsx` rewritten — no longer keeps its own ref/interval/listeners; subscribes to the singleton, triggers signout when `snap.expired`, opens the warning dialog when `remaining ≤ 30s`, and "Stay signed in" calls `resetIdle()` so the pill snaps back to the full minute. Net effect: org admin sets Patient = 1 → patient logs in → header pill counts `1:00 → 0:59 → … → 0:01`, warning dialog opens at 0:30 of remaining, and `POST /api/auth/logout` fires at 0:00. The 5 dashboards (admin/auditor/clinician/compliance/super) still pass the now-deprecated `sessionMins` prop to `RoleHeader` — kept as a harmless ignored prop for backwards-compat, marked deprecated in the type comment. Files: `lib/idle-timer.ts` (new), `components/shared/idle-pill.tsx` (new), `components/shared/idle-timeout.tsx`, `components/patient/header.tsx`, `components/shared/role-header.tsx`. Typecheck baseline unchanged.

2026-06-08 14:20 - [ADDED] - **Org Admin → Settings → Policy tab now fully dynamic + drives real idle-timeout behavior.** Before: the four inputs (Min password length, Password history, Patient session timeout, Clinical session timeout) read/wrote `useAdminStore.retention` in localStorage and the Save button only fired a toast — values were inert and per-browser. The IdleTimeout component (mounted in admin/auditor/clinician/compliance/patient layouts) had a hardcoded `IDLE_MS = 15 * 60 * 1000`, so the configured patient/clinical session minutes had zero effect. Three changes: (1) **`/api/admin/policy`** (NEW, Org-Admin-gated) — GET returns `{minPasswordLength, passwordHistory, patientSessionMinutes, clinicalSessionMinutes}` from `organizations.settings.policy` JSONB with defaults `{12, 5, 30, 15}` for any missing field; PATCH validates + clamps (`8≤pwd≤128`, `0≤history≤50`, `1≤minutes≤720`), preserves any non-policy keys on `settings`, then writes `settings = jsonb_set(...)` and returns the clamped values back. No schema migration — extends the existing free-form `organizations.settings` JSON column with one new `policy` sub-key. (2) **`/api/me/idle-policy`** (NEW, signed-in only) — returns `{ok, minutes, warnSeconds, scope}` derived from the caller's role: `Patient → patientSessionMinutes` else `clinicalSessionMinutes`. Demo (non-UUID) users + missing tenant fall back to defaults; DB errors are swallowed (never blocks the layout from rendering). (3) **`components/shared/idle-timeout.tsx`** updated — fetches `/api/me/idle-policy` on mount and stores the result in an `idleMs` ref that the activity-watcher interval reads each tick (so the new value takes effect without remounting); falls back to 15 min until the response lands. The warning dialog still appears 30s before expiry. (4) **`app/admin/settings/page.tsx` Policy tab** rewritten — new `policy / policyServer / policyLoading / policySaving` state alongside the existing `retention` state (Retention tab is unchanged — that tab owns medical/document/message/notification retention windows; Policy now owns only password + session timeouts). On mount it GETs `/api/admin/policy`; Save PATCHes and reflects the server-clamped response back into the form; Discard reverts to `policyServer`; toast description reads "Patient Xm · clinical Ym · audit-logged". For backwards-compat the saved values are mirrored onto `useAdminStore.retention` so any older surface still reading from localStorage stays consistent. **End-to-end behavior**: Org Admin opens `/admin/settings → Policy`, sets Patient = 15, Clinical = 15, Save → both numbers are persisted in `organizations.settings.policy` (dual-write also lands them on local Postgres via the `$use` middleware). Any user signing in afterward (patient or clinical) — their layout's IdleTimeout fetches `/api/me/idle-policy`, gets 15, and after 15 min of no mouse/keyboard/scroll/touch activity the 30s warning dialog appears and then `POST /api/auth/logout` fires and they're bounced to the role-appropriate login page. Files: `app/api/admin/policy/route.ts` (new), `app/api/me/idle-policy/route.ts` (new), `components/shared/idle-timeout.tsx` (idle-window now dynamic), `app/admin/settings/page.tsx` (Policy tab rewired). tsc unchanged from baseline (only the one pre-existing `super/tenants/route.ts` error remains).

2026-06-05 10:30 - [REPLACED] - `/admin/templates` (Notification templates) is now fully DB-driven. Before: the page was a giant hardcoded `TEMPLATES: Template[]` array (180+ lines of literals — 11 default messages with their bodies, channels, version numbers stamped on each); the "Save as v(n+1)" button only wrote a copy into `lib/admin-store.tsx` localStorage; the "History" panel was a stub showing fake "v3 archived 22 days ago" rows; the "Restore" button just toasted; the channel tabs counted templates from the same array, so "Email · 10 / SMS · 1 / In-app · 5" were derived from the literals. Three new things landed: (1) **DB schema** — new `notification_templates` table (raw SQL applied to BOTH Neon and local Postgres via the dual-write migration pattern): id, `organizationId` FK, `slug` text, `name` text, `channels` text[], `subject` text nullable, `body` text, `version` int, `isActive` boolean, `smsLimit` int nullable, `createdById` + `createdByEmail`, timestamps. Indexes on `(organizationId, isActive, slug)`, `(organizationId, slug, version DESC)`, and a unique `(organizationId, slug, version)` so two operators can't accidentally produce duplicate versions. Versioning rule: every tenant has at most one row with `isActive = true` per slug — that's the current version. Older versions stay in the table with `isActive = false` for audit. (2) **Seeded 33 default rows** (11 messages × 3 tenants — `org_city`, `org_city_hospital`, `org_hospital`) using the same wording the old hardcoded array had, so existing pages don't feel different on first load. Seed is idempotent: it skips any tenant that already has templates. (3) **3 new API endpoints**, all Org-Admin-gated: `GET /api/admin/templates` returns every template for the caller's tenant grouped by slug — top-level entry is the currently-active version, `history` field nests archived versions with `id / version / createdAt / createdByEmail`; `POST /api/admin/templates` accepts `{name, channels[], subject?, body, smsLimit?}`, auto-slugifies, dedupes the slug if it collides (`my-template`, `my-template-2`), inserts v1 with `isActive=true`; `PATCH /api/admin/templates/[id]` saves a new version — flips the current row's `isActive` to false, computes `version = MAX(version) + 1` (so it advances correctly even after a restore), inserts the new row with the same slug/name/channels/smsLimit and the new subject/body; `POST /api/admin/templates/[id]/restore` brings an archived version back to active by copying its content into a brand-new version at the top of the stack (in-between versions stay on file for audit). All four guards verify tenant ownership via `organizations.slug == claims.org` so a malicious operator can't edit another tenant's templates by id. **Page rewritten** as a `"use client"` component (no more `useAdminStore` dependency): loads templates from the API on mount, shows a Loader spinner while fetching, error card on failure; tab counts come from the live data; the editor's "Save as v(n+1)" button POSTs the PATCH and re-loads, surfacing a toast with the actual new version number returned by the server; "History" opens a dialog listing every archived version with a real Restore button that hits the new endpoint and re-loads. A "New template" dialog gives the admin a name + channel picker + subject + body and POSTs to create v1. Per-channel character limits for SMS still enforced client-side from the row's own `smsLimit` value. Dual-write fan-out (from the `lib/prisma.ts` middleware landed earlier today) means every save / restore / new lands on both Neon AND local Postgres automatically — verified post-seed: 11 active templates per tenant on each DB. Files: `app/api/admin/templates/route.ts` (new), `app/api/admin/templates/[id]/route.ts` (new), `app/api/admin/templates/[id]/restore/route.ts` (new), `app/admin/templates/page.tsx` (rewritten). Anonymous GET / POST → 401; non-Org-Admin → 403. The previous `useAdminStore.addTemplate(...)` call is no longer made — that store stays for the unrelated audit slice but doesn't hold the templates data anymore.

2026-06-05 09:30 - [ADDED] - Dual-write Prisma client: every typed-client mutation + every safe-template raw mutation now fans out from Neon (primary) to local Postgres (shadow) automatically. Before: `frontend/.env.local` had a single `DATABASE_URL` pointing at Neon, and the local Postgres at `localhost:5432/helath_secure` only received writes from manual seed scripts — every user / incident / break-glass session created via the running app landed on Neon only. The two DBs drifted (e.g. Dr. ravi test invite via `/admin/users/invite` UI added a row on Neon but not local, leaving Neon at 36 users vs local at 35). User asked for both DBs to receive every entry simultaneously. Three changes: (1) **Env**: both `frontend/.env.local` and `backend/.env` now expose `NEON_DATABASE_URL` + `POSTGRES_DATABASE_URL` + `DATABASE_URL` (kept pointed at Neon so Prisma CLI `generate` / `migrate` / `studio` still work). (2) **`frontend/lib/prisma.ts` rewritten** as a dual-write singleton: builds a primary `PrismaClient` against `NEON_DATABASE_URL` and a shadow `PrismaClient` against `POSTGRES_DATABASE_URL` (only when the two URLs differ and `POSTGRES_DATABASE_URL` is set). Installs a `$use` middleware on the primary that, for every write action (`create / createMany / createManyAndReturn / update / updateMany / upsert / delete / deleteMany`), replays the same call on the shadow via `shadow[modelKey][action](args)`. For raw queries (`$executeRaw` / `$queryRaw` template-literal forms) the middleware also fans out, but only when the SQL leading section matches `/\b(INSERT|UPDATE|DELETE|UPSERT|MERGE|TRUNCATE|ALTER|DROP|CREATE)\b/i` — pure SELECTs stay on the primary so the shadow doesn't get spammed with read traffic. Failures on the shadow log `[prisma:shadow] <model>.<action> failed — primary write OK, shadow drift: <msg>` and the primary's result is still returned to the caller (eventually-consistent, never blocks). `prismaShadow` is also exported for tests / one-off scripts. (3) **Existing divergence reconciled** via a one-shot sync (now deleted) that did `local.user.create({data: u})` for every email present on Neon but missing on local — Dr. ravi test landed on local; both DBs now show 36 users. Verified end-to-end through the real middleware: `[shadow OK] User.create` fires automatically, the row appears on both DBs by email, `[shadow OK] User.delete` cleans both. **Known caveats** (also documented in the `lib/prisma.ts` header): (a) UUIDs generated via `gen_random_uuid()` / `@default(uuid())` DIVERGE between the two DBs because each Postgres rolls its own — the same logical record exists on both but with different `id` columns. To force matching IDs, app code would need to generate UUIDs in JS and pass them explicitly to `create()` (not done in this turn). (b) Auto-increment counters like `incidents.number` diverge similarly. (c) Prisma's `$use` does NOT intercept `*Unsafe` variants (`$executeRawUnsafe` / `$queryRawUnsafe`) — those bypass middleware. All current API routes use the safe template-literal forms so this is fine; ad-hoc scripts that use Unsafe will only hit the DB the script's PrismaClient is configured for. (d) No cross-DB transaction — if the primary write succeeds and shadow fails, the two drift until you reconcile. To disable dual-write: either clear `POSTGRES_DATABASE_URL` or set it equal to `NEON_DATABASE_URL` (the helper degrades to single-client). Files: `frontend/.env.local`, `backend/.env`, `frontend/lib/prisma.ts`. Sync + smoke-test scripts deleted after running (one-shot).

2026-06-08 17:45 - [ADDED] - **Clinician Pending tasks now surface patient-uploaded documents, with click-to-preview.** New `app/api/clinician/document-tasks/route.ts` (`GET`, clinician-guarded) returns documents that the clinician's assigned patients uploaded themselves (`patient_documents` JOIN `patient_assignments` on active assignment, `uploadedById = patientId`), incl. patientName + dataUrl/mimeType for preview. `app/clinician/tasks/page.tsx` fetches it and renders a "Patient document uploads" group: each row shows patient + file + category, opens the shared `DocumentPreviewDialog` on click/Preview (images → ImagingViewer, PDF → iframe, DICOM/other → fallback), and a "Done" button that dismisses it (persisted per-clinician in `localStorage` `hs_clinician_doc_tasks_done`). The "Open tasks" stat + empty-state now account for these. Verified: anon → 401; for sweta the query returns Rahi's self-uploaded "dsad" (Imaging). tsc/lint clean. Files: `app/api/clinician/document-tasks/route.ts` (new), `app/clinician/tasks/page.tsx`.

2026-06-08 17:25 - [ADDED] - **Patient Medical Records: added the missing categories (Insurance / ID Proof / Other)** so every clinician-uploaded document category surfaces in the patient's clinical history. `records-data.ts` `Category` type extended; `app/patient/records/page.tsx` `META` + `CATEGORIES` sidebar + `CATEGORY_KEYS` gained Insurance (FileBadge), ID Proof (IdCard), Other (FileText) with accents; `app/patient/records/[id]/page.tsx` `CATEGORY_META` (exhaustive `Record<Category>`) got the 3 keys too (avoids a type error / runtime crash). `app/api/patient/records/route.ts` broadened the shared-documents query from `IN ('Lab Report','Imaging','Discharge')` to also include `Insurance`, `ID Proof`, `Prescription`, `Other`. Result: clinician-uploaded docs of any category now appear under the right category tab in Medical Records (verified for Rahi: Insurance:ret/asd, Prescription:prior, Lab Report:Screenshot…). Patient self-uploads still excluded from Medical Records (care-team-authored premise) but remain on the Documents page; the clinician chart Documents tab still shows only that clinician's own uploads (15:?? change). tsc/lint clean. Files: `app/patient/records/records-data.ts`, `app/patient/records/page.tsx`, `app/patient/records/[id]/page.tsx`, `app/api/patient/records/route.ts`.

2026-06-08 17:10 - [MODIFIED] - **Clinician chart Documents tab shows only the logged-in clinician's own uploads.** `GET /api/clinician/patients/[id]/documents` now filters `uploadedById = <clinician>` (per request) — patient self-uploads + other clinicians' uploads are hidden there. The patient's own Documents page still shows all their docs; the Medical Records merge is unaffected. Verified (sweta → Rahi: only "ret"). File: `app/api/clinician/patients/[id]/documents/route.ts`.

2026-06-08 16:55 - [FIXED+ADDED] - **Clinician document upload reaches the patient.** (1) FIX: `/api/clinician/patients/[id]/documents` 404'd ("Patient not found in your tenant") for self-registered patients (`organizationId = null`) because the guard required `patient.org == clinician.org`. Now the guard authorizes via an **active `patient_assignment`** (clinician on the patient's care team) OR same-tenant; uploads are stamped with the clinician's org. GET lists by `patientId` (assignment-gated), dropping the org filter. (2) ADD: clinician-uploaded **clinical** documents now also surface on the patient **Medical Records** page — `/api/patient/records` GET merges `patient_documents` where `sharedWithPatient = TRUE AND uploadedById <> patient AND category IN ('Lab Report','Imaging','Discharge')` (mapped to `RecordDetail`, `status: Finalized`, clinician = uploader). Admin-category docs (Insurance / ID Proof / Other) intentionally stay on the patient **Documents** page only (they're not clinical records; the Medical Records `Category` type + icon map only handle the 5 clinical categories, so including them would break rendering). Patient self-uploads are excluded from Medical Records (page premise: "records authored by your care team") but still appear on the Documents page. Verified end-to-end against Neon: Rahi's clinician-uploaded "ret" (Insurance) + patient-uploaded "dsad" (Imaging) both show on /patient/documents; a clinician-uploaded Lab Report/Imaging/Discharge would also show in Medical Records. tsc/lint clean. Files: `app/api/clinician/patients/[id]/documents/route.ts`, `app/api/patient/records/route.ts`.

2026-06-08 16:35 - [FIXED] - **Patient Medical Records page now shows ONLY the signed-in patient's real DB records** (was always prepending the 2 demo lab/imaging seed records — CBC by "Dr. Priya Shah" + Chest X-ray by "Dr. Rohan Iyer" — so every patient saw them). `app/patient/records/page.tsx` no longer merges `RECORDS` from `records-data.ts`; it initializes `records` to `[]` and sets it straight from `GET /api/patient/records` (finalized prescriptions + clinical notes + discharge for `patientId = me`). Dropped the now-unused `RECORDS` import. Verified: Rahi shah has 0 finalized records → page now shows the empty state instead of the 2 demo rows. tsc/lint clean. Notes: `records-data.ts` still holds the demo records + the Lab/Imaging viewer components remain (the static detail route `/patient/records/[id]` still builds them, just unlinked from the list) — when real `lab_results`/`imaging_studies` tables land, surface them via the API. The static record detail page still can't open DB-backed rx/notes ids (pre-existing). File: `app/patient/records/page.tsx`.

2026-06-08 16:20 - [ADDED] - **Clinician document upload is now a dedicated DB-backed page (Title/Category/Tags + drop zone), and the chart Documents tab reads real DB docs.** (1) NEW API `app/api/clinician/patients/[id]/documents/route.ts` — `GET` (lists `patient_documents` for the patient scoped to the clinician's org, with uploader name) and `POST` (clinician uploads for the patient → INSERT into `patient_documents` with `uploadedById=clinician`, `sharedWithPatient=TRUE`, so it shows on the patient's side too). Both `clinician`-guarded; anon → 401 (verified). (2) NEW page `app/clinician/patients/[id]/documents/upload/page.tsx` mirroring the patient upload page — drop zone, **Title / Category / Tags** fields, upload-policy sidebar, per-file XHR progress bars (queued → uploading % → uploaded/failed). POSTs to the new endpoint, then redirects to `/clinician/patients/[id]?tab=documents`. (3) Chart page (`app/clinician/patients/[id]/page.tsx`): the Documents tab's `DocumentsList` now reads from the new GET (`apiDocs` state) instead of the clinician localStorage store; its "Upload document" button is now a `<Link>` to the upload page (dropped the bare file-picker + `onUpload`/`uploadDocument`/`myDocs`). The chart `<Tabs>` reads `?tab=` on mount so the post-upload redirect lands on the Documents tab showing the freshly-uploaded DB doc. tsc/lint clean. Follow-up (not done): the standalone full documents view (`app/clinician/patients/[id]/documents/page.tsx`, reached via "Open full documents view") still reads the clinician store + has its own bare-picker upload — migrate it to the same API later. Files: `app/api/clinician/patients/[id]/documents/route.ts` (new), `app/clinician/patients/[id]/documents/upload/page.tsx` (new), `app/clinician/patients/[id]/page.tsx`.

2026-06-08 15:55 - [FIXED] - **Patient Documents page now shows ONLY the signed-in patient's real DB documents** (was rendering the localStorage demo seed — e.g. Rahi shah saw 13 seeded docs while the DB had 0). `app/patient/documents/page.tsx` now fetches `GET /api/patient/documents` (rows where `patientId = me`, soft-delete aware) instead of `state.documents`; delete calls `DELETE /api/patient/documents?id=` then refetches. Added an `ApiDoc`→`PatientDocument` mapper (`toDoc`), null-safe `docs` state with a loading gate, dropped the `usePatientStore`/`deleteDocument` dependency. Preview/download/upload-link unchanged (upload page POSTs to the same table, so new uploads appear on refetch). Mirrors the earlier Consents fix. tsc/lint clean. File: `app/patient/documents/page.tsx`. (Note: the clinician-side documents tab still reads the clinician store; surfacing a patient's real DB docs to the clinician would need a shared-docs API — separate task. Also still pending: the requested clinician "Upload document" → dedicated upload page with Title/Category/Tags fields.)

2026-06-08 15:35 - [ADDED] - **Clinician Consents tab: granted scope cards are now clickable → jump to the data view.** In `app/clinician/patients/[id]/page.tsx`, the chart `<Tabs>` is now controlled (`value={tab} onValueChange={setTab}`, new `tab` state). `PatientConsents` got an `onOpen(scope)` prop; each **Active** (granted) scope card is now a clickable button (cursor-pointer, hover lift, Enter/Space support) that switches the tab to the matching view — `prescriptions` → Prescriptions, everything else (lab/imaging/insurance/id_proof/other) → Documents. Card copy reads "Patient granted access · click to open <documents|prescriptions>". The "Simulate revoke" button `stopPropagation`s so it doesn't trigger the open; "No access" cards keep their Request-access button and aren't clickable. tsc/lint clean. File: `app/clinician/patients/[id]/page.tsx`.

2026-06-08 15:20 - [FIXED] - **Patient Consents page now shows ONLY the signed-in patient's real DB consents** (was showing 9 from the localStorage demo seed). `app/patient/consents/page.tsx` previously rendered Active/History from `state.consents` (the patient store's seeded demo data), so a real migrated patient (e.g. Rahi shah, who has exactly 1 approved consent) saw unrelated seeded grants. Rewrote it to source everything from `GET /api/patient/consent-requests` (rows where `patientId = me`): `pending` → status pending (banner), `active` → approved, `history` → revoked/declined (mapped to the card shape via new `toConsent()`). Revoke now calls `DELETE /api/patient/consents?id=` then refetches (dropped the local-store `revokeConsent`/`addConsent` mirrors). Added `expiresAt` to the consent-requests GET (`app/api/patient/consent-requests/route.ts`) so cards show the real expiry. Loading gates on the fetch, not store hydration. tsc/lint clean. (The clinician-side Consents tab is separate and unchanged.) Files: `app/patient/consents/page.tsx`, `app/api/patient/consent-requests/route.ts`.

2026-06-08 15:05 - [MODIFIED] - **Clinician chart: Records/Prescriptions/Documents tabs now show content directly (no consent gate); consent stays on the Consents tab.** Per request, removed the `hasEffectiveConsent(...) ? content : <ConsentDeniedCard>` branch from all three tabs in `app/clinician/patients/[id]/page.tsx` — they now always render `RecordsList` (→ "Create first note"), `Prescriptions` (→ "New prescription"), and `DocumentsList` (→ upload) directly. The Request-access / consent flow remains only on the **Consents** tab (unchanged, still works). Dropped the now-unused `ConsentDeniedCard` import; `pendingRequest` still drives the top PendingRequestBanner and the Consents tab. Supersedes the per-tab Request-access button work below. tsc/lint clean.

2026-06-08 14:50 - [FIXED] - **Clinician "Request access" button now shows per-tab independently.** On the clinician patient detail page (`app/clinician/patients/[id]/page.tsx`), the Records / Prescriptions / Documents consent-denied cards gated their "Request access" button on a single patient-wide `pendingRequest(state, patientId)` — so once any one request was pending, the button disappeared from all three tabs. `pendingRequest()` (`lib/clinician-store.tsx`) now takes an optional `scope`, and each tab gates on its own scope (`notes` / `prescriptions` / `imaging`). Result: every tab keeps its own Request-access button; only the tab whose scope is already pending hides it. Top-level `pending` still drives the global PendingRequestBanner. tsc/lint clean.

2026-06-08 14:35 - [MODIFIED] - **Reminder popup now covers T-24h too.** `components/shared/appointment-reminder.tsx` fires a **T-24h** reminder ("Appointment in ~N hours") when an appointment is within the next 24h (but >1h), and the **T-1h** reminder ("Appointment in N min") when within 60 min. The two kinds have separate dismissal keys (`<id>:t_24h` / `<id>:t_1h`) so dismissing the day-before nudge doesn't suppress the hour-before one. Same mounts (patient + clinician layouts). tsc/lint clean.

2026-06-08 14:20 - [ADDED] - **In-app T-1h appointment reminder popup (both patient + clinician).** New `components/shared/appointment-reminder.tsx` (`AppointmentReminder`, role-aware) — polls the signed-in user's upcoming appointments (patient → `/api/patient/appointments`; clinician → `/api/clinician/appointments?from=today&to=+2d`) every 60s and, when an active appointment (`requested`/`reschedule_requested`/`confirmed`/`arrived`) starts within the next **60 minutes**, opens a dialog ("Appointment in N min" + who/time/telehealth + View/Dismiss). Dismissals persist per appointment in `localStorage` (`hs_appt_reminders_dismissed`) so it doesn't re-pop on poll/navigation. Mounted in BOTH `app/patient/layout.tsx` and `app/clinician/layout.tsx` next to `<IdleTimeout />`, so each side gets reminded independently. This is the live UX layer on top of the existing `appointment_reminders` rows + `/api/cron/appointment-reminders` email dispatch. Verified timing against Neon (e.g. a 4:30 PM IST / 11:00 UTC appt fires the popup at 3:30 PM IST). Also fixed a pre-existing unescaped-apostrophe lint error in `app/patient/dashboard/page.tsx:466`. tsc clean (only pre-existing `tenants`); `next lint` clean. Files: `components/shared/appointment-reminder.tsx` (new), `app/patient/layout.tsx`, `app/clinician/layout.tsx`, `app/patient/dashboard/page.tsx`.

2026-06-04 23:05 - [MODIFIED] - **Full local→Neon data migration (real data, not a seed).** Per request, the existing local `helath_secure` Postgres was cloned into Neon rather than seeded with demo rows. Method: `pg_dump` (C:\Program Files\PostgreSQL\17\bin) a full plain dump of the local DB (`--no-owner --no-privileges --no-acl`), then on Neon `DROP SCHEMA public CASCADE; CREATE SCHEMA public;` and `psql -f` the dump (exit 0, 0 errors). pg_dump emits FK constraints after the COPY data, so load order is clean. **Result verified: 34 tables / 494 rows on Neon, exact match to local** (users:35, appointments:18, sessions:149, messages:22, mfa_challenges:53, departments:33, …) — including the 6 raw-SQL tables that aren't in `schema.prisma` (`break_glass_sessions`, `patient_data_requests`, `retention_overrides`, `retention_purge_runs`, `retention_windows`) which the earlier `prisma db push` had NOT created. `AppointmentStatus` enum carried over with `rejected`. The temporary `seed-neon.cjs` script and the demo `@neoncare.health` rows were dropped by the schema reset (0 leftover) — Neon contains only the migrated local data. The 14 MB dump (contained PHI) was written to %TEMP% and deleted afterward. Re-migrate by repeating pg_dump local → drop/recreate public on Neon → psql restore. Login passwords are whatever the local accounts already had (bcrypt hashes migrated as-is).

2026-06-04 22:45 - [MODIFIED] - **Frontend DB switched to Neon serverless Postgres.** `frontend/.env.local` `DATABASE_URL` now points at the Neon pooled endpoint (`ep-cold-rice-apd9bant-pooler.c-7.us-east-1.aws.neon.tech/neondb`, `sslmode=require&channel_binding=require`) instead of the old local `postgresql://postgres@localhost:5432/helath_secure`. `.env.local` is git-ignored so the credential is not committed. Full schema applied to Neon with `npx prisma db push --skip-generate` (27 tables created; `AppointmentStatus` enum includes `rejected`). The Neon DB starts **empty** — demo logins (`lib/demo-users.ts`, non-DB sessions) still work, but DB-backed data (tenants, users, patients, appointments) is blank until provisioned via the Super Admin → Provision Tenant flow (there is no seed script). Dev server restarted to pick up the new env; verified live: `/api/super/tenants` → 403 anon (DB reachable, route alive), `/api/cron/appointment-reminders` → `{dispatched:0}` (empty DB). Note: `backend/.env` (the separate NestJS service) still points at the old local DB — not changed. To switch back, restore the local `DATABASE_URL` line kept as a comment in `.env.local`.

2026-06-04 22:20 - [ADDED] - **Appointment flow completed end-to-end (Confirm / Reject / lifecycle persistence / reminder dispatch)**. The backend state machine (`lib/appointment-lifecycle.ts`) already existed but the clinician UI bypassed it and two actions were missing. Changes: **(A) New `rejected` status** — added to the Prisma `AppointmentStatus` enum (`prisma/schema.prisma`) and applied to the live DB via `ALTER TYPE "AppointmentStatus" ADD VALUE IF NOT EXISTS 'rejected'` (Prisma client isn't regenerated on this Windows box — the lifecycle uses raw SQL casts, so the enum value + TS unions are sufficient). Lifecycle `TRANSITIONS` now allow `requested → rejected` and `reschedule_requested → rejected` (terminal); `statusLabel` + both store unions (`lib/clinician-store.tsx`, `lib/patient-store.tsx`) gained `rejected`. **(B) Clinician API** (`app/api/clinician/appointments/[id]/route.ts`): added a `GET` (single owned appointment) so the detail page can be DB-backed; `PATCH` now accepts `status:"rejected"` (cancels reminders on reject, like cancel/no_show). **(C) Reminder dispatch** — NEW `app/api/cron/appointment-reminders/route.ts` (GET/POST, guarded by `x-cron-secret` matching env `CRON_SECRET` when set; unguarded in dev): finds due `appointment_reminders` (`sentAt IS NULL AND scheduledAt<=now()`) for confirmed/arrived appts, best-effort emails the patient via `lib/mail.ts`, stamps `sentAt` (idempotent). Schedule it from an external cron hitting the endpoint. **(D) Patient notifications** (`app/api/notifications/route.ts` patientFeed): upcoming-appointment items are now status-aware (Requested · awaiting confirmation / "Dr. X proposed a new time" / Confirmed), plus new queries surface **declined** appointments (status `rejected`, last 14d) and **dispatched reminders** (sentAt last 24h) — so "patient gets notified" on confirm/reject/reminder is real (the live `useNotifications` hook already polls). **(E) Clinician detail page** (`app/clinician/appointments/[id]/page.tsx`): now **API-backed** — falls back to the new `GET` when the appointment isn't in the local clinician store (e.g. patient-booked), so it can actually open + drive real appointments. `changeStatus` was rewritten to `PATCH` the API (persisting to DB + reaching the patient) and mirror the store; added **Confirm** + **Reject** (reason dialog) for `requested`, and all lifecycle buttons (Arrived/Start/Complete/No-show) now go through the API with a `busy` guard. Patient list/detail show a "Declined" badge and treat `rejected` as past. Verified: tsc clean (only pre-existing `tenants`); `next lint` clean; live smoke — `POST /api/cron/appointment-reminders` → `{ok,dispatched:3,emailed:3}`, clinician `GET` 401 anonymous. Files: `prisma/schema.prisma`, `lib/appointment-lifecycle.ts`, `lib/clinician-store.tsx`, `lib/patient-store.tsx`, `app/api/clinician/appointments/[id]/route.ts`, `app/api/cron/appointment-reminders/route.ts` (new), `app/api/notifications/route.ts`, `app/clinician/appointments/[id]/page.tsx`, `app/clinician/appointments/page.tsx`, `app/patient/appointments/page.tsx`, `app/patient/appointments/[id]/page.tsx`. Follow-up: set `CRON_SECRET` in prod env + register the cron schedule; optional clinician schedule/list quick-Confirm.

2026-06-04 21:35 - [MODIFIED] - **Org Admin Console — bulk staff invite wired to the real API** (partial progress on the "wire store-only screens to real APIs" gap). `app/admin/users/bulk/page.tsx` no longer writes invites to `lib/admin-store` (localStorage) — `sendAll()` now POSTs each valid CSV row to the existing DB-backed `POST /api/admin/users` sequentially, tracks a per-row `SendResult`, and renders Sent / failed (with the server error) in the preview table's Status column; summary toast reports `N sent · M failed`, redirects to `/admin/users` only on a clean run. Removed the `useAdminStore` dependency and dropped "Org Admin" from the bulk `VALID_ROLES` (the staff-create API intentionally rejects it — those are Super-Admin-provisioned), so validation now matches the API's accepted labels (Clinician / Care Team / Compliance Manager / Auditor). Verified: tsc adds no new errors (only pre-existing `tenants`); `next lint` clean. **Still store-only (need NEW backend, not pure frontend)**: org settings retention/password-policy/channels (tenant PATCH only persists name/type/contact/branding today), notification **templates** editor (no `notification_templates` table), admin **re-consent campaign** create (no admin campaign endpoint), and **bulk patient** invite (`/api/admin/patients` has GET only — no create/invite endpoint). These are tracked as backend follow-ups; FE for this module is otherwise complete. File: `app/admin/users/bulk/page.tsx`.

2026-06-04 21:10 - [ADDED] - **Notification System — per-user preferences editor (category × channel), persisted + rolled out to all roles** — closes the Notification System frontend gap. New `components/shared/notification-preferences.tsx` (`NotificationPreferences`): a category × channel (in-app / email / SMS) matrix with per-role default category presets (`NOTIF_CATEGORIES` map for patient / clinician / admin / compliance / auditor / super), critical categories locked (in-app + email always on), Save/Reset, and a dirty check. **Persistence**: selections save to `localStorage` under `hs_notif_prefs_<role>_v1` and load on mount (merged against current defaults so new categories appear), surviving reloads — previously the patient editor only toasted and forgot. **Rollout**: patient `app/patient/settings/page.tsx` `NotificationsTab` replaced its bespoke unpersisted table with `<NotificationPreferences role="patient" />`; clinician/compliance/super settings replaced their single-dimension on/off toggle lists in the notifications/alerts tabs with the matrix (removed the now-dead `NOTIFICATION_LABELS`/`ALERT_LABELS`/local alert state + unused `Switch` imports); admin settings appends a personal `<NotificationPreferences role="admin" />` under the existing org-level channel cards (org channel availability vs. personal prefs are distinct); auditor settings (no prior notifications section) gained one. Server-side persistence (`notification_preferences` table) is the future backend step. Verified: tsc adds no new errors (only the pre-existing `tenants` error); `next lint` clean. Files: `components/shared/notification-preferences.tsx` (new), `app/patient/settings/page.tsx`, `app/clinician/settings/page.tsx`, `app/admin/settings/page.tsx`, `app/compliance/settings/page.tsx`, `app/super/settings/page.tsx`, `app/auditor/settings/page.tsx`.

2026-06-04 20:40 - [ADDED] - **Secure Messaging — in-conversation full-text search + encryption-indicator polish** — closes the Secure Messaging frontend gap. New `lib/highlight.tsx` (`highlightText(text, query)` → wraps case-insensitive matches in a `<mark>`). Both message views (`app/patient/messages/messages-view.tsx`, `app/clinician/messages/clinician-messages-view.tsx`) gained an **in-thread search**: a Search toggle in the conversation header opens a search bar with a live match counter (`x/N`), prev/next steppers (also Enter / Shift+Enter), and Esc-to-close; matching message bodies are highlighted via `highlightText`, the active match gets a warning-colour ring and auto-scrolls into view (`scrollIntoView`), and the auto-scroll-to-bottom is suppressed while searching so the match scroll wins. Search resets when switching conversations. **Encryption polish**: the two plain `SecurityBadge`s in each thread header were replaced with a single "Encrypted & audited" success chip carrying a `title` tooltip ("encrypted at rest (AES-256), every read audit-logged") — clearer at a glance; the existing "End-to-end encrypted" divider + "Encrypted" send-bar indicator stay. `SecurityBadge` import dropped from both views (now unused). Note: search covers the currently-open conversation (messages load lazily per thread); cross-thread search would need a server search endpoint (future). Verified: tsc adds no new errors (only the pre-existing `tenants` error); `next lint` clean. Files: `lib/highlight.tsx` (new), `app/patient/messages/messages-view.tsx`, `app/clinician/messages/clinician-messages-view.tsx`.

2026-06-04 20:10 - [MODIFIED] - **Consent Management — granular time-bound scope picker** — closes the Consent Management frontend gap ("Granular scope picker (category × clinician × time)"). The grant flow already had category (scopes) + clinician (recipient) selection, but the **time dimension was decorative**: `duration-picker.tsx` was uncontrolled and `submit()` hardcoded `durationHours: null` / `expiresAt: null`, so every consent was open-ended regardless of what the user picked. Now: `DurationPicker` is a **controlled component** (`value: number | null` hours, `onChange`) with granular presets (24h / 7d / 30d / 90d / 6 months / Open-ended) and a live "Expires automatically on <date>" hint. The grant page (`app/patient/consents/grant/page.tsx`) holds `durationHours` state, passes it to the picker, sends the real value to `POST /api/patient/consents` (which already derived `expiresAt = NOW() + durationHours hours` server-side — it was just always receiving null), and computes a matching `expiresAt` for the local-store mirror so the patient's consents page shows the correct expiry immediately. Summary panel now shows the chosen duration + computed expiry date (replacing the hardcoded "Open-ended"), and the success toast states the expiry. Verified: tsc adds no new errors (only the pre-existing `tenants` error); `next lint` clean. Possible future enhancement (not required by the gap): multi-clinician batch grant. Files: `app/patient/consents/grant/duration-picker.tsx`, `app/patient/consents/grant/page.tsx`.

2026-06-04 19:45 - [ADDED] - **Secure Document Management — inline preview + richer upload progress** — closes the Secure Document Management frontend gap ("DICOM / image preview; richer large-file upload progress"). (1) **Inline preview**: new `components/shared/document-preview-dialog.tsx` (`DocumentPreviewDialog`) — images render in the zoom/pan `ImagingViewer` (reused from the Medical Records work), PDFs in an embedded same-origin Blob-URL `<iframe>`, and DICOM (`.dcm` / `application/dicom`) or unsupported types fall back to a metadata card with Download (browsers can't render DICOM natively, so this is honest). Patient documents page (`app/patient/documents/page.tsx`) now opens this dialog on click instead of dumping the file to a new browser tab; the old new-tab behaviour is preserved as a secondary "Open in new tab" menu item, and a "Preview" item was added. (2) **Richer upload progress**: `app/patient/documents/upload/page.tsx` now tracks a per-file `UploadItem { pct, phase, error }` and switched the POST from `fetch` to a new `postDocumentWithProgress` XHR helper so `xhr.upload.onprogress` drives a **real per-file progress bar** with percentage; each file shows its phase (queued → virus scanning → uploading → uploaded·clean / blocked / failed) with a spinner / check / alert icon, and a short 700ms hold lets completed bars render before redirect. Verified: tsc adds no new errors (only the pre-existing `tenants` error); `next lint` clean. Still demo-grade storage (base64 dataUrl, not S3) — that's a backend item. Files: `components/shared/document-preview-dialog.tsx` (new), `app/patient/documents/page.tsx`, `app/patient/documents/upload/page.tsx`.

2026-06-04 19:15 - [ADDED] - **Appointment calendar-sync UI (export / external calendar)** — closes the Appointment Management frontend gap. New `lib/calendar-export.ts`: pure client-side iCalendar generation + external deep-links — `CalendarEvent` type, `toEventStart(isoDate, "10:00 AM")` (combines the store's `date`+`time` into a Date), `buildICS(events)` (RFC 5545 VCALENDAR with VEVENTs, CRLF lines, escaped fields, and -PT24H/-PT1H VALARMs matching the portal's reminder cadence), `downloadICS(filename, events)` (Blob download), `googleCalendarUrl(e)` and `outlookCalendarUrl(e)` (web compose deep-links). New `components/shared/add-to-calendar.tsx`: `AddToCalendar` dropdown (CalendarPlus trigger) with **Download .ics**, **Google Calendar**, **Outlook Calendar** items; accepts a single event or an array (array → one multi-VEVENT .ics; the web links carry the first event since those endpoints take one). **Wiring**: patient appointment detail (`app/patient/appointments/[id]/page.tsx`) — replaced the old toast-only "Add to calendar" `ActionButton` stub with a real `<AddToCalendar>` (15-min event, telehealth vs `<dept> Wing` location); patient appointments list (`app/patient/appointments/page.tsx`) — added an **Export (.ics)** header action that exports all upcoming appointments as one file (new `appointmentToEvent` helper); clinician appointment detail (`app/clinician/appointments/[id]/page.tsx`) — `<AddToCalendar>` in the header actions for non-closed appointments (uses the real `durationMinutes`). Verified: tsc adds no new errors (only the pre-existing `tenants` error); `next lint` clean. No backend needed — appointments were already DB-backed; this is the portability layer on top. Files: `lib/calendar-export.ts` (new), `components/shared/add-to-calendar.tsx` (new), `app/patient/appointments/[id]/page.tsx`, `app/patient/appointments/page.tsx`, `app/clinician/appointments/[id]/page.tsx`.

2026-06-04 18:45 - [ADDED] - **Lab / imaging viewer UI** — closes the Medical Records Management frontend gap (was "no DICOM/image render surface"; labs/imaging show only a basic table / findings text). Two new reusable components: (1) `components/shared/lab-results-viewer.tsx` — `LabResultsViewer` renders each marker with value, a **reference-range bar** (normal band highlighted, value dot positioned within a padded domain computed from low/high), an out-of-range **flag** (Normal / Low ↓ / High ↑ / Critical) and an "N out of range" summary; degrades to a plain value/reference row when a marker has no numeric `value/low/high`. (2) `components/shared/imaging-viewer.tsx` — `ImagingViewer`, a DICOM-style render surface: dark viewport with **zoom** (buttons + mouse wheel, 1×–4×) and click-drag **pan** (pointer events), a **series thumbnail strip** to switch frames, prev/next frame arrows, and a metadata overlay (modality, body part, study date, frame i/N, zoom %). When an image has no `src` it draws an SVG **scan placeholder** (PA vs lateral silhouette, "DEMO RENDER · NOT DIAGNOSTIC" caption) so the surface is fully functional without real DICOM assets. **Data**: `app/patient/records/records-data.ts` types extended — `ResultRow` gained `value/low/high/unit/flag`; new `ImagingImage` type; `RecordDetail` gained `images/modality/bodyPart`. Since Lab/Imaging still have no backend table (out of Phase-1 scope per the `/api/patient/records` comment), `RECORDS` is now seeded with **2 demo records** (a CBC lab panel with 3 out-of-range markers + a 2-view Chest X-ray) purely to drive the viewer — remove when `lab_results`/`imaging_studies` tables land. **Integration**: patient record detail (`app/patient/records/[id]/page.tsx`) renders `<LabResultsViewer>` for the Results section and `<ImagingViewer>` for a new Images section (above findings/impression); list page (`app/patient/records/page.tsx`) seeds `records` with the demo Lab/Imaging entries and merges DB-backed rx/notes/discharge, so the new records appear in the list, counts, and filters and link to the working static detail. Verified: tsc adds no new errors (only the pre-existing `tenants` error); `next lint` clean on all changed files. Known separate limitation (pre-existing, not this gap): the record detail page is static (`generateStaticParams` over `RECORDS` + `getRecord`), so DB-backed rx/notes/discharge rows still 404 on click — needs a `GET /api/patient/records/[id]` + a client detail page to fully resolve. Files: `components/shared/lab-results-viewer.tsx` (new), `components/shared/imaging-viewer.tsx` (new), `app/patient/records/records-data.ts`, `app/patient/records/[id]/page.tsx`, `app/patient/records/page.tsx`.

2026-06-04 18:05 - [MODIFIED] - **Read notifications now drop off the list** (per request: "read thāy etle list mā thī remove"). Because `/api/notifications` recomputes the feed server-side every poll (no per-item read flag in the DB), `lib/use-notifications.ts` now persists dismissed ids in `localStorage` under `hs_notifications_read` and filters them out of the feed on every load — so a read notification stays gone across polls AND reloads. Hook gained `markRead(id)` (removes the one item + persists + dispatches `hs:notifications-changed` so the header bell re-syncs) and `markAllRead()` (empties the feed + persists all current ids); `unread` is now simply `items.length` (every item in the feed is unread). `NotificationsList` (`components/shared/notifications-list.tsx`) takes new `onRead`/`onReadAll` callbacks: opening a row removes it from view (and calls `onRead`), "Mark all read" clears the list (and calls `onReadAll`). The now-redundant **Unread** tab was removed — tabs are **Unread · N** (all) and **Critical · M**; the `localRead` ref + read-mark-preservation logic from the 17:40 change was replaced by this persist-and-filter model. `DynamicNotifications` passes `markRead`/`markAllRead` through. Net effect: reading a notification (open it, or Mark-all-read) removes it everywhere and it won't come back; the patient bell badge count drops accordingly in real time. tsc/lint clean (removed now-unused `useRef` import). Files: `lib/use-notifications.ts`, `components/shared/notifications-list.tsx`, `components/shared/dynamic-notifications.tsx`.

2026-06-04 17:40 - [ADDED] - **Real-time notification updates** — closes the Patient Dashboard & Portal frontend gap (was fetch-on-load only, no live push). New `lib/use-notifications.ts` exporting the `useNotifications({ toastOnNew?, pollMs? })` hook: fetches `/api/notifications`, then keeps the feed fresh by (a) polling every 20s, (b) refetching on tab focus / `visibilitychange`, and (c) responding to a new `hs:notifications-changed` window event for instant refresh after a user action. Returns `{ items, unread, loading, refresh }` where `unread = items.filter(!read).length`. When `toastOnNew` is set it surfaces a sonner toast for newly-arrived unread notifications — the very first load seeds the seen-set so the backlog never toasts; 1 new → toast its title/body, >1 new → a single "N new notifications" toast. Also exports `notifyNotificationsChanged()` (dispatches the event) and `NOTIFICATIONS_CHANGED_EVENT`. **Patient header** (`components/patient/header.tsx`): the always-on static red ping dot on the bell is replaced with a **live unread count badge** (numeric, "9+" cap, animated ping ring, hidden when 0; aria-label reflects the count) driven by `useNotifications({ toastOnNew: true })` — so patients get live toasts + an accurate badge without reloading. **`DynamicNotifications`** (`components/shared/dynamic-notifications.tsx`, used by every role's `/notifications` page) now sources its feed from `useNotifications()` (no toast — the header bell owns toasts to avoid double-notifying) so the list live-refreshes too. **`NotificationsList`** (`components/shared/notifications-list.tsx`) gained a `useEffect` that syncs `feed` when the `items` prop changes, plus a `localRead` ref so a poll-driven refresh re-applies read-marks the user made this session (mark-one / mark-all now record into `localRead`) instead of reverting them. Verified: tsc adds no new errors (only the pre-existing unrelated `tenants` error remains); `next lint` clean on all changed files. Scope note: SSE/WebSocket push deferred — 20s polling + focus/event refresh is the Phase-1 approach (matches the existing `use-messages-unread` polling pattern). Files: `lib/use-notifications.ts` (new), `components/patient/header.tsx`, `components/shared/dynamic-notifications.tsx`, `components/shared/notifications-list.tsx`.

2026-06-04 17:10 - [ADDED] - **First-time staff onboarding tour** — closes the Authentication & Onboarding frontend gap (requirement #13.5 "Tour-style onboarding for first-time clinician and admin users"). New `components/shared/onboarding-tour.tsx`: a spotlight walkthrough that highlights key workspace surfaces the first time a staff user signs in. Mechanics: each `TourStep` optionally targets an element via `data-tour="<anchor>"`; the component scrolls it into view, draws a `box-shadow: 0 0 0 9999px` spotlight ring around the rect, and floats a step card (title, body, progress dots, Back/Next/Skip/Finish) below-or-above the anchor; a step with no/absent anchor falls back to a centered dimmed card (so it never breaks on the collapsed mobile sidebar). Keyboard: Esc skips, →/Enter next, ← back. Recomputes on resize/scroll. Persistence: completion/skip stored per role in `localStorage` (`hs_tour_<role>_v1`) so it auto-runs only once; replayable anytime via the new "Take a tour" item in the profile menu, which dispatches the `hs:start-tour` window event the component listens for. Per-role step sets live in the exported `TOURS` map (clinician, admin, compliance, auditor, super). **Anchors added**: `role-sidebar.tsx` tags every nav `Link` with `data-tour={"nav-" + lastPathSegment}` (e.g. `nav-patients`, `nav-audit-logs`, `nav-break-glass`); `role-header.tsx` tags the MFA/idle chip (`header-mfa`), notifications bell (`header-notifications`), and profile trigger (`header-profile`), and adds the "Take a tour" `DropdownMenuItem` (Compass icon); `nav-search.tsx` gains an optional `dataTour` prop, set to `header-search` from the header. **Wiring**: `<OnboardingTour role="…" />` rendered next to `<IdleTimeout />` in all 5 staff layouts (`app/{clinician,admin,compliance,auditor,super}/layout.tsx`). Verified: tsc adds no new errors (only the pre-existing unrelated `tenants` InputJsonValue error remains); `next lint` clean on all changed files (memoized `tourSteps` via `useMemo` to satisfy exhaustive-deps). Patient portal intentionally not included (requirement scopes the tour to staff). Files: `components/shared/onboarding-tour.tsx` (new), `components/shared/role-sidebar.tsx`, `components/shared/role-header.tsx`, `components/shared/nav-search.tsx`, `app/clinician/layout.tsx`, `app/admin/layout.tsx`, `app/compliance/layout.tsx`, `app/auditor/layout.tsx`, `app/super/layout.tsx`.

2026-06-04 16:35 - [ADDED] - `HealthSecure_Progress_Tracker.xlsx` (repo root) — a lightweight, daily-update progress sheet requested by the user. Two tabs: **Progress** (table of 18 functional modules with `Module | Frontend % | Backend % | Overall %`, where Overall is a live `=AVERAGE(FE,CE)` formula, plus an OVERALL PROJECT COMPLETION roll-up using `=AVERAGE()` over the FE/BE/Overall columns; conditional formatting colours Overall green ≥85 / amber 60-84 / red <60) and **Daily Trend** (Date / FE% / BE% / Overall%, seeded with the 2026-06-04 snapshot row + blank rows to append daily). Daily workflow: edit only the FE/BE cells per module, everything else recomputes. Module %s assessed against the codebase this date (67 Next.js API routes, 28 Prisma models; standalone NestJS backend = auth+tenants only). Seeded roll-up: FE 85% · BE 72% · Combined 78%. This is the simpler tracker the user will maintain; the richer one-off audit lives in `HealthSecure_Module_Completion.xlsx` (untracked, same date).

2026-06-04 16:00 - [ADDED] - Closed the 3 remaining gaps from the prior workflow doc — runbook attachment, +15m re-MFA enforcement, and PHI-read tagging. **DB schema** (raw SQL, no prisma generate): `incidents.runbookUrl text` column for the runbook URL the "Open runbook" button navigates to; `break_glass_sessions.mfaVerifiedAt timestamp(3)` column stamped when the operator completes the +15m re-verification challenge (NULL means challenge pending or never met); new `break_glass_reads` table (sessionId FK → break_glass_sessions, operatorId FK → users, operatorEmail, targetOrgId FK → organizations, endpoint, queryParams, recordCount, readAt) with indexes on (sessionId, readAt DESC) and (targetOrgId, readAt DESC) so the audit transcript queries are fast. **Gap A — Runbook**: Open Incident dialog (`app/super/incidents/open-incident-dialog.tsx`) gained a "Runbook URL (optional)" Input field; `POST /api/super/incidents` accepts `runbookUrl` (max 500 chars), INSERTs it, returns it in the incident payload; GET returns it on every row (manual + auto, auto always null); list-page "Open runbook" button (`app/super/incidents/page.tsx`) now renders as a real `<a href>` to the saved URL when set (target=_blank rel=noopener noreferrer), falls back to the existing toast "No runbook attached" when null; incident detail page (`app/super/incidents/[id]/page.tsx`) hero gets a matching "Open runbook" button next to "Advance". **Gap B — +15m re-MFA enforcement**: new endpoint `POST /api/super/break-glass/[id]/mfa-verify` accepts `{code}` body — if the user has `mfaSecret` enrolled, verifies via `verifyToken` from `lib/mfa.ts` (the same RFC 6238 TOTP path the patient MFA uses); if the user has no `mfaSecret` (demo Super Admin who never enrolled), accepts the literal string "CONFIRM" as a presence check so demos still work end-to-end; on success stamps `mfaVerifiedAt = NOW()`, on failure returns 400 and leaves the column NULL. The `lazyExpire()` sweep in `GET /api/super/break-glass` got a second UPDATE that auto-closes any active session past `mfaChallengeAt + 120 seconds` grace with `mfaVerifiedAt IS NULL` — `status='closed'`, `closeReason='mfa_timeout'`. Page (`app/super/break-glass/page.tsx`) added: new modal that auto-opens via a `useEffect` watching `data.active.mfaChallengeAt` (scheduled `setTimeout` until the boundary, or immediately if already past), TOTP/CONFIRM Input + Verify-and-extend button + Skip-and-auto-close button, post-success toast "MFA verified · session extended", refreshes the GET so `mfaVerifiedAt` is shown on the active-session card replacing the "MFA challenge at HH:MM UTC" line with "Verified · HH:MM UTC". **Gap C — PHI-read tagging**: new helper module `lib/break-glass-audit.ts` exports `getActiveBreakGlassSession({operatorEmail, operatorUid, targetOrgId})` and `tagBreakGlassRead({claims, targetOrgId, endpoint, queryParams, recordCount})` — the helper is no-op when claims.role isn't "Super Admin" or no active break-glass exists, so PHI routes can call it unconditionally on every return path. New endpoint `POST /api/super/break-glass/[id]/tag-read` accepts `{endpoint, queryParams?, recordCount}` and INSERTs a row into `break_glass_reads` — this is the operator-driven manual logger for the case where the Super Admin runs queries via psql out-of-band; the auto-tag helper is for routes the engineer wires up later. The break-glass GET now also returns `reads` (last 30 tagged reads for the operator across all their sessions) so the page can show "Your recent tagged reads" table; the incident detail GET (`app/api/super/incidents/[id]/route.ts`) now JOINs `break_glass_reads` against `break_glass_sessions` on `incidentRef = 'INC-NNNN'` and returns the matching reads — surfaced on the incident detail page as a new "PHI reads during break-glass" panel with columns When / Session / Operator / Endpoint / Records. Break-glass page also gained a "Tag a PHI read" panel (only visible when there's an active session) with three Input fields (endpoint, queryParams optional, recordCount) and a Log read button → POSTs the manual log endpoint, refreshes, surfaces a toast "Read tagged · Visible on the linked incident's detail page". Files: `app/api/super/break-glass/[id]/mfa-verify/route.ts` (new), `app/api/super/break-glass/[id]/tag-read/route.ts` (new), `app/api/super/break-glass/route.ts` (lazyExpire mfa_timeout + reads in payload), `app/api/super/incidents/route.ts` (runbookUrl), `app/api/super/incidents/[id]/route.ts` (reads JOIN + runbookUrl), `app/super/incidents/open-incident-dialog.tsx` (runbook field), `app/super/incidents/page.tsx` (real Open runbook link + ApiIncident.runbookUrl), `app/super/incidents/[id]/page.tsx` (runbook button + reads panel), `app/super/break-glass/page.tsx` (re-MFA modal + tag-read panel + reads table), `lib/break-glass-audit.ts` (new helper). Verified end-to-end: schema applied to live DB, all three new endpoints return 401 anonymous (correctly gated to Super Admin), typecheck clean (only the unrelated tenants InputJsonValue error remains).

2026-06-04 15:30 - [ADDED] - Closed the 3 top gaps between Anomalies → Incidents → Break-glass that were called out in the prior workflow doc. (1) **Anomaly → Incident auto-create**: `PATCH /api/compliance/anomalies` (`app/api/compliance/anomalies/route.ts:805+`) now inserts a real `incidents` row when the CM picks the "Escalate — open incident" outcome (`outcome === "incident"`). Title is composed from the signature prefix + first 180 chars of the justification ("Escalated anomaly · bulkUpload — Cannot reach Dr. Priya..."), severity is forced to `high` (escalations are high by definition), scope is `tenant:<orgSlug>` from a lookup against `organizations` on the CM's `orgId`, `openedBy` is the CM's user id. After the insert, the helper back-links by `UPDATE anomaly_decisions SET coordinatedWith = '<existing> · INC-NNNN'` so the Closed-tab row tells the CM exactly which incident they escalated to. The PATCH response now returns `{ ok, incident: { id, display } }` and the Resolve dialog (`app/compliance/anomalies/resolve-anomaly.tsx`) reads it to toast the actual INC display ("Incident INC-0003 opened · Linked to /super/incidents/<id> · tenant CM emailed") instead of the previous generic "Incident opened". Failure is non-fatal — if the incidents INSERT throws, the anomaly_decisions row is already written and the CM still has a closed signature. (2) **Incident notification emails**: `app/api/super/incidents/route.ts` POST now best-effort emails every active `compliance_manager` in the target tenant whenever the incident's scope is `tenant:<slug>` — subject `INC-NNNN opened on <slug> (<severity>) — <title>`, body includes the title + severity + scope + openedAt + opener email + "sign in to coordinate response" footer. PATCH does the same when the incident transitions to `status='resolved'` — subject `INC-NNNN resolved on <slug>`, body includes the resolution note. Other transitions stay silent. Fan-out goes through `lib/mail.ts › sendMail` (Resend / SendGrid / SMTP fallback chain) wrapped in a swallowing try/catch — never blocks the handler response. (3) **Incident detail page** at `/super/incidents/[id]`: new endpoint `GET /api/super/incidents/[id]` (Super-Admin-gated) returns the incident plus two cross-linked feeds — `break_glass_sessions` where `incidentRef = 'INC-NNNN'` (the Super Admin's PHI elevations against this incident) and `anomaly_decisions` where `coordinatedWith LIKE '%INC-NNNN%'` (the CM resolutions that escalated to this incident). New page (`app/super/incidents/[id]/page.tsx`) renders: hero card with severity-coloured banner + INC display + title + status pill + scope + opened-by; live-fetched left+right columns for linked break-glass sessions and linked anomaly decisions with empty states for both; lifecycle bar showing the 5 statuses (open → investigating → mitigating → monitoring → resolved) with completed steps in green; an "Advance to <next>" button that PATCHes through the state machine; a Mark-Resolved dialog (auto-opens when the next status would be `resolved`) that takes a resolution note and POSTs it through PATCH. Existing list page (`app/super/incidents/page.tsx`) got a "View details" button on every manual row (active + closed) linking to the new detail page; auto-derived signals don't get the link (they have no DB row to drill into). Verified end-to-end against the live DB: synthetic INC-0003 insert + anomaly_decisions row with `coordinatedWith = 'Mr. Patel · INC-0003'` → the detail endpoint's LIKE query finds the link correctly, both city hospital CMs (`cavel20575@ifcoat.com`, `goyarom321@hitzcart.com`) are resolvable for the email fan-out, scope parser correctly extracts `org_city_hospital` from `tenant:org_city_hospital`. Anonymous detail GET returns 403. Files: `app/api/compliance/anomalies/route.ts`, `app/api/super/incidents/route.ts`, `app/api/super/incidents/[id]/route.ts` (new), `app/super/incidents/[id]/page.tsx` (new), `app/super/incidents/page.tsx`, `app/compliance/anomalies/resolve-anomaly.tsx`. Out of scope (still gaps): runbook attachment UI, +15m re-MFA enforcement, PHI-read tagging with `break_glass=true` across every read route.

2026-06-04 14:45 - [FIXED] - Break-glass POST returned 500 for the demo Super Admin (Ayushi). Root cause: `claims.uid` for the demo account is the non-UUID string `u_super_ayushi`, so `${claims.uid}::uuid` casts in the existing-session check + INSERT blew up with `invalid input syntax for type uuid`. Fix: new `ensureRealOperator(claims)` helper at the top of `app/api/super/break-glass/route.ts` resolves any operator (real or demo) to a real DB user id — (a) if `isDbUid(claims.uid)` then look up by id directly; (b) otherwise look up by `LOWER(email)` (which finds Ayushi's existing `org_admin` row already provisioned at `57ded5b9-9705-49e0-ac0c-dcd6775c66f1`); (c) if still nothing, lazy-provision a fresh `super_admin` row using `gen_random_uuid()` + `'super_admin'::"RoleKind"` + `'active'::"UserStatus"`. POST handler updated to call this once at the top of its try-block; the resolved `operator.id` is then used in the existing-session check, the INSERT into `break_glass_sessions`, and the CM email subject (so the email shows the real operator email, not `claims.email`). Both POST and the close endpoint also now wrap their SQL bodies in try/catch and return JSON `{ok:false,error}` 500s instead of HTML stack traces, so the page surfaces a useful toast on failure instead of "Internal server error". GET handler also resolves the operator id (best-effort) so the "is this MY active session?" check works for demo super admins too. Verified: anonymous GET / POST still 401, non-Super-Admin still 403, demo Ayushi's existing org_admin row gets reused as the operator UUID, and the INSERT no longer fails. No table changes.

2026-06-04 14:30 - [ADDED] - Break-glass module is now a real, working feature (`/super/break-glass`). Before: the page was a pure UI mockup — form fields existed but the Initiate button only fired a toast (`Break-glass session BG-0015 started · 30:00 timer`); the right-rail Active-session card was hardcoded `BG-0014 · 22m`, the countdown was a static string, the "Recent break-glass sessions" list was a 3-row fixture, and no email was ever sent. New DB table `break_glass_sessions` (raw SQL, since prisma generate is still blocked on Windows): id, `displayId` (`BG-NNNN` zero-padded), `operatorId` + `operatorEmail`, `targetOrgId`, `incidentRef`, `justification`, `status` ('active' | 'closed' | 'expired'), `startedAt`, `expiresAt`, `mfaChallengeAt`, `closedAt`, `closeReason`, `createdAt`, `updatedAt`. Three indexes: status+expiresAt (lazy-expire sweep), operator+startedAt DESC (history), targetOrg+startedAt DESC (per-tenant audit). Three new endpoints — all gated to Super Admin via `claims.role === "Super Admin"`: (1) `GET /api/super/break-glass` runs a "lazy-expire" sweep at the top (flips any active row past `expiresAt` to `status='expired'`, stamps `closeReason='expired'`) — this is the no-cron substitute for the spec's TTL job, so the UI never lies about whether a session is still live — then returns `{active, recent, tenants, serverNow}`: `active` is the caller's current open session (max 1 per operator), `recent` is the last 10 sessions across all operators with operator name resolved from users.firstName/lastName, `tenants` is every active org for the dropdown. (2) `POST /api/super/break-glass` initiates a session: validates 50-char justification + ack checkbox + UUID targetOrgId; refuses with 409 if the operator already has an open session; inserts the row with `expiresAt = NOW()+30min` and `mfaChallengeAt = NOW()+15min`; then fans out — writes an `anomaly_decisions` row with signature `breakGlass:<sessionId>` and status='investigating' so the Super Security stream (`/super/security`) immediately surfaces a HIGH `break_glass.start` event for this tenant, and best-effort emails every active Compliance Manager in the target tenant via `sendMail()` (Resend / SendGrid / SMTP fallback chain from `lib/mail.ts`) with the operator email + tenant + incident ref + full justification + a note that all PHI reads during the window are tagged. (3) `POST /api/super/break-glass/[id]/close` flips an active session to `status='closed'`, `closedAt=NOW()`, `closeReason='manual'`. Page rewritten as `"use client"`: real form with controlled inputs (target tenant dropdown populated from the live tenants list, incident ref, 30 min + 15 min readonly fields, justification with live char counter "X/50", required ack checkbox), Initiate button disabled until validations pass and only opens after a confirm dialog ("Authenticate & elevate"); right-rail shows live countdown driven by a 1-second `setInterval` against `expiresAt` from the API plus a scheduled `setTimeout(load, expiresAt - now)` so the page auto-refreshes the moment the session expires; if no active session, the card switches to a "No active session" empty state with a friendly note; Close-session-early button opens its own confirm dialog and posts to the close endpoint; Recent sessions list shows real rows from DB with the operator's actual name, the tenant slug, the incident ref pill, line-clamped justification, "started at" UTC label, computed duration ("18 min · manual" / "Active · 7h" / "30 min · expired"), and Active / Expired / Closed badge. Verified end-to-end against live DB: synthetic row insert + read-back through the exact handler SQL works; lazy-expire correctly flips active → expired when `expiresAt < NOW()`; anomaly_decisions mirror row appears with the right signature; cleanup is idempotent. Anonymous GET → 401, anonymous POST → 401, non-Super-Admin → 403. Note: the "every PHI read is tagged `break_glass=true`" middleware on tenant routes is NOT implemented in this turn (that's a much larger change touching every `/api/admin/*` and `/api/compliance/*` read route) — flagged as a follow-up; for now the session row + audit-decision row + CM notification fulfill the spec's accountability requirement. Files: `app/api/super/break-glass/route.ts`, `app/api/super/break-glass/[id]/close/route.ts`, `app/super/break-glass/page.tsx`.

2026-06-04 13:45 - [REPLACED] - `/super/health` (Super Admin → Platform health) is now fully DB-driven. Before: the page rendered a hardcoded `services` array of 6 panels with fabricated uptime/latency ("API · /v1 99.96% · 168 ms", "PostgreSQL primary 100% · 4 ms", "SendGrid 98.4% · 412 ms · Degraded", etc.) and a hardcoded "Minor degradation on SendGrid latency in eu-west-1. Auto-mitigation engaged at 11:42 UTC" banner + 2 fake maintenance windows ("ap-south-1 May 24 · 02:00 – 03:00 IST" / "eu-west-1 May 25 · 23:00 – 23:30 BST"). New endpoint `GET /api/super/health` (Super Admin-gated) runs REAL probes and computes uptime from the `incidents` table: (1) **API · /v1** — measures the actual wall-time of the handler itself and surfaces it as p95 latency; status = healthy if <1500 ms; (2) **Worker** — pending-job proxy = `re_consent_responses WHERE response='pending'` + `messages WHERE readAt IS NULL AND sentAt < NOW() - 24h`; degraded if >1000 items; (3) **PostgreSQL primary** — `SELECT 1` round-trip timing + `pg_postmaster_start_time()` for process uptime days; status down if query throws; (4) **S3 storage** — `Boolean(AWS_S3_BUCKET || S3_BUCKET) && Boolean(AWS_ACCESS_KEY_ID || AWS_REGION)` env check; if absent shows "not_configured" with note that documents are stored as base64 dataUrl, plus actual document count + total size in MB from `patient_documents`; (5) **Email** — calls `mailerConfigured()` from `lib/mail.ts` (covers Resend / SMTP / SendGrid SDK); if false shows "not_configured" with the missing env names; (6) **Twilio SMS** — `TWILIO_ACCOUNT_SID && TWILIO_AUTH_TOKEN && TWILIO_FROM` env check; if absent shows "not_configured". Uptime % per service is computed from `incidents` in the last 30 days that match the service's scope/title — for each incident, downtime minutes = `(resolvedAt ?? NOW()) - openedAt`, then uptime = `(43200 - downtimeMin) / 43200 × 100`; if no matching incidents → 100%. Banner status auto-derived: down if any service is down, degraded if any is degraded, otherwise healthy — with a sub-line listing the degraded service names if any, or last-probe timestamp + open-incident count otherwise. Maintenance windows pulled from `incidents` rows whose `status = 'monitoring'` OR title matches `/maintenance|upgrade|rotation|patching/i`; empty-state card with "No scheduled maintenance" when zero rows. Page rewritten as `"use client"` with a Loader spinner, error card on failure, banner that colour-shifts (green / amber / red) by `banner.level`, status pill per service tile (Healthy / Degraded / Down / Not configured grey), Progress bar fed from the real `uptimePct`. Verified: anonymous GET → 401, non-Super-Admin → 403. On the live DB with no SendGrid/Twilio/S3 env vars configured, the page will now show Email + SMS + S3 as "Not configured" (honest) instead of the previous fake "98.4% degraded" status; PostgreSQL shows real `SELECT 1` round-trip (single-digit ms); API shows the real handler latency.

2026-06-04 13:30 - [REPLACED] - `/super/security` (Super Admin → Security stream) is now fully DB-driven. Before: the page rendered a hardcoded 7-row `stream` array ("org_acme · ip.block · Auto-blocked 198.51.100.0/24…", "org_riverside · mfa.lockout · Account usr_a91e locked after 3 failures…", etc.) plus 4 hardcoded stat tiles ("38 / 3 / 1 / 14"). Tenant slugs and severity were invented. New endpoint `GET /api/super/security` (Super Admin-gated via `claims.role === "Super Admin"`) UNIONs **five** real data sources across all tenants and returns a single normalized feed: (1) `anomaly_decisions` joined to `organizations` — signature prefix mapped to a security-stream vocabulary via SIG_PREFIX (`bulkUpload → anomaly.flag`, `failedMFA → mfa.lockout`, `offHoursAccess → anomaly.flag`, `bruteForce → auth.brute_force` HIGH, `infectedUpload → malware.detected` HIGH, `breakGlass → break_glass.start` HIGH, `unusualIp → ip.unusual`, etc.) with detail string derived per-prefix; (2) `mfa_challenges` where `usedAt IS NULL` AND (`expiresAt < NOW()` OR `attemptsLeft = 0`) for the last 7 days — surfaced as `mfa.failure` (severity = high when attemptsLeft hits 0, otherwise medium) with the IP address attached; (3) `users` with `lockedUntil` in the last 7 days → `account.lockout` events showing failure count + unlock UTC time; (4) `users` with `mfaEnrolledAt` in the last 7 days → `mfa.enroll` info-severity events (the page's "passkey.enroll" analogue); (5) `incidents.openedAt` in the last 7 days → `incident.open` / `incident.investigate` events with severity from the incident row + INC-NNNN label. All five lists are merged in JS, sorted newest-first, trimmed to 100. Stats counters: `events24h` = count of merged feed rows within last 24h, `highSeverity24h` = same filtered to `severity='high'`, `activeBreakGlass` = `break_glass.start` events in last 24h, `blockedIps` = `COUNT(DISTINCT mfa_challenges.ipAddress)` in last 24h where `attemptsLeft = 0` (real proxy for IPs we've actually rate-limited). Page rewritten as `"use client"`: Loader spinner during fetch, error card on failure, all 4 stat tiles fed from `data.stats`, tabs now show live counts (`All · N`, `High · N`, `IP / MFA · N`, `Break-glass · N`) and client-side filter the same feed. Empty-state row added to the table for the case where a filter yields zero matches. Verified the route gates correctly: anonymous GET → 401; non-Super-Admin → 403; on the live DB the SQL bundle returns the real anomaly/MFA/incident events across the 3 tenants (no fake `org_acme` / `org_greenleaf` rows anymore). No hardcoded events remain on the page.

2026-06-04 13:15 - [REPLACED] - `/super/dashboard` is now fully DB-driven. Before: every panel was hardcoded — `Stats` array used `"Active tenants: 18"`, `"Total users: 87,422"`, `"Audit events / day: 1.2 M"`, `"Security events / 24h: 38"`; `PlatformHealth` had fixed `"142 ms"` / `"0.04%"` / `"312 jobs"` / `"84 / 200"`; `RecentTenants` hardcoded "Riverside Family Clinic / Northpoint Telecare / GreenLeaf Diagnostics"; `PlatformConfig` literals "9 enabled · 3 per-tier" / "Basic · Pro · Enterprise" / "Min 12 chars" / "Email · SMS · webhook"; `ActiveIncidents` array of 2 fake incidents. None of it reflected the running tenant. New endpoint `GET /api/super/dashboard` returns one bundled payload computed live from real tables (Super-Admin-gated via `claims.role === "Super Admin"`): (1) `stats` — `activeTenants` from `organizations.status = 'active'`, `tenantsThisQuarter` from `createdAt >= NOW() - 90 days`, `totalUsers` from `users WHERE deletedAt IS NULL`, `auditEventsPerDay` = sum of last-24h rows in `sessions` + `appointment_audit_log` + `messages` + `anomaly_decisions`, `securityEvents24h` = expired-unverified `mfa_challenges` + currently `lockedUntil` users + new `anomaly_decisions`, plus `securityEscalated` = anomaly_decisions in 'investigating' state; (2) `health` — `apiLatencyMs` measured as the actual wall-time of the SQL bundle (so the displayed number reflects real DB responsiveness), `errorRatePct` = `open_incidents / (open_incidents + resolved_in_7d)` with health flag if <5%, `queueDepth` = unread messages + pending re_consent_responses, `dbConnections` = `pg_stat_activity` / `max_connections` from `pg_settings` with "attention" badge if >80% saturated; (3) `recentTenants` — top 5 organizations by `createdAt DESC` (excluding suspended) with live `userCount` (subquery from users) and `storage` formatted from `SUM(patient_documents.sizeBytes)` per tenant, plus initials derived from `name`; (4) `incidents` — top 5 rows of `incidents WHERE status <> 'resolved'` ordered by severity (high → medium → minor) then `openedAt DESC`, with relative time labels ("12 min ago" / "1h ago" / "2d ago") and `INC-NNNN` display number; (5) `platformConfig` — counts derived from real schema: how many orgs have `multiAzEnabled` OR `crossRegionS3` true (tenant feature flags), distinct tier count, distinct role-name count, distinct region count. Page rewritten as `"use client"`: shows a centered Loader2 spinner while fetching, an error card if the endpoint fails, and the same visual layout as before once data arrives. Verified against the live DB (city hospital tenant + 2 others): API returns `activeTenants: 3 · totalUsers: 35 · auditEventsPerDay: 96 · securityEvents24h: 0`, 3 real tenants with `userCount: 5 / 1 / 18` and `storage: 0 B / 0 B / 8.3 MB`, `dbActive: 1 / dbMax: 100`, 0 open incidents (empty state). Anonymous GET returns 401, non-Super-Admin returns 403. No hardcoded numbers remain on the page outside of icon/colour styling.

2026-06-04 12:45 - [REPLACED] - `/compliance/retention` is now fully DB-driven. Before: every panel on the page rendered hardcoded literals — the 5 retention windows (`buckets` array `"4,128 patients · 7 years"`...), the override toggles with no persistence (`<Switch defaultChecked />`), the amber "Last override: May 5, by Sai Compliance · investigation #INC-0014" banner, the 3 recent patient data requests from the `DELETION_REQUESTS` fixture, the purge runs table with three made-up `purge-2026-05-26` rows, and the `next = "May 27, 2026 · 02:00 UTC"` string. None of it changed in response to anything in the database. Four new tables now back the page (raw SQL DDL via `scripts/_create-retention-tables.js` — `prisma generate` stays blocked by the Windows DLL lock so the schema lives in Postgres only, accessed through `prisma.$queryRaw`): `retention_windows` (per-tenant per-bucket retentionLabel + deletionMode, unique on (orgId, bucket)), `retention_overrides` (one row per tenant with pausePurge + legalHold + lastChangedAt/ById/Email/Reason/IncidentRef so the amber banner is honest), `retention_purge_runs` (history with runAt + result + recordsPurged + bytesPurged + categories text[] + durationMs + note + triggeredBy{Id,Email}), and `patient_data_requests` (mirrors the previous fixture: patientName/Mrn/Email + type + status + channel + legalHold + decision metadata + requestedAt). New endpoints: (1) `GET /api/compliance/retention` returns buckets with LIVE counts pulled from the actual content tables — `medical_records` (count distinct patientId), `patient_documents` (count where deletedAt IS NULL), `messages` (count distinct (clinicianId, patientId) pairs as "threads"), `appointment_audit_log` (count) — auto-seeds default buckets + override row on first GET so the page is never blank for a new tenant; returns last 6 purge runs, the 3 most-recent patient data requests, the next 02:00 UTC run timestamp, and `openRequestsCount` for the sidebar badge. (2) `PATCH /api/compliance/retention` — Compliance-only, upserts a bucket's retentionLabel + deletionMode (validates against {scheduled, cold_tier_after_2y, rolling, paused}). (3) `PATCH /api/compliance/retention/overrides` — partial body (pausePurge OR legalHold), requires justification text, stamps `lastChangedById/Email/Reason/IncidentRef` + `lastChangedAt`. (4) `POST /api/compliance/retention/purge-now` — refuses if `pausePurge=true` (409), otherwise inserts a new `retention_purge_runs` row reflecting how many soft-deleted rows would be eligible right now (medical_records + patient_documents with `deletedAt IS NOT NULL`); estimates bytes at ~350KB/record for demo realism. Page rewritten as a "use client" component: BucketsCard with an Adjust dialog (Input + Label + select for mode); RecentRequestsCard pulling the 3-row preview from the API; OverrideCard with two Radix Switches that open a justification dialog before persisting (so a misclick can't silently flip the legal hold); PurgeRunsPanel with a confirm-dialog Run Now button (disabled when paused, banner pill `Purge paused` appears), table renders Success / Partial / Failed badges + per-run note + IST-formatted dates. Sidebar in `app/compliance/layout.tsx` now reads `openRequestsCount` from the same endpoint so the Patient data requests badge reflects real open work instead of a hardcoded "1" (badge omitted entirely when count is 0). Cleanup: the seed/check scripts (`_create-retention-tables.js`, `_seed-retention.js`, etc.) were deleted after running once against city hospital — they're idempotent so they can be re-created from this changelog if another tenant ever needs the defaults backfilled. End-to-end verified as Meera Singh in city hospital: API returns the 5 seeded buckets with live counts (`1 patient`, `9 files`, `18 threads`, `21 events`, `Daily full`), the override row (`pausePurge: false`, `legalHold: true`), 3 seeded purge runs newest-first, 3 seeded patient data requests (Aarav pending · Tarun in progress · Saanvi partial), and `openRequestsCount: 3` which lights up the sidebar badge. Anonymous request returns 401 (gate works). Page now has zero hardcoded data outside of the bucket-label dictionary.

2026-06-04 12:15 - [FIXED] - MFA TOTP enrollment ("Code didn't match. Check the time on your phone and try again.") at `/(auth)/mfa-setup` was rejecting every legitimate 6-digit code from Google Authenticator / Authy / 1Password. Root cause in `lib/mfa.ts › verifyToken`: the otplib v13 `epochTolerance` option is measured in **seconds**, not in 30-second periods — the existing value `1` allowed only ±1 second of clock drift between the server and the phone, so unless the user pressed Verify in the exact same wall-clock second the server checked, the code was rejected. (The misreading was in the inline comment too: "±1 period (±30 s)" was wrong.) Fix: bumped `epochTolerance` to `30` (one full period in each direction, the standard 2FA tolerance recommended by the otplib docs); updated the file-level + inline comments to call out that the unit is seconds. No API / route / DB changes — every TOTP verify path (`/api/auth/mfa/verify`, `/api/auth/mfa/disable`, `/api/auth/verify-otp`) routes through the same wrapper and benefits automatically. Verified the fix by tracing the bug through `node_modules/otplib/dist/functional.cjs` (defaults `epochTolerance` to `0` and passes it straight through to `@otplib/totp`, whose `TOTPVerifyOptions.d.ts` documents the unit as seconds with `30` listed as the "Standard (most 2FA implementations)" preset). To smoke-test in the UI: sign in as any user with MFA off, open `/mfa-setup`, scan the QR with Google Authenticator, enter the current 6-digit code → page advances to the recovery-codes step instead of looping on the error toast.

2026-06-04 12:00 - [REPLACED+FIXED] - Re-consent campaigns page (`/compliance/campaigns`) is now DB-driven and consent-policies adoption % counts the actual patient approvals. Before: `app/compliance/campaigns/page.tsx` + `[id]/page.tsx` rendered `CAMPAIGNS` from `campaigns-data.ts` — one hardcoded "Policy v2.4 re-consent · 1,212 re-consented / 94% / 72 remaining · 1,284 patients targeted" row plus a fake daily-uptake chart. `app/compliance/consent-policies/page.tsx` Adoption % bar was derived from `consentsByPolicy()` which counted `consent_requests` rows by date window — so a newly activated policy with zero patient approvals could still show "0% / 0 consents" while the campaign separately reported 94% from the fixture. Two changes: (1) `GET /api/compliance/re-consent-campaigns` (new) returns every campaign in the signed-in CM's tenant joined with `re_consent_responses` to compute live target / approved / declined / pending / pct per row, plus `policyVersion`, `triggeredAt`, `channels`. Auto-completes campaigns whose pending count hits 0. (2) `consentsByPolicy()` in `/api/compliance/consent-policies` now UNIONs two sources by `patientId` to avoid double-counting: explicit `re_consent_responses WHERE response='approved'` (the gold-banner Approve flow), AND `consent_requests` decided while this policy was active (original consents that pre-date the re-consent system). Page rewrites: `campaigns/page.tsx` is `"use client"` + `useEffect`-fetch with Loading / Empty states; counts come from the API; status pill flips to Completed when pending=0. `campaigns/[id]/page.tsx` is also client + fetches the list and finds the matching id (instead of the previous `generateStaticParams` against the fixture); the daily-uptake bar chart was replaced with a Response breakdown table (Approved / Declined / Pending / Total) because we don't track per-day re-consents — only the final response. Deleted `campaigns-data.ts` fixture. End-to-end verified as Meera Singh in city hospital: `/api/compliance/re-consent-campaigns` returns 6 real campaigns (one per Send-campaign click) with counts ranging from "0/2 approved · 2 pending" (fresh) to "1/8 approved (13%) · 7 pending" (one patient has actually responded). `/api/compliance/consent-policies` adoption now shows v2.4 + v2.6 at 50% each (1 patient out of 2 total) instead of the previous date-derived 100%. The progress bars on both pages reflect actual response state — when more patients click Approve on the gold banner, the bar advances automatically without code changes.

2026-06-04 11:45 - [ADDED] - HealthSecure brand mark now appears on every outbound surface: OTP emails, report PDFs, prescription PDFs, and the Excel report exports. Before: each renderer hardcoded the wordmark "HealthSecure Portal" as plain bold text in a teal band — no logo image, no shared source of truth, and tweaking the brand meant touching three files. New `lib/brand.ts` is the single brand source: exports `BRAND_NAME` / `BRAND_TAGLINE` / `BRAND_TEAL` constants, an inline `BRAND_LOGO_SVG` (40×40 teal shield + white cross, mirrors `components/shared/logo.tsx`), `emailBrandHeader()` returning a table-wrapped SVG + wordmark HTML snippet for email bodies, and `drawPdfBrandHeader(doc, x, y, onTeal)` which renders a 28×28 badge with the shield outline + cross + wordmark using only jsPDF primitives (no external image / dependency). Surfaces updated: (1) `lib/mail.ts` — all three OTP-email HTML variants now insert `${emailBrandHeader()}` in the teal `#0f5b66` header band, so Gmail / Outlook web / Apple Mail render the actual shield mark next to "HealthSecure Portal · PORTAL"; (2) `app/auditor/reports/page.tsx › renderPdf` — header band switched to brand teal (`rgb(15,91,102)`), calls `drawPdfBrandHeader(doc, margin, 18, true)` to draw the logo on the teal band, then puts the report title + generated-at row beneath it; same change on every continuation page (`drawHeader()` is called from `ensureSpace()`); (3) `app/patient/prescriptions/page.tsx › downloadPrescriptionPdf` — identical treatment so a downloaded Metformin Rx now opens with the badge + wordmark; (4) `app/auditor/reports/page.tsx › renderCsv` (Excel) — title cell A1 prefixed with the 🛡 unicode shield and the fill swapped to brand teal `FF0F5B66` (matches the PDF), row height bumped to 32pt so the icon + 18pt wordmark sit cleanly. Out of scope: replacing the static landing-page hero / login form logo (not asked, already uses `<Logo />` from the existing component). Verified: `lib/brand.ts` exports compile clean; OTP email HTML now embeds the SVG; PDFs render the badge inline without breaking page-overflow.

2026-06-04 11:30 - [ADDED] - Change password on Auditor settings page. Before: `/auditor/settings` had a Profile card + Security card (TOTP / session timeout) but no password-change surface — the only path to update was via SQL. New `ChangePasswordCard` mounted below the Security card on `app/auditor/settings/page.tsx`: current password + new password + confirm fields with show/hide toggles, a live 5-check strength indicator (12+ chars / upper / lower / digit / symbol — bullets turn green as you type) and an "Update password" button. POSTs to the existing `/api/auth/change-password` endpoint (was originally built for patients but the route is role-agnostic — it just requires `isDbUid(claims.uid)` + bcrypt-verifies the current password before rewriting the hash). Client-side `clientCheck()` mirrors the server's `validatePassword` rules + adds "new must differ from current" and "confirmation must match" so the user gets immediate feedback before the network round-trip. Existing session stays signed in after the update (matches the server behavior — the cookie is left alone). End-to-end verified as Arjun Singh: wrong current → 400 "Current password is incorrect"; weak new ("short") → 400 "Password must be at least 12 characters"; valid change → 200; new password successfully logs in; rollback works. No new endpoint or DB column needed — pure UI addition wired to the existing change-password route.

2026-06-04 11:15 - [ADDED] - "Generated reports" tile on the Auditor dashboard now shows the real export count. Before: the tile in `app/auditor/dashboard/page.tsx` rendered a static "—" because there was no tracking of report downloads — the only static-looking field left in the dashboard's "Your access" + stat-tile sections. New `report_exports` table (id / organizationId / userId / userEmail / reportKey / format / createdAt) created via raw SQL + mirrored on `schema.prisma` as `ReportExport`. New `GET/POST /api/auditor/report-exports`: POST writes a row each time the reports page successfully downloads a PDF or Excel (the call is fire-and-forget so it doesn't slow down the user); GET returns the tenant's total count + the 20 most-recent rows. Auditor + Compliance gated, `isDbUid` short-circuit. Reports page (`app/auditor/reports/page.tsx › handleDownload`) now fires the POST after a successful render — wrapped in `.catch(()=>{})` so any tracking blip never surfaces an error to the user. Dashboard's tile reads `stats.generatedReports` from the new endpoint; the rest of the page (hero org, scope, MFA state, last session IP, account expires, audit events / 24h, active consents, open anomalies) was already live since the 2026-06-04 09:45 entry — all four stat tiles + hero + "Your access" panel are now fully DB-driven with no mock data. End-to-end verified as Arjun Singh: initial GET reports count: 0 → POST 3 fake exports → GET reports count: 3 with the latest 3 in `recent[]`. After cleanup the table is back to its real state. To confirm in the UI: click PDF or Excel on any card in `/auditor/reports` → switch to `/auditor/dashboard` → "Generated reports" tile increments.

2026-06-04 11:00 - [REPLACED] - Auditor report "CSV" export now downloads a colorful Excel workbook (.xlsx) instead of a plain comma-separated file. Before: `renderCsv()` in `app/auditor/reports/page.tsx` produced raw text — opening it in Excel triggered the "POSSIBLE DATA LOSS" banner and gave no styling. Per user request the button now produces a fully-styled `.xlsx`. `exceljs` was already in node_modules from the earlier completion-report work; promoted to a real `dependencies` entry via `npm install --save`. New `renderCsv()` (kept the name to avoid touching the call sites) dynamically imports exceljs only when the user clicks Excel — keeps the initial JS bundle untouched — and builds: title band (rows 1–2, teal `#0F766E` fill with white 18pt Calibri "HealthSecure Portal" + 11pt subtitle "Report · Generated <IST timestamp>"), per-section content (paragraph rows use wrapped Calibri 10 in `#374151`; stats sections get a header row with teal fill + white bold text, then label/value/sub rows with thin bottom borders; table sections get the same teal header + zebra-striped body rows alternating `#F8FAFB`, with status-aware cell coloring — "success" goes green bold `#15803D`, "denied"/"failure" goes red bold `#B91C1C`, anything containing "anomaly" goes amber bold `#B45309`), and auto-fit column widths derived from the longest cell per column (capped at 60 chars). The worksheet is renamed to the report title (truncated to Excel's 31-char limit), the tab gets a teal color, and rows 1–4 are frozen. Buttons relabeled CSV → **Excel** on every card and inside the View modal; toast now reads "<title> · Excel downloaded". Filename: `access_report-2026-06-04.xlsx`. End-to-end: opening the file in Excel no longer surfaces the data-loss warning; the access report renders with the teal header, every column is properly sized so `mebila4127@dosbee.com` no longer overflows into the next cell, and the STATUS column shows green "success" pills inline.

2026-06-04 10:45 - [FIXED] - Auditor report PDFs now have proportional column widths (no more text spilling from ACTOR into ACTION) and every page carries a footer. Before: the previous `table()` helper in `app/auditor/reports/page.tsx › renderPdf` divided the page width evenly across columns (`tableW / colCount`) and only truncated cells whose JS string length exceeded 50 chars — so a 27-character email like `mebila4127@dosbee.com` fit the length check but its rendered pixel width still bled into the next column's text. Long actor emails visibly overlapped the action column. Pages with little/no body content also showed nothing below the table — no footer, no page number, just whitespace. Fix: (1) The `fitText(text, maxW)` helper measures actual rendered width via `doc.getTextWidth()` and binary-searches the longest prefix that fits, appending an ellipsis. Used everywhere a cell is drawn (table body, table header, stat values). (2) `table()` computes proportional column widths in a two-pass scan — first sums the longest cell width (capped) per column from the first 80 rows, then scales those ideal widths so they exactly sum to the available `tableW`, with a `minColW = 36pt` floor so a status column never collapses. (3) Page-break logic was unified — `ensureSpace()` now uses `contentBottom = pageHeight - footerHeight` and re-draws the teal report header on every new page so subsequent pages also have a title band. (4) After all content is rendered, a final pass over `doc.getNumberOfPages()` stamps the footer on each page: a thin divider line, "HealthSecure Portal · <Report title> · audit-logged" on the left, "Page X of Y" centred, and "Generated <timestamp>" on the right. End result: no more "ACTOR | ACTION" overlap (the email and action verb are now visually separated), and the page-2 white space the user pointed to is replaced by a real footer with page number + generation timestamp. Same `renderPdf` is shared by all 4 reports (compliance summary / access report / consent compliance / audit summary), so the fix applies to every download from `/auditor/reports`.

2026-06-04 10:30 - [ADDED+REPLACED] - Auditor `/auditor/reports` page is fully DB-driven. Before: `app/auditor/reports/page.tsx` rendered 4 hardcoded cards backed by `lib/report-generators.ts` which produced 100% mocked PDFs (fixed actor names like "priya.shah@citygeneral", fixed timestamps like "2026-05-18 12:04:18", fixed stats "12,489 audit / 4 anomalies / 1,284 consents"). The PDFs were correctly formatted but completely fictional — different tenants saw the same numbers. Every card had a static "Last run 4h ago" badge. No "View" option existed. New: real report engine. (1) `GET /api/auditor/reports/[key]` (Auditor + Compliance Manager gated, `isDbUid` short-circuit, tenant-scoped via the signed-in user's organizationId) returns a structured `{report: {key, title, description, generatedAt, generatedAtLabel (IST), windowStart, windowEnd, sections: ReportSection[]}}` payload. ReportSection is a discriminated union of `paragraph | stats | table` so the page can render any mix per report. The four report builders pull live data: compliance_summary counts active consents + 24h audit events + open anomalies, then lists every consent_policies row with computed adoption %; access_report UNIONs consent decisions / sessions / patient_documents into the newest 200 events with IST timestamps; consent_compliance returns active/revoked/expired stats + top-10 requesting clinicians; audit_summary returns event volume by category over 30 days. (2) Shared `lib/report-types.ts` defines the wire types (extracted out of the route since Next.js route.ts files only allow HTTP-method exports). (3) Page rewrite: `"use client"` + initial `Promise.all` fetch on mount to populate "Last run" timestamps from each report's `generatedAt`, replacing the static "4h ago". Every card now has **three** buttons: a primary **View** (opens a Dialog rendering sections + a max-50-row table preview + the generated-at label, with Refresh / CSV / PDF in the footer), and outline **PDF** / **CSV** that fetch the same payload and stream a real file. PDF rendering uses jsPDF with a teal header band, paragraph/stats/table sections, and page-overflow handling; CSV rendering produces a multi-section file with `# <heading>` separator comments. Filenames look like `compliance_summary-2026-06-04.pdf`. No mock data anywhere on the page; clicking View on a fresh tenant with no events shows "No data for this tenant yet." instead of the previous fake numbers. End-to-end verified as Arjun Singh: all 4 GETs return 200 with real sections — compliance_summary now reports 5 actual policies + their adoption split, access_report returns 80 real events with IST timestamps, audit_summary aggregates 5 categories over 30 days. Out of scope: `lib/report-generators.ts` is left intact (other surfaces still reference it for the legacy ReportsWidget) but is no longer wired to the auditor page; the compliance/reports page is a separate file and is untouched in this turn.

2026-06-04 09:45 - [FIXED] - Audit ledger sidebar count + audit-row times are dynamic, and the audit timestamp column now renders in IST regardless of where the server runs. Before: (1) `app/auditor/layout.tsx` + `app/compliance/layout.tsx` shipped the "Audit ledger 12.4k" + "Consents 1284" badges as hardcoded literals — the same value for every tenant; (2) `fmtTime()` in `app/api/compliance/audit-logs/route.ts` used `d.getHours()` / `getMinutes()` / `getSeconds()` which read the Node process timezone — on the UTC dev box every row showed as UTC, so an Indian auditor saw 09:30 instead of the 15:00 wall-clock they actually experienced; (3) `app/auditor/dashboard/page.tsx` had the same problem with "12,489 audit events visible · 1,284 consent records · 4 open anomalies" hardcoded plus a "City General · May 1 – May 18, 2026 · Jun 1, 2026" hero panel that was true for nobody. Fix (1+3): both layouts now `useEffect`-fetch `/api/compliance/audit-logs` (events24h → compact format like "62" / "12.5k" via a small `compactCount` helper) and `/api/compliance/consents` (active count for the Compliance sidebar's Consents badge); the auditor dashboard goes one further and fetches `/api/auditor/profile` for the hero panel's scope / MFA / last-IP / expiry plus stitches in stats from audit-logs + consents + anomalies for the 4 stat tiles. Hero title becomes "Auditor overview · <real org>" with "Your account expires automatically on <real date>"; the "Audit window" subtitle was dropped because there's no audit-window DB column to back it. Fix (2): `fmtTime()` switched to `d.toLocaleTimeString("en-GB", { hour12: false, timeZone: "Asia/Kolkata" })` so HH:MM:SS is always in IST. End-to-end verified as Arjun Singh (real Auditor): GET /api/compliance/audit-logs returns events24h: 62 + first 3 event times in IST (14:57:23 / 14:50:22 / 14:42:44) instead of the prior UTC-only digits. Sidebar badge now reads "62" instead of "12.4k". Out of scope: the hardcoded "Generated reports" tile on the auditor dashboard still shows "—" because there's no reports table yet.

2026-06-04 09:30 - [ADDED] - Auditor `/auditor/settings` page is now DB-backed. Before: `app/auditor/settings/page.tsx` was a server component with every field hardcoded — Name "Anand Verma", Organization "Independent Regulator", Scope "org_citygeneral", Account expires "Jun 1, 2026", "TOTP enrolled" badge always green, "IP allowlist · 203.0.113.0/24 (regulator office)". A real signed-in auditor saw someone else's name + a fake tenant slug + a fake expiry. New `GET /api/auditor/profile` (`app/api/auditor/profile/route.ts`, Auditor-only gate, `isDbUid` short-circuit returns a slim demo-friendly payload so the page never blanks): joins `users` to `organizations` for the auditor's name (firstName + lastName), email, organization name + slug (the tenant they audit), `mfaEnrolledAt` / `mfaRequired`, plus the latest non-revoked row from `sessions` for the IP allowlist hint and the current session's `expiresAt`. Account expiry is derived as `createdAt + 90 days` since the User table has no explicit user.expiresAt column — that gives the page a real moving date instead of a static "Jun 1, 2026". Page rewritten as `"use client"` + `useEffect`-fetch. Profile card now shows the auditor's actual Name / Organization / Scope / Account expires + a new Email row. Security card flips between "TOTP enrolled" + green Active and "TOTP not enrolled" + amber Pending based on `mfaEnrolledAt`; subtitle shows the IP from the most recent session when present, otherwise the tenant's MFA-required policy. Session timeout row appends "current session ends MMM D, h:mm a" when sessionExpiresAt is set. End-to-end verified as Arjun Singh (`mebila4127@dosbee.com`, the only Auditor in the DB): page now reads "Arjun Singh · city hospital · org_city_hospital · Sep 2, 2026 (createdAt + 90d) · MFA not enrolled · last session IP 117.97.169.13 · session ends Jun 4 9:20 PM".

2026-06-03 22:45 - [FIXED] - Patient `/patient/prescriptions` Download button now generates a real PDF instead of just toasting "Download started". Before: the button was an `ActionButton` with only `toastMessage` + `toastDescription` props — clicking it surfaced the toast and did nothing else. Fix: replaced with a plain `<Button onClick={() => downloadPrescriptionPdf(rx)}>` that calls a new `downloadPrescriptionPdf` helper inside `app/patient/prescriptions/page.tsx`. The helper uses jsPDF (already in the bundle for the patient data-export feature) to build a single-page A4 PDF with a teal header band ("HealthSecure Portal · Prescription record"), the drug + strength as the headline, route/frequency/duration as the subtitle, a two-column facts grid (Refills left, Status, Prescribed by, Department, Source, Prescribed on), the instructions block wrapped to fit, and a footer line ("Audit-logged · download recorded against the patient's access ledger.") plus the prescription id. Filename is `<drug>-<id8>.pdf`. Errors fall back to `toast.error("Could not generate PDF")`. No server roundtrip — generated from the same `rx` data the card already has.

2026-06-03 22:30 - [ADDED+REFACTOR] - Compliance can now toggle ANY policy version between active and archived from `/compliance/consent-policies`. Before: the only lifecycle button was "Activate" on a draft, and the PATCH route explicitly rejected `action: "activate"` against an archived row ("Cannot reactivate an archived policy"). The active card had Re-consent campaign + Edit draft but no way to deactivate it from the list page. Archived cards had only View text + Version history. Fix: PATCH `/api/compliance/consent-policies/[id]` `action: "activate"` now accepts the source row being either `draft` or `archived` — same atomic flow either way (archive whatever is currently active, then flip this row to active and clear archivedAt). The history JSONB gets a "re-activated" event when the source was archived so the timeline reads correctly. Two new buttons on the list card (`app/compliance/consent-policies/page.tsx`): **Deactivate** on the active card (POSTs `action: "archive"`) and **Re-activate** on archived cards (POSTs `action: "activate"`, gets a "Re-activate" label instead of "Activate"). Reused the existing `ActivateDraftButton` with an optional `label` prop for the archived-card variant. New `DeactivateButton` follows the same confirm + toast pattern. Refactor side-effect: `guardCompliance` was previously exported from `app/api/compliance/consent-policies/route.ts` — Next.js App Router complains in the generated `.next/types/...` that route.ts files may only export HTTP method handlers. Extracted to `lib/compliance-guard.ts` and updated the three consumers (consent-policies GET, consent-policies/[id] PATCH/GET, re-consent-campaigns POST) to import from there. End-to-end verified live: list shows v2.6 active + v2.5/v2.4/v2.3/v2.2 archived → Deactivate v2.6 → ALL versions become archived → Re-activate v2.5 → v2.5 becomes active, v2.6 stays archived → Re-activate v2.6 → v2.6 active again, v2.5 auto-archived. Audit ledger picks up each transition via the policy's history JSONB.

2026-06-03 22:00 - [ADDED] - Gold re-consent banner that surfaces on every patient's dashboard + consents page whenever Compliance dispatches a campaign for a new policy version. Before: `POST /api/compliance/re-consent-campaigns` only inserted a single `re_consent_campaigns` row + appended an event to the policy history — no patient-facing artifact, no approve/decline UI. New table `re_consent_responses` (campaignId / organizationId / patientId / policyId / policyVersion / response ("pending"|"approved"|"declined") / respondedAt / decisionNote, unique on (campaignId, patientId)). Schema mirror added as `ReConsentResponse` model. Campaign POST extended to enumerate recipient patient ids via UNION of three sources: (a) patients with approved non-expired consents decided before the policy activated (strict "still on prior version"), (b) patients with ANY consent_request in this tenant (catches patients whose 24-hour grants have lapsed — the strict query would miss them), (c) patients with an active patient_assignment to any clinician in this tenant (catches care-team patients who haven't booked or consented yet). Each recipient gets one pending row in `re_consent_responses` via `ON CONFLICT DO NOTHING` so the same campaign can be re-triggered idempotently. New `GET /api/patient/re-consent` returns all pending rows for the signed-in patient with their policy summary + sections (joined from `consent_policies`); `PATCH /api/patient/re-consent` flips response to approved or declined with respondedAt + optional note. New client component `components/patient/re-consent-banner.tsx` — gold gradient banner with the policy version pill, "Active since…" subtitle, summary, and three buttons: **View text** (opens a Dialog with all 4 sections), **Approve** (primary), **Decline** (ghost danger). Mounted on `/patient/dashboard` above the GreetingHero and on `/patient/consents` below the PageHeader. Click handlers optimistically drop the responded row from local state so the banner disappears the moment the API returns 200. End-to-end verified: Meera triggers a campaign for a freshly-activated v2.5 policy → 8 recipients inserted (covers both Aarav patients + priya via the assignment-based fallback) → priya hits `/api/patient/re-consent` → sees 1 pending row with the policy's summary + 4 sections → PATCH `{decision:"approved"}` → pending list returns 0 on next fetch. The audit ledger picks the campaign trigger up via the existing `consent_policies.history` JSONB append. Out of scope: actual SMTP / SMS dispatch (the row records the intent), and automatic migration of the patient's existing `consent_requests` rows to reference the new policy version (the response row is the source of truth for the patient's acceptance — consent_requests still derive their policyVersion from `decidedAt` against policy thresholds).

2026-06-03 21:30 - [FIXED] - Patient `/patient/appointments` no longer shows a ghost duplicate of the same appointment after the patient accepts a clinician-proposed reschedule. Root cause: `mergeAppointments(db, local)` keyed dedup by `(clinician, date, time)`. Pre-accept, the DB and local-store entries shared that tuple so the local one was dropped. Post-accept, the DB row's date+time moved to the proposed slot while the local-store entry kept the old values — so the dedup key no longer matched and BOTH cards showed: the new DB one as **Confirmed** at the new time, AND the stale local one as **Requested** at the old time. The user kept seeing "Reschedule / Cancel" buttons under what looked like the same appointment, so the previous optimistic-state fix appeared not to work. Fix: simplified `mergeAppointments` to `if (db.length > 0) return db; return local;` — whenever the patient has ANY DB-backed appointment, treat the API as the only source of truth and drop ALL local-store mirrors. Local-store appointments still surface for legacy demo users who never booked through the API (db.length === 0). This eliminates the ghost duplicates and lets the optimistic Accept / Decline state changes from the prior fix actually win the render.

2026-06-03 21:15 - [FIXED] - Patient appointment card: Accept new slot / Decline now optimistically updates the local state so the amber proposal banner + "Reschedule proposed" pill disappear instantly without waiting for the network refetch. Before: `onAcceptReschedule` just called `reload()` which bumped reloadKey → triggered the `useEffect` → made a fresh `/api/patient/appointments` fetch → 100–500 ms later the updated state finally arrived and the banner was hidden. During that window the user saw the same Accept/Decline buttons they just clicked. Fix: after the PATCH succeeds, the parent handler now: (a) writes `dbExtras[id] = { proposedStartsAt: null, proposedNote: null, startsAt: <accepted> }` via `setDbExtras` so `hasProposal` evaluates to false on the very next render; (b) maps `dbAppointments` to set `status: "confirmed"` (or `"requested"` for the decline path) with the new date/time, so the badge flips from "Reschedule proposed" to "Confirmed" the moment the API returns 200. The follow-up `reload()` still runs to reconcile against the server in case anything else changed concurrently. Net effect: tap Accept new slot → toast + banner gone + card pill flips to blue Confirmed in the same frame.

2026-06-03 21:00 - [FIXED+ADDED] - (1) Clinician Schedule Day view: per-row **Reschedule** button — clinician can now propose a new slot directly from the schedule table (not just from the patient chart). Each appointment row (status requested / upcoming / next / reschedule-requested) gets a small "Reschedule" link next to its status pill that opens the shared `RescheduleAppointmentDialog` in `clinician_propose` mode and PATCHes `/api/clinician/appointments/[id]` with `action: "request_reschedule"`. Schedule auto-refreshes (15s polling + new manual `refetchAppointments()` callback) so the row's pill flips to amber "Reschedule" without a reload. SlotRow extended with `appointmentId` + `startsAtIso` so the row knows its own DB id (the previous SlotRow was index-only). (2) Patient slot picker (`/api/patient/clinicians/[id]/slots`) now correctly hides slots that are already booked AND skips past times for today. Two bugs fixed in the same route: (a) the booked-appointments query filtered on `startsAt >= ${now}`, so any booking earlier today was excluded from the takenByDay dedup — and the same slot reappeared as available for the next patient. Changed lower bound to `startOfToday` so all of today's bookings are visible to the dedup. (b) Today's slot times that have already passed (e.g. 9:00 AM at 6 PM) still showed as bookable. New helper `slotLabelToMinutes()` parses the slot label back to minutes-since-midnight, and today's row filters out any slot whose start is `<= nowMinutes`. Combined effect: when a clinician already has Wed Jun 3 10:00 AM / 2:00 PM / 2:45 PM / 3:30 PM / 4:15 PM on their calendar, the next patient booking flow no longer offers any of those slots; if all of today's slots are past/booked the day tile is omitted entirely and the picker jumps to the next working day. Verified live as priya: the slots endpoint now returns Thursday Jun 4 as the earliest available day (Wed Jun 3 entirely hidden because every slot is either taken or in the past).

2026-06-03 20:30 - [FIXED+ADDED] - Patient `/patient/appointments` page now displays the real lifecycle state (Confirmed / Arrived / In progress / Completed / Reschedule proposed) instead of collapsing everything to "Confirmed", and the Reschedule / Cancel / Accept-reschedule actions are wired end-to-end through the new lifecycle API. Before: `app/patient/appointments/page.tsx › mapDbStatus()` mapped DB `arrived` and `in_progress` to UI `"confirmed"`, so the patient saw "Confirmed" while the clinician saw "Arrived" / "Completed" — visible status divergence for the same appointment. Reschedule + Cancel still hit the local-store via `usePatientStore()`, so neither reached the DB; Cancel happened to be silently fine via my earlier wiring on the page, but Reschedule used the old hardcoded-slot `RescheduleDialog` which only emitted a toast. There was also no way for the patient to accept or decline a clinician-proposed reschedule. Fix is in three layers. (1) `lib/patient-store.tsx › AppointmentStatus` widened with `"reschedule-requested" | "arrived" | "in-progress"`; `mapDbStatus()` in the page now passes those through verbatim; the upcoming-card badge renderer covers all 8 statuses (warning amber for Requested / Reschedule proposed / Arrived, success green for In progress / Completed, info blue for Confirmed, muted/danger for Cancelled / No-show). (2) New shared dialog `components/shared/reschedule-appointment-dialog.tsx` with a real date + time picker, optional note, future-only guard, and a `mode` prop that controls copy + button label ("Send proposal" vs "Request reschedule"); the dialog is mode-agnostic over the network call (caller supplies `onSubmit({startsAt, note})`). The patient page mounts the dialog with `mode="patient_request"` and the clinician chart's EncounterCard mounts it with `mode="clinician_propose"`. (3) Patient API at `app/api/patient/appointments/route.ts` gained two new PATCH actions on top of `accept_reschedule` and `cancel`: `decline_reschedule` (clears the proposed slot, flips status back to `requested`, audit-logs "Patient declined the proposed reschedule") and `patient_reschedule` (moves `startsAt` to the new slot, flips status to `requested` so the clinician re-confirms, cancels any in-flight reminders, audit-logged). The GET also now returns `proposedStartsAt` + `proposedNote` so the page can render the proposal banner. Clinician chart EncounterCard added a `Reschedule` button (visible while status is `requested` or `confirmed`) that opens the new dialog and PATCHes `{action:"request_reschedule", proposedStartsAt, proposedNote}`. The patient's upcoming card now shows an amber proposal banner with **Accept new slot** + **Decline** buttons when `status==="reschedule-requested"` (renders the proposed date + the clinician's note). Bonus fix: the `proposedStartsAt` and `appointment_reminders.scheduledAt` columns were originally created as `timestamptz` while the rest of the appointment schema uses `timestamp(3)` (tz-naive UTC) — the implicit column-copy on patient-accept then bled +5:30h. Resolved by `ALTER COLUMN ... TYPE timestamp(3) USING ... AT TIME ZONE 'UTC'` for all three columns, plus passing the new value as a `'YYYY-MM-DD HH:MM:SS.sss'::timestamp` string from the route (not a JS Date) so Prisma's Date→tz-string serialization can't double-convert under non-UTC dev TZs. End-to-end smoke as priya↔rahul: priya books `2026-06-05 3 PM` → row at `requested`, startsAt `15:00:00Z`. rahul PATCHes `request_reschedule` with proposed `2026-06-05T16:00:00.000Z` + note → row flips to `reschedule_requested`, proposedStartsAt persists as `16:00:00Z` (round-trips cleanly). priya PATCHes `accept_reschedule` → startsAt becomes `16:00:00Z` exactly, proposedStartsAt cleared, status `confirmed`, two reminder rows inserted at startsAt - 24h / startsAt - 1h. Each transition writes an audit_log row capturing previous status, new status, actor email + role, and reason. UI surfaces (patient list, clinician schedule, clinician dashboard) all reflect the new state within their respective refresh cadences (15s polling on the schedule).

2026-06-03 20:00 - [ADDED] - Full appointment lifecycle workflow with audit trail. Before: appointment status transitions were piecemeal — the clinician PATCH at `/api/clinician/appointments/[id]` flipped status directly via raw SQL, but new patient bookings landed straight at `confirmed` (skipping any clinician review), there was no `requested` / `reschedule_requested` enum value, no audit ledger of who-changed-what-when, no concept of a clinician-proposed reschedule slot, and no T-24h/T-1h reminder scheduling. Net effect: the patient dashboard tile showed local-store "Requested" badges but the same appointment in the DB-backed list showed "Confirmed", and there was no way for a clinician to confirm or reject a request — every booking was implicitly approved on creation. New design enforces a real lifecycle: `requested → confirmed → arrived → in_progress → completed`, with `reschedule_requested` as a branch (clinician proposes new slot → patient accepts → back to `confirmed`) and `cancelled` / `no_show` as terminal off-ramps. Schema additions: 2 new enum values added to `AppointmentStatus` via `ALTER TYPE` (`requested`, `reschedule_requested`) — total 9 states; new `appointment_audit_log` table (id / appointmentId / organizationId / previousStatus / newStatus / changedById / changedByEmail / changedByRole / reason / metadata JSONB / createdAt); new `appointment_reminders` table (id / appointmentId / organizationId / kind ("t_24h" | "t_1h") / scheduledAt / sentAt) with unique constraint on (appointmentId, kind) so re-confirming is idempotent; two new nullable columns on `appointments` (`proposedStartsAt`, `proposedNote`) to hold a reschedule offer until the patient accepts it. Matching Prisma models for `AppointmentAuditLog` + `AppointmentReminder` + the extended `Appointment` were added to `schema.prisma`. New library `lib/appointment-lifecycle.ts` is the single source of truth: exports `transitionStatus({appointmentId, organizationId, newStatus, actor, reason, metadata})` which validates the transition against a hardcoded adjacency map (`TRANSITIONS`), writes the new status via raw SQL `status::"AppointmentStatus"` cast, and inserts an audit row in the same call; `scheduleReminders()` and `cancelReminders()` idempotently INSERT/DELETE the T-24h + T-1h rows; `statusLabel()` maps enum values to UI strings; `writeAudit()` is a standalone variant used for the initial INSERT path where no transition exists yet. All raw SQL because the generated Prisma client can't be regenerated under the Windows DLL lock. API rewrites: (1) `POST /api/patient/appointments` now INSERTs with `status='requested'`, drops the old `prisma.appointment.create` in favor of raw SQL, and writes the initial audit row (`previousStatus=null, newStatus=requested, actor=Patient`). (2) `/api/patient/appointments` gains a PATCH supporting `action: "accept_reschedule"` (moves proposedStartsAt → startsAt, clears the proposal, transitions to confirmed, schedules reminders, audit) and `action: "cancel"` (transitions to cancelled, cancels reminders, audit). (3) `/api/clinician/appointments/[id]` PATCH rewritten on top of `transitionStatus`: any `status: "confirmed|arrived|in_progress|completed|no_show|cancelled"` flows through the helper (always writes audit + persists), with `confirmed` also calling `scheduleReminders` and `cancelled`/`no_show` calling `cancelReminders`; a new `action: "request_reschedule"` path saves `proposedStartsAt`+`proposedNote` and transitions to `reschedule_requested`. UI rewrites: `lib/clinician-store.tsx › AppointmentStatus` widened to include `reschedule-requested`. Chart status mapper (`app/clinician/patients/[id]/page.tsx:268`) translates DB `requested` → UI `"requested"`, DB `reschedule_requested` → UI `"reschedule-requested"`. EncounterCard surfaces a new "Confirm appointment" primary button when status is `requested` or `reschedule-requested`; the existing Mark arrived / Start consultation / Complete / No-show buttons stay disabled until the appointment is `confirmed` (so the clinician has to confirm before the lifecycle can begin); No-show is also disabled in the requested states. Schedule page (`app/clinician/schedule/page.tsx`) SlotRow widened with `requested` + `reschedule-requested`, mapping + amber-pill rendering added. Clinician dashboard `ApiAppt` type widened + matching Requested / Reschedule-requested pills added. Patient appointments page `mapDbStatus` now passes `requested` and `reschedule_requested` through (the latter also surfaces with the amber Requested pill — patient sees "Requested" while the proposal is outstanding). End-to-end verified via curl: priya books `2026-06-04 11:00 AM` → row lands as `requested` with first audit entry "Patient-initiated booking"; rahul PATCHes `{status:"confirmed"}` → row becomes `confirmed`, two reminder rows appear at `startsAt - 24h` and `startsAt - 1h`, audit row records `requested → confirmed by Clinician`; rahul walks `arrived → in_progress → completed` → each transition writes a fresh audit row with prev/new status, actor email + role, default reason "Clinician marked X"; final audit ledger for this one appointment shows 5 rows in chronological order. Out of scope: the actual outbound channel for the reminder rows (no SMTP cron in the dev stack); a proposed-reschedule date/time picker UI on the chart (the PATCH `action:"request_reschedule"` is wired and tested, but the EncounterCard doesn't yet open a date picker — for now reschedule has to be triggered programmatically); reports module hasn't been touched.

2026-06-03 19:30 - [FIXED] - Patient dashboard, clinician dashboard, and Org Admin appointments list were all returning HTTP 500 whenever any row in `appointments` carried the new `arrived` or `in_progress` enum value — the same Prisma-deserializer hazard that bit `/api/clinician/appointments` earlier. Net effect for the patient: `/api/patient/dashboard` 500 → empty header avatar (the avatar dropdown had no name/MRN to render), no "next appointment" tile, etc. Each call followed the same broken pattern — `prisma.appointment.findMany({ include: {clinician: ...} })` or `prisma.appointment.update({ include: {...} })`, which implicitly selects `status` and trips the deserializer because the generated client only knows the original 5 enum values (Windows DLL lock blocks `prisma generate`). Fix is the same pattern as the earlier `/api/clinician/appointments` GET — swap the offending Prisma call for `prisma.$queryRaw` that pulls `status::text AS status` and joins to `users` for the clinician name. Patches: (1) `/api/patient/dashboard/route.ts` — `upcoming` switched to raw SQL with embedded join + `dayStart` filter + `NOT IN ('cancelled','no_show')`; the response mapping now flattens `clinicianFirstName`/`clinicianLastName`/`clinicianDesignation`/`clinicianDepartment` instead of nested `clinician.{...}`; `past` count stays on prisma (no status read). (2) `/api/clinician/dashboard/route.ts` — `todayAppointments` switched to raw SQL; the `nextUp` filter compares against plain string literals (`"completed"`, `"cancelled"`, `"no_show"`, `"blocked"`) instead of the `AppointmentStatus.*` constants. (3) `/api/admin/appointments/route.ts` GET — replaced findMany with raw SQL + join, then maps each row through the existing `shape()` helper. (4) `/api/admin/appointments/[id]/route.ts` PATCH — `prisma.appointment.update` keeps writing (the incoming `status` is always one of the 5 known values), but the post-write read is now a raw `SELECT ... status::text AS status` join so the response can include the row's current enum value even when it's `arrived` / `in_progress`. End-to-end verified as priya Mehta: `GET /api/patient/dashboard` returns 200 with `next.status: "arrived"` (her 10:00 AM with Dr. rahul sharma), `upcomingCount: 4`, `pastCount: 2`, careTeam present, MRN computed. The avatar dropdown now renders her name + MRN correctly.

2026-06-03 19:00 - [FIXED] - Clinician Schedule + Dashboard now render the appointment's real lifecycle state (Arrived / In progress / No-show / Cancelled) instead of mashing everything that isn't `completed` into a static "Upcoming" pill, and the Day view auto-refreshes so a Mark arrived click on the patient chart propagates to the schedule without a manual reload. Before: `app/clinician/schedule/page.tsx` mapped `a.status` → its own narrow `"completed" | "next" | "upcoming" | "blocked" | "available"` set, with `no_show`/`cancelled` explicitly collapsed back to `"upcoming"` and `arrived`/`in_progress` (added by the recent enum extension) silently falling through to `"upcoming"` because they weren't in the enum union the page knew about — so a clinician could click Mark arrived → Start consultation → Complete on the chart and the schedule's status column would still read "Upcoming" until they completed the appointment. Same gap on `app/clinician/dashboard/page.tsx`, whose ApiAppt type still listed only the original 5 enum values. Fix: extended SlotRow status to `"arrived" | "in-progress" | "no-show" | "cancelled"` in addition to the existing values; mapping turns DB `arrived` → SlotRow `"arrived"`, DB `in_progress` → `"in-progress"`, DB `no_show` → `"no-show"`, DB `cancelled` → `"cancelled"`; badge rendering adds matching pills — warning amber "Arrived" / info blue "In progress" / danger red "No-show" / muted gray "Cancelled" — alongside the existing Completed/Upcoming/Blocked/Available. Dashboard ApiAppt type widened to include `arrived` + `in_progress` and matching Arrived/In-progress badges. Added a 15-second polling loop + `window.focus` refetch to the schedule page's `useEffect` so any status change (anyone in the tenant marking arrived, starting consult, completing, no-show) shows up on the schedule within 15 seconds without requiring a refresh. End-to-end verified: signed in as Dr. rahul sharma, `GET /api/clinician/appointments?from=2026-06-03&to=2026-06-03` returns lifecycle states verbatim — priya Mehta 10:00 AM `arrived`, Aarav Mehta 2:00 PM `completed`, priya Mehta 2:45/3:30/4:15 PM `confirmed`. The Day view tab now shows amber Arrived / green Completed / gray Upcoming pills correctly per status. Out of scope: clinician dashboard "Up next" tile + patient appointments page still treat arrived/in_progress as "confirmed" — those are informational on the patient side and the dashboard's tile renders its own simple "Scheduled" pill that doesn't need the new states yet.

2026-06-03 18:30 - [FIXED] - `GET /api/clinician/appointments` was returning HTTP 500 after the earlier `AppointmentStatus` enum extension (added `arrived` + `in_progress`). Root cause: the generated Prisma client still maps the enum to its old 5-value set (Windows DLL lock blocks `prisma generate`), so `prisma.appointment.findMany({ select: { status: true } })` threw `Inconsistent column data` when any row had `arrived` or `in_progress`. Net effect: as soon as a clinician hit "Mark arrived" on any patient chart, the next load of `/clinician/schedule` (and any other surface that listed appointments) crashed — exactly the "not working" the user saw. Fix: rewrote the GET handler to use `prisma.$queryRaw` with `status::text AS status` (mirrors the same trick we already use in `/api/clinician/appointments/[id]` PATCH). The conditional `from`/`to` date filters are stitched in via `Prisma.sql` / `Prisma.empty` fragments. `create` + `findFirst` calls in the same route are unchanged because they either don't select status or only write values the client already knows (`confirmed`). End-to-end verified: signed in as Dr. rahul sharma, `GET /api/clinician/appointments?from=2026-06-03&to=2026-06-03` returns 5 rows including the `arrived` one (Aarav Mehta · 2:00 PM · back pain). The schedule page rehydrates correctly, and the Mark arrived → Start consultation → Complete lifecycle remains visible after the click.

2026-06-03 18:00 - [ADDED] - Patient `/patient/appointments` page now hydrates from the DB so DB-resident bookings (incl. clinician-created ones) show up alongside any legacy local-store entries — and so the patient finally sees the real UUID for each appointment instead of the local-store `apt-xxxxxxxx` slug. Before: the page imported `usePatientStore()` and rendered `state.appointments` only; even though `POST /api/patient/appointments` already wrote a real `appointments` row at booking time, the patient list never re-fetched it, so the patient kept seeing the local-store mirror (slug id, slug status) while the clinician's schedule (which always reads DB) showed those same bookings under their real UUIDs — visually divergent for the same data. New `GET /api/patient/appointments` (`app/api/patient/appointments/route.ts`): Patient-only gate, `isDbUid` short-circuit for demo sessions, joins `appointments` to `users` for the clinician's name + department, returns `{id, clinicianId, clinicianName, clinicianDepartment, startsAt (ISO), date (YYYY-MM-DD local), time ("3:30 PM"), durationMinutes, room, notes, status, mode}` newest first. Match key is `patientEmail` since that's what the booking POST stamps; falls back to empty list when the user row is missing. Page update (`app/patient/appointments/page.tsx`): `useEffect`-fetches the API on mount, maps each DB row into the local-store `Appointment` shape (status enum collapsed — `arrived`/`in_progress`/`confirmed`/`blocked` all read as `"confirmed"` for the patient view, `completed`/`cancelled`/`no_show` map straight through), and merges with `state.appointments` via a `(clinicianName.toLowerCase(), date, time)` signature with DB rows winning (drops the legacy local-store duplicates that had slug ids). Both Upcoming and Past tabs now compute against the merged list. End-to-end verified as priya Mehta (yaxoce2732@hitzcart.com): GET returns 4 rows — the same 4 the patient previously saw in the UI but now with real UUIDs (54651c8e, 2030e4c2, 3490fdbd, b51ef425), matching exactly what the clinician sees on `/api/clinician/appointments`. Out of scope: cancel/reschedule actions still update the local store only — those need DB-backed PATCH/DELETE wiring in a follow-up.

2026-06-03 17:30 - [FIXED] - Clinician patient-chart EncounterCard buttons (Mark arrived → Start consultation → Complete) now actually advance the appointment through its lifecycle. Before: the AppointmentStatus enum only contained `confirmed | no_show | blocked | cancelled | completed` — there was no `arrived` or `in_progress`. The PATCH route at `app/api/clinician/appointments/[id]/route.ts` treated those two inputs as "UI-only" (returned `persisted: false`, no DB write); the chart page only refetched (`reloadAppts`) when `persisted` was true; the chart's status mapper collapsed anything that wasn't completed/cancelled/no_show into `"confirmed"`. Net effect: clicking Mark arrived appeared to do nothing — the toast fired, but on the next render `appt.status` was still `"confirmed"` so Mark arrived stayed enabled and Start consultation stayed disabled. Lifecycle was stuck. Fix: extended the Postgres enum (`ALTER TYPE "AppointmentStatus" ADD VALUE 'arrived'` + same for `in_progress`) so both values are first-class on the DB side; mirrored on `schema.prisma` so future regenerations stay in sync. PATCH route rewritten to (a) accept both dash-form ("in-progress") and underscore-form ("in_progress") from the client and normalize to the DB underscore form; (b) drop the `PERSISTED_STATUSES`/`UI-only` distinction — every accepted status now writes to the DB; (c) use raw SQL (`prisma.$executeRaw ... status = ${nextStatus}::"AppointmentStatus"`) for the UPDATE because the generated Prisma client doesn't yet know about the new enum values (Windows DLL lock prevents `prisma generate`); (d) also switch the ownership check + post-update read to raw SQL for the same reason. Chart page mapping (`app/clinician/patients/[id]/page.tsx:268`) now turns DB `arrived` → UI `"arrived"` and DB `in_progress` → UI `"in-progress"`, so the EncounterCard's per-button `disabled` and `variant` props evaluate correctly after a reload. The existing `if (data.persisted) await reloadAppts()` line now always fires for these transitions (no behaviour change needed there). End-to-end verified via curl signed in as Dr. rahul sharma: PATCH `{status:"arrived"}` → `{ok:true, persisted:true, appointment:{status:"arrived"}}`, then `{status:"in-progress"}` → `status:"in_progress"`, then `{status:"completed"}` → `status:"completed"` — DB row confirms the final state stuck. The "No-show" button already worked (mapped to `no_show` previously); the lifecycle is now: `confirmed → arrived → in_progress → completed` with `no_show` and `cancelled` as terminal off-ramps. UI unchanged — only the data layer.

2026-06-03 17:00 - [ADDED] - Compliance Manager → "Versioned policy text" page (`/compliance/consent-policies`) is fully DB-backed and all three card buttons (View text / Edit draft / Re-consent campaign) plus "Create New Policy" now hit real endpoints. Before: `app/compliance/consent-policies/page.tsx` was a server component importing a hardcoded `POLICIES` array from `consent-policies-data.ts` (3 versions baked in: v2.4 active 1,284 consents 94% adoption / v2.3 archived 5% / v2.2 archived 1%); `[version]/page.tsx` and `[version]/edit/page.tsx` ran `generateStaticParams()` against the same fixture and rendered notFound for any other slug; `policy-form.tsx` submit was a single `toast.success()` + `router.push()` — nothing persisted; `Re-consent campaign` was an `ActionButton` with a confirm dialog that only showed a toast. New tables (raw-SQL created + matching Prisma models added to `schema.prisma`): `consent_policies` (id, organizationId, version, slug, status, summary, sections JSONB, scopeCategories, roleBindings, effectiveDate, draftedAt, activatedAt, archivedAt, createdById, history JSONB, unique(organizationId,version) + unique(organizationId,slug)) and `re_consent_campaigns` (id, organizationId, policyId, policyVersion, recipientCount, status, triggeredById, triggeredAt, completedAt). Seeded the 3 default policies for city hospital so the page renders the same as before out of the box. New `GET/POST /api/compliance/consent-policies` + `GET/PATCH /api/compliance/consent-policies/[id]` (`isDbUid` short-circuit + Compliance Manager + Auditor gate + tenant scoping, mirroring audit-logs/anomalies/consents). GET list returns each policy with `adoption` and `consents` derived live from `consent_requests` (count rows where `decidedAt` falls inside the policy's `[activatedAt, archivedAt)` window AND status='approved' AND not expired; adoption = that count / total active across all versions). POST creates a draft with 4 required sections + summary. PATCH supports three `action`s — `save_draft` (update text/scope/role bindings on draft or active), `activate` (atomic: archive the current active row, then mark this one active with timestamps + history events), `archive` (active → archived with history event). Single-policy GET accepts either UUID or slug. New `POST /api/compliance/re-consent-campaigns` inserts a `re_consent_campaigns` row, counts patients whose latest approved consent predates the active policy's `activatedAt` as the `recipientCount`, and appends a "Re-consent campaign dispatched to N patients" entry to the policy's history. Page rewrites: list (`page.tsx`) is now `"use client"` + `useEffect`-fetch; the same PageHeader / version-pill / Active/Archived/Draft badge / Adoption% bar / "X categories · N role bindings" 3-col grid layout preserved; the `ActionButton` confirm was replaced with a real `Dialog` that POSTs to `/api/compliance/re-consent-campaigns`. Detail page (`[version]/page.tsx`) is a client component that fetches by slug, dropping `generateStaticParams()` since policies are tenant-scoped; the Reviewer-decision-style "policy not found" empty state replaces `notFound()` for missing slugs. Edit page (`[version]/edit/page.tsx`) fetches by slug then mounts the new client-side `PolicyForm` (now keeps every field in React state and PATCHes `action: 'save_draft'` on submit; POSTs to the create endpoint when no policy is supplied). Drafts get a new yellow "Draft" pill on both list and detail; draft cards expose an "Activate" button next to View text/Edit draft that flips the row to active and archives the prior one. Deleted the stale `consent-policies-data.ts` fixture file. End-to-end verified via curl as Meera Singh in city hospital tenant: list returns 3 policies (v2.4 active 100% 2 consents — Aarav's new grant + 1 surviving old one — / v2.3 archived 0% / v2.2 archived 0%); detail GET by slug `v2-4` returns 4 sections + 3 history entries; POST a new `v2.5-test` draft returns 201 with the id and the list flips to show it at top with the Draft pill; PATCH `save_draft` persists summary + section edits; POST `/re-consent-campaigns` returns `{recipientCount: 0, id: ...}` (correctly 0 because no patient's consent predates v2.4's May 1 activation) and the policy's history grows by one entry. Out of scope: bulk outbound mailer (campaign row exists for audit; actual emails not sent), patient grant page still hardcodes `"v2.4"` (could be wired to fetch the tenant's active policy version in a follow-up).

2026-06-03 16:00 - [ADDED] - Patient-initiated consent grants and revokes now persist to the `consent_requests` table so they surface on the Compliance Manager / Auditor feeds. Before: `app/patient/consents/grant/page.tsx › submit()` only called `patientStore.addConsent()` which inserts a row into `lib/patient-store.tsx` (client-side, localStorage-backed) with a custom id like `con-hc7k1jwsan` — meaning a patient could grant Dr. rahul sharma access to all 6 scopes but the row never reached the DB, so `/compliance/consents` (which reads `consent_requests`) couldn't see it. Same gap for the Revoke button on `/patient/consents`. New `POST/DELETE /api/patient/consents` (`app/api/patient/consents/route.ts`, Patient-only gate, `isDbUid` short-circuit, tenant-fallback to clinician's org for self-registered patients): POST validates clinicianId is a UUID belonging to a `roleKind='clinician'` user, requires ≥1 scope, accepts an optional `durationHours` (clamped to [1, 24*365*5], null = open-ended), then INSERTs a `consent_requests` row with status='approved' / requestedAt=decidedAt=NOW / expiresAt computed from durationHours / reason='Patient-initiated grant' and returns the new id. DELETE flips status to 'declined' with decisionNote='Revoked by patient' — preserves the original `decidedAt` so the audit trail keeps the approval timestamp; the compliance feed maps declined→Revoked. Grant page rewired: `submit()` now `await fetch("/api/patient/consents", {method:"POST", body: {clinicianId: selected.id, scopes, durationHours: null, policyVersion}})` BEFORE the local-store mirror. The local store call is kept so the patient's own UI updates instantly even on network failure; a toast surfaces the server error if the API rejects. Revoke handler on `/patient/consents` similarly fires `DELETE /api/patient/consents?id=...` only when the consent id is a real UUID (skips for legacy local-store ids like `con-hc7k1jwsan` that don't exist server-side). End-to-end verified via curl: Aarav Mehta signs in → POSTs the 6-scope grant for Dr. rahul sharma → row lands in `consent_requests` with status='approved' / scopes=["insurance","id_proof","lab","imaging","prescriptions","other"] / expiresAt=null; Meera Singh (Compliance Manager in same tenant) refreshes `/compliance/consents` → stats now read active=2 (was 1) / total=6 (was 5); Aarav's row shows as `Aarav Mehta → Dr. rahul sharma · Insurance,Id_proof,Lab,Imaging,Prescriptions,Other · active · 3 Jun`. Out of scope: `/compliance/consents/[id]` detail re-export still reads the patient-store; needs its own DB-backed rewrite in a follow-up.

2026-06-03 15:30 - [ADDED] - Compliance Manager → "Organization-wide consent oversight" page (`/compliance/consents`) is now DB-backed. Before: `app/compliance/consents/page.tsx` rendered a hardcoded array of 5 fixtures (`cns_8a90c` Aarav Mehta → Dr. Priya Shah · Lab + Rx + Notes · v2.4 · Active · May 10, etc.) and four hardcoded stat tiles (Active 1,284 · Revoked 312 · Expired 68 · On stale policy (v2.3) 256), so the page was identical regardless of tenant or actual consent activity. New `GET /api/compliance/consents` (`app/api/compliance/consents/route.ts`, Compliance Manager + Auditor gated, tenant-scoped via signed-in user's `organizationId`, `isDbUid` short-circuit like audit-logs/anomalies): SELECTs from `consent_requests` joined to `users` for patient + clinician names/emails, filtered to `status IN ('approved','declined','expired')` (pending requests live on the Approvals page, not here), ordered by `COALESCE(decidedAt, requestedAt) DESC`, capped at 500 rows. Status mapping is the meaningful part — DB `approved` + `expiresAt > NOW` → "active"; DB `approved` + `expiresAt <= NOW` → "expired" (consent silently timed out, same outcome as a cleanup job marking it `expired`); DB `expired` → "expired"; DB `declined` → "revoked". Policy version is derived from the decision date against three rolling thresholds (≥2026-04-01 → v2.4, ≥2025-10-01 → v2.3, ≥2025-01-01 → v2.2) so the "On stale policy" stat tile actually counts something real until a per-row `policyVersion` column lands. Response shape: `{consents: [{id, patient, clinician, scope (joined string), scopes (string[]), status, version, date, requestedAt, decidedAt, expiresAt, reason}], stats: {active, revoked, expired, onStalePolicy, stalePolicyLabel}, policyVersions: []}`. Page rewrite: existing PageHeader / Filter / Export / 4 stat tiles / search box / 12-col table / status & policy filter dropdowns / CSV export are all preserved — only the data source changed. The stat tiles now show real counts; the "On stale policy" label adapts to whatever the current version is ("pre-v2.4" today). Empty state replaces the generic "No consents match the current filters" with "No consents recorded in this tenant yet — rows appear here once patients approve or decline clinician access requests" when the tenant genuinely has zero rows. End-to-end: city hospital tenant has 5 real consent_request rows (4 approved by Yaxoce → Rahul Sharma, 1 declined); Meera Singh's page now renders those 5 rows with correct status mapping (the 4 approved with expiresAt in the past correctly show as "expired"; the 1 declined shows as "revoked"). Out of scope: the detail page (`/compliance/consents/[id]` is still a re-export of the patient detail page which reads from the client-side `patient-store`) — needs its own DB-backed rewrite in a follow-up; clicking a real DB id falls through to the patient detail page's notFound() until then.

2026-06-03 15:00 - [ADDED] - Next.js OTP login flow now mirrors every challenge into the `mfa_challenges` table so the Compliance Manager → Anomalies "Failed MFA" detector fires from the live UI. Before: `app/api/auth/login`, `/api/auth/verify-otp`, `/api/auth/resend-otp` were cookie/in-memory only (the OTP hash + attempts counter lived inside the `hs_pending` JWT cookie); `mfa_challenges` was only written by the separate NestJS backend, which the Next.js app doesn't hit. Net effect: a staff user could fail OTP 5x in the live web app and the detector at `/api/compliance/anomalies` (which reads `mfa_challenges`) would still show zero — which is why the previous testing required a manual SQL insert. New helper `lib/mfa-challenge-store.ts` exposes `recordChallengeIssued / recordChallengeAttempt / recordChallengeExhausted / recordChallengeUsed` — each wraps a `prisma.$executeRaw` and short-circuits via `isDbUid(uid)` so demo users never touch the table. Wiring: login route inserts a new row (revoking any prior unused row for that user first) right after the OTP is generated, recording IP from `x-forwarded-for` and UA from the request headers; resend-otp does the same; verify-otp decrements `attemptsLeft` on every wrong code, force-clamps to 0 on the cookie-side exhaustion path, and stamps `usedAt = NOW()` on success (without touching attemptsLeft, so the detector can tell exhausted-failures apart from successful logins). Detector tightened: `app/api/compliance/anomalies/route.ts:178` and `app/api/compliance/audit-logs/route.ts:305` now both require `mc."usedAt" IS NULL` alongside `mc."attemptsLeft" = 0`, so a successful login is never misread as a failure. Auth itself stays resilient — every helper call is wrapped in a try/catch with `console.error`, so a Postgres hiccup on the audit mirror can't break the login flow itself. Also: the audit-logs route's `mfa_challenges` subquery was already using `mc."createdAt"` (a column that doesn't exist on this model — only `issuedAt` does), so the silently-failing query is now corrected to `mc."issuedAt" AS "createdAt"`. End-to-end: sign in as a real DB-backed staff user with `users.mfaRequired = TRUE`, fail OTP 5x → a `Repeated failed MFA` Medium card appears for that user on `/compliance/anomalies` (escalates to High at ≥3 exhausted challenges in 7 days); enter the correct OTP → no false-positive card, because the row has `usedAt` set. Demo users (non-UUID uids from `lib/demo-users.ts`) sign in normally with zero DB writes.

2026-06-03 14:00 - [ADDED] - Compliance Manager → Anomalies module is fully DB-driven. Before: `app/compliance/anomalies/anomalies-data.ts` exported two hardcoded arrays (`OPEN_ANOMALIES` with 4 fixtures: bulk-document-download / repeated-failed-mfa / off-hours-phi-access / consent-revoked-access; `CLOSED_ANOMALIES` with 2 fixtures) consumed by the list page, the detail page, and the compliance dashboard widget; the Resolve dialog only toasted and did nothing. New `anomaly_decisions` table (db-pushed via raw SQL, also added as `AnomalyDecision` model on the Prisma schema): `id / organizationId / signature (unique per org) / status (open|investigating|resolved|dismissed) / decidedById / decidedByEmail / outcome (legitimate|policy_update|incident) / coordinatedWith / justification / decidedAt / created+updatedAt`. New `GET/PATCH /api/compliance/anomalies` (`app/api/compliance/anomalies/route.ts`, Compliance Manager + Auditor gated, tenant-scoped via signed-in user's organizationId, `isDbUid` short-circuit): GET runs 11 detector rules across the real source tables we own — bulkUpload (≥4 `patient_documents` from same uploader in any 10-min window → High; ≥12 → Critical), failedMFA (`mfa_challenges.attemptsLeft = 0` in last 7d, grouped per user/day → Medium; ≥3 → High), offHoursAccess (`sessions.issuedAt` hour ∈ [22,5) for clinician/staff/org_admin roles → Medium), lockedAccount (`users.lockedUntil ≥ NOW() - 7d` → Medium; ≥8 fails → High), bruteForce (`users.failedLoginCount ≥ 5` not currently locked → High), infectedUpload (`patient_documents.scanStatus = 'infected'` → Critical), consentRevokedAccess (`consent_requests.status = 'declined'` in last 7d → Medium), unassignedPatient (consent_requests with no active patient_assignment between clinician and patient → Medium), excessiveViews (≥10 consent_requests per clinician in 24h → Low; ≥25 → High), breakGlass (consent_requests.reason containing "emergency"/"break-glass"/"urgent" → High), unusualIp (sessions from a new IP for an account that has prior sessions from other IPs → Medium). Each detected item carries a stable `signature` (e.g. `bulkUpload:<uploaderId>:<10minBucket>` / `failedMFA:<userId>:<dayBucket>` / `unusualIp:<userId>:<ip>`) so the reviewer's decision in `anomaly_decisions` overlays back onto the right item on every recompute. PATCH upserts the decision by signature, requires `justification` to close (resolved/dismissed), stamps decidedAt/decidedById/decidedByEmail when isClosed. Page rewrite (`app/compliance/anomalies/page.tsx` → 'use client'): existing PageHeader / red-banner cards / Investigate+View-events+Resolve buttons / Tabs (Open/Closed) preserved; added 4 small stat tiles (Open / Investigating / Critical / High), a search box (matches title/description/summary/actor/IP/type), severity filter dropdown (All/Critical/High/Medium/Low), type filter dropdown (11 detector types), pagination at 8 per page; severity-card backgrounds extended to 4 tones (critical has slightly darker red than high; low is neutral). Detail page (`app/compliance/anomalies/[id]/page.tsx`) rewritten as a client component that fetches the same endpoint, finds the matching signature, and shows the same hero banner / Actor·IP·Window facts grid / Evidence list / Recommended action panel; if the anomaly no longer matches (resolved or aged out) it shows a friendly "no longer matches" empty state instead of `notFound()`. New "Reviewer decision" card on the detail page surfaces the persisted decision (outcome / decided-by / coordinated-with / decided-at / justification italic block) when present. ResolveAnomalyButton (`app/compliance/anomalies/resolve-anomaly.tsx`) now actually PATCHes the API: existing 3 outcomes (Investigated — legitimate → dismissed, Tune detection rule → resolved, Escalate — open incident → resolved) map to a status; added two quick-action buttons in the footer ("Mark investigating" — status change only, no justification required; "Dismiss" — same as legitimate). Compliance dashboard widget (`app/compliance/dashboard/page.tsx`): the in-file `AnomalyFeed` function (which mapped `OPEN_ANOMALIES`) was removed and replaced with the new `app/compliance/dashboard/anomaly-feed.tsx` client component that fetches the same endpoint, filters to open/investigating only, takes the top 6, and renders the same row layout (icon + title + severity badge + summary + Investigate link). The fixture file `anomalies-data.ts` was deleted. End-to-end: a clinician hitting 4+ document uploads in 10 minutes shows up immediately in Open with a "Bulk document burst" card; clicking Investigate goes to the detail page; clicking Resolve → fills justification → submits → the row moves to Closed with the reviewer's name/date/justification; the same signature won't be re-flagged as Open until the decision is reverted or aged out of the lookback window.

2026-06-03 13:20 - [MODIFIED] - `POST /api/patient/documents` no longer 400s with "No tenant on your account" for self-registered patients. Before: the route required `users.organizationId` to be non-null before INSERTing into `patient_documents` (the table's `organizationId` is NOT NULL). Self-registered patients like Aarav land with `organizationId = NULL` and only acquire a tenant relationship through `PatientAssignment` to a clinician — so the upload modal toasted `Could not save: <file> · No tenant on your account.` even when the patient HAD an assigned clinician at a real tenant. Fix: same fallback pattern as the patient-profile route — when `users.organizationId` is null, query `patient_assignments` joined to clinicians for the most recent active assignment and use `clinician.organizationId` as the row's tenant. Final 400 only fires now when the patient is genuinely unassigned, with a clearer message ("ask your clinic to assign a clinician to you first").

2026-06-03 13:00 - [ADDED] - Patient document upload is now a real network call that persists to Postgres. Before: scanned files were saved into `lib/patient-store` only (localStorage), so DevTools → Network showed nothing on Upload and the row vanished if the user cleared site data. New `GET/POST/DELETE /api/patient/documents` (`app/api/patient/documents/route.ts`, raw SQL): POST validates name (≤240 chars) / category (Insurance | ID Proof | Lab Report | Imaging | Prescription | Other) / sizeBytes (≤25 MB) / scanStatus (must NOT be `infected` — infected files are rejected at the API too) / dataUrl prefix (`data:` only), resolves the patient's `organizationId`, INSERTs into `patient_documents` and returns the new row; GET lists non-deleted rows (≤200, newest first); DELETE soft-deletes by id with `deletedAt = NOW()`. Upload page rewrite (`app/patient/documents/upload/page.tsx`): after the existing client-side scan passes, `startUpload` now `await fetch("/api/patient/documents", {method: "POST", ...})` for each clean file BEFORE mirroring into the local store. POST failures get their own toast (`"Could not save: <file>" · <server error>`) and a `netFailed` counter that surfaces in the success toast (e.g. "5 files uploaded · 1 blocked · 1 failed to save · 5 clean · audit-logged"). When every file is rejected/failed the page stays put so the user can pick others. The Postgres row already feeds the compliance audit ledger (`documents.upload` / `documents.upload_infected` event), so each upload now also shows up immediately on `/compliance/audit-logs` under the patient's actor email.

2026-06-03 12:30 - [ADDED] - Patient document upload now runs a real client-side scan instead of always flagging files "Clean". Before: `lib/patient-store.tsx › addDocument` inserted every upload with `scanStatus: "pending_scan"` and a 1500ms `setTimeout` that flipped the row to `"clean"` regardless of what was actually in the file — uploading an .exe renamed `report.pdf` would still show the green "Clean" badge. New `lib/document-scanner.ts › scanFile(file)` inspects up to the first 256 KB of bytes and returns `{status: "clean"|"infected", reason?}` based on four checks: (1) extension is in the dangerous list (.exe / .bat / .ps1 / .vbs / .scr / .msi / .jar / etc. — refused outright), (2) body contains the canonical EICAR antivirus test signature, (3) magic-byte verification against the declared extension (rejects PDFs that don't start with `%PDF-`, PNGs without the PNG magic, etc., catches `.jpg` files that are really `4d5a` Windows EXEs or ELF binaries), (4) PDFs containing suspicious tags `/JavaScript`, `/JS`, `/OpenAction`, `/Launch`, or `/EmbeddedFile` (the patterns ClamAV's Pdf.Exploit.* family flags). Upload flow rewrite (`app/patient/documents/upload/page.tsx`): per-file `await scanFile(f)` BEFORE persisting; infected files never enter the vault, the user gets `toast.error("Blocked: <name>", { description: <reason> })`; clean files are added with `scanStatus: "clean"` explicitly. Counter toast at the end ("3 files uploaded · 1 blocked · 3 clean") so multi-file uploads with mixed results are transparent. Store changes: `addDocument` signature now accepts an optional `scanStatus` (defaulted to `pending_scan`), the auto-promote-after-1500ms timer is gone — the upload caller is responsible for setting the verdict from the real scan. Documents page's existing `<ScanStatus>` component already renders the three states correctly, so no UI changes there.

2026-06-03 02:15 - [ADDED] - Compliance Manager → Audit ledger is now DB-derived instead of a synthetic 200-event loop. Before: `app/compliance/audit-logs/page.tsx` had a `BASE[]` array of 10 fixture rows (priya.shah/records.read, riya.mehta/consent.revoke, auditor@regulator/auth.mfa_failure, etc.) that got cycled with shifting timestamps to fake 200 events; the four top-of-page stat tiles (Events / 24h: 12,489 · Denied access: 38 · Failures: 14 · Anomalies flagged: 4) were hardcoded literals regardless of tenant activity. New: `GET /api/compliance/audit-logs` (Compliance Manager + Auditor gated, tenant-scoped via the signed-in user's `organizationId`) UNIONs nine state-change source tables at read-time into one chronological event stream — `consent_requests` (request + decision, decided=denied → status=denied), `appointments` (create), `prescriptions` (draft + finalize), `medical_records` (draft + finalize), `messages` (send), `patient_documents` (upload, infected → status=failure + anomaly flag), `mfa_challenges` (exhausted → status=failure + anomaly), `sessions` (issuedAt + ipAddress → auth.session.create), and `users.lockedUntil` (account_locked → status=denied + anomaly). Each row is shaped to the audit envelope `{ts, actor email, role, action verb, resource, ip, status, flag, sessionId}` and the response also returns `stats` computed from the last 24h (events24h / denied / failures / anomalies). Each subquery is wrapped in try/catch so one schema mismatch on a side table never empties the whole feed — the rest still surface. Page rewritten as a client component that fetches once on mount, drops the synthesized `EVENTS` constant and `TOTAL=200` loop, keeps the existing filter/search/pagination UI (action verb prefix dropdown updated to the real verbs: `consent.* / appointment.* / prescription.* / note.* / message.* / documents.* / auth.*`), and renders a meaningful empty state ("No audit events in this tenant yet — events appear here as consent decisions, appointments, prescriptions, messages, and uploads land in the system") when nothing's happened. The IP column is real for `auth.session.create` events (from `sessions.ipAddress`, which the 00:05 fix already resolves via ipify in dev) and "—" for sources that don't carry IP yet.

2026-06-03 01:45 - [MODIFIED] - Header `user` dropdown for Compliance / Auditor / Super Admin layouts is now sourced from the live session instead of hardcoded literals. Before: all three role layouts passed a fixed object to `<RoleHeader user={...}>` — "Sai Compliance · compliance@citygeneral.health · Compliance Manager · City General" (compliance), "Anand Verma · External Auditor · regulator.gov" (auditor), "Ayushi Diyora · Super Admin · Sensussoft" (super) — regardless of who was actually signed in, so a real signed-in compliance manager would see someone else's name and email in the top-right avatar dropdown. Fix: each layout now `useEffect`-fetches `/api/auth/me` on mount, shapes the response (already existing) into `{name, subtitle: role + " · " + org, initials, email}`, and falls back to `Loading… / ··` while the fetch is in flight. The clinician and admin layouts already used this pattern — this change just brings the remaining three roles into line. No new endpoint required; `/api/auth/me` was already returning everything needed.

2026-06-03 01:30 - [MODIFIED] - `POST /api/super/incidents` now accepts demo Super Admin sessions instead of rejecting them with 400. Before: a demo Super Admin (the seeded `u_super_ayushi` user from `lib/demo-users.ts` whose `uid` is a non-UUID slug) opening the New-incident dialog and clicking submit got `400 Bad Request "Demo session — requires a real account."` because the route's `isDbUid` short-circuit ran before the insert. Fix: dropped the guard; `Incident.openedBy` made nullable on the schema + db-pushed; the INSERT now uses a `Prisma.sql` fragment that resolves to `${g.claims.uid}::uuid` for real users and `NULL` for demo users. GET's `LEFT JOIN users` already handled the null path — the `context` shape falls back to "Manual" when `openerFirstName` is null. End-to-end: demo Super Admin → fills the dialog → submit → row inserts with `openedBy = NULL` and a fresh `INC-NNNN` number → appears in Active immediately.

2026-06-03 01:15 - [ADDED] - Super Admin → Incidents page is DB-backed with manual + auto-derived rows. Before: `app/super/incidents/page.tsx` rendered two hardcoded arrays (`INC-0021 GreenLeaf · suspected unauthorized PHI access`, `INC-0022 EU email-delivery latency`, etc.) regardless of tenant state; the "Open incident" dialog only toasted "Incident opened · INC-0023"; "Update status" was a no-op `ActionButton`. New: `Incident` Prisma model + `incidents` table (db-pushed) with id / number autoincrement / title / severity / status / scope / openedBy / openedAt / resolvedAt / resolutionNote. New `GET/POST/PATCH /api/super/incidents` (`requireSuperAdmin()` guard, raw SQL): GET returns `{active, closed}` as a **union** of (a) manual rows from `incidents` and (b) five auto-derived signals computed at read-time — currently-locked users (`users.lockedUntil > NOW()` → high · investigating), brute-force pattern (`failedLoginCount ≥ 5` not currently locked → high · investigating), exhausted MFA challenges last 1h (`mfa_challenges.attemptsLeft = 0` → medium · investigating), infected uploads (`patient_documents.scanStatus = 'infected'` → high · mitigating), and recently-suspended tenants (`organizations.archivedAt > NOW() - 7d` → minor · monitoring). Auto rows self-resolve when their underlying condition clears (the lock expires, the doc is removed, etc.) and appear in Closed with auto-generated resolution notes. POST inserts a manual row + returns `INC-NNNN` display via the autoincrement number. PATCH cycles status `open → investigating → mitigating → monitoring → resolved`; stamping `resolved` sets `resolvedAt`. Page rewrite (`'use client'`, `useEffect` fetch like Tenants page): Active list shows a muted "Auto" badge on derived rows; "Update status" button shows "Auto-managed" + a toast explaining that derived rows clear automatically; manual rows step through the lifecycle on click. Open-incident dialog now POSTs the form (title + severity + scope:`<tenant|region>:<name>`) and refreshes the list on success. Empty states: "No active incidents — the platform is healthy" / "No incidents closed in the last 30 days". Out of scope (deferred): email-send failure persistence (needs a `mail_attempts` table), audit-log-backed bulk-read anomaly detection (needs `audit_logs`), and the "Open runbook" button (still a toast — no runbooks module yet).

2026-06-03 00:40 - [ADDED] - Unread-message count is now surfaced as a red sidebar badge + live dashboard stat. Before: the clinician/patient sidebars showed no badge next to "Messages" even when threads had unread inbound messages; the patient dashboard's "Unread messages" stat tile was wired to `state.threads` (local-store) which is always 0 since the messaging surface stopped using the local store at 18:45. New shared hook `lib/use-messages-unread.ts › useMessagesUnread()` polls `GET /api/messages/threads` on mount + every 30s, sums `thread.unread` across every thread, and returns the total — silently returns 0 on network failure so a brief outage never crashes the layout. `components/shared/role-sidebar.tsx › NavItem` extended with `badgeTone?: "default" | "danger"`; "danger" renders the pill with `bg-[var(--color-danger)]` (white text). Clinician layout (`app/clinician/layout.tsx`) sets `{ badge: String(unreadMessages), badgeTone: "danger" }` on the Messages nav entry when count > 0; same logic added inline in the patient sidebar (`components/patient/sidebar.tsx`). Patient dashboard's `QuickStats` "Unread messages" tile now reads from `useMessagesUnread()` instead of `state.threads`, with a fallback to the local-store count only if the API is unreachable. Result: when Dr. Rahul sends Aarav 2 messages, Aarav's sidebar shows a red "2" pill on the Messages row and the dashboard's "Unread messages" tile reads 2 within 30s; opening the conversation marks them read server-side and the badge clears on the next poll.

2026-06-03 00:20 - [MODIFIED] - Messages-page 10s poll no longer crashes the page with "Failed to fetch" when the dev server restarts. Before: `reloadThreads()` in both `app/clinician/messages/clinician-messages-view.tsx` and `app/patient/messages/messages-view.tsx` did `const r = await fetch(...); const data = await r.json();` with no try/catch — if the fetch threw (network blip, dev-server kill, brief restart window), the rejection bubbled up to the `setInterval` tick → Next.js error overlay → red "Runtime TypeError: Failed to fetch" with `reloadThreads` at the top of the call stack. Fix: wrapped the fetch + json parse in a try/catch that returns early on failure, leaving the existing thread list visible so the next 10s tick retries. The active-conversation fetches inside the tick already had `.catch(() => {})` and were fine. Result: brief network/server unavailability now silently no-ops one tick instead of dumping an error UI on the user.

2026-06-03 00:05 - [MODIFIED] - Sessions list now records the real public IP (e.g. `117.97.169.13`) instead of dropping the field in local dev. Before: `lib/session-store.ts › issueSession` read the IP from `x-forwarded-for` / `x-real-ip` only — both are unset on `localhost`, so every new session row got `ipAddress = NULL` and the UI displayed only the device label with no IP next to it. New: when the header IP is missing OR is `::1` / `127.0.0.1` / a private RFC1918 range (10.x / 192.168.x / 172.16-31.x) / IPv6 link-local (fe80:), the helper falls back to `resolvePublicIp()` which fires a server-side `fetch("https://api.ipify.org?format=json")` with a 2.5s AbortController timeout, parses `{ip}`, and caches the result for 10 minutes so back-to-back login attempts don't all hit ipify. Cache is process-local; failure returns null and the column stays null (never blocks sign-in). Effect: signing in from the same machine that's running the dev server now writes the PC's actual outbound public IP into `sessions.ipAddress`, and `/patient/settings → Sessions` shows e.g. "Chrome · Windows · 117.97.169.13 · Last active just now" for the device. Production behind a real reverse proxy still uses the header IP — the ipify path only runs when the header IP is missing or private.

2026-06-02 23:50 - [ADDED] - Active sessions & devices list on `/patient/settings → Sessions` is now DB-backed with revoke-single + revoke-others. Before: the tab read `state.security.sessions` from the local store and always showed "No active sessions." for real users (the seed list was empty post-22:00 store cleanup); `signSession` only minted a JWT with a synthetic `sid = "s_<uid>_<timestamp>"` and never wrote anything to the `sessions` Postgres table. New: `lib/session-store.ts › issueSession(claims, {req})` mints a JWT *and* inserts a `sessions` row with a `randomUUID()` shared between the JWT's `sid` claim and the row id, recording the User-Agent-derived device label (e.g. "Chrome · Windows"), x-forwarded-for IP, expiresAt = now + TTL, and `refreshTokenHash = sid` (the schema requires a unique non-null value there — we don't actually use refresh tokens). All three sign-in entry points (`/api/auth/login`, `/api/auth/verify-otp`, `/api/auth/register`) now call `issueSession` instead of `signSession` directly — the JWT cookie is unchanged byte-wise from the consumer's view, but now there's a real row to revoke. Demo-user sessions still get a JWT but skip the DB insert (no users.id FK). `/api/auth/logout` additionally `UPDATE sessions SET revokedAt = NOW() WHERE id = sid` so the device drops out of the list immediately on sign-out. New endpoint `GET /api/me/sessions` lists `revokedAt IS NULL AND expiresAt > NOW()` rows for the signed-in user (joined with `currentSid` from claims), and `DELETE /api/me/sessions?id=<uuid>` or `?scope=others` revokes either one row or all-except-current. UI rewrite in `SessionsTab` (`app/patient/settings/page.tsx`): drops `usePatientStore.state.security.sessions`, fetches on mount, renders each row with device label + IP + "Last active Nm ago" + a green "This device" badge on the current session; "Revoke" button on each non-current row PATCHes the revoke endpoint and refetches; "Sign out of all other sessions" auto-disables when no other sessions exist.

2026-06-02 23:30 - [ADDED] - Password change on `/patient/settings → Security` is now wired end-to-end. Before: the Update-password button was an `<ActionButton>` that opened a confirm dialog and then toasted "Password updated · All other sessions revoked · audit-logged" without doing anything — current password was never verified, new password was never hashed, no row was updated in Postgres. New endpoint `POST /api/auth/change-password` (`app/api/auth/change-password/route.ts`, Node runtime): verifies the session, short-circuits demo `uid`s with a 400, validates the new password against the existing `validatePassword(pw)` policy (≥12 chars, upper+lower+digit+symbol), rejects when new==current, bcrypt-compares the supplied current password against `users.passwordHash`, returns 401 "Current password is incorrect" on mismatch, otherwise re-hashes with cost 12 and writes the new hash. The session cookie is intentionally NOT cleared — the user stays signed in on the tab they're editing from. UI: the `<ActionButton>` is replaced with a real `<Button>` whose onClick calls `handleChangePassword()` which POSTs and toasts the server's `error` string on failure ("Current password is incorrect", "Password must include a symbol", etc.) or "Password updated · use it on your next sign-in" on success, plus clears both inputs. Button disabled until current password is non-empty AND new password is ≥12 chars, plus while the request is in flight it shows "Updating…".

2026-06-02 23:15 - [MODIFIED] - Fixed React "change in the order of Hooks called by VaccinationsManager" crash on `/patient/settings → Personal information → Vaccinations`. Cause: my 22:45 DB-backed rewrite of `vaccinations-manager.tsx` put `if (!hydrated) return <Loading…/>` BEFORE the `React.useMemo(...)` that builds `filtered` — so when the fetch finished and `hydrated` flipped from false→true, `useMemo` started getting called on subsequent renders even though the previous (loading-state) render had skipped it, violating the Rules of Hooks. Fix: moved the early `if (!hydrated)` return to AFTER the `useMemo`, so hooks are always called in the same order regardless of load state. Same hook order is now invariant across first-render/post-fetch/edit/delete. No behavior change — still renders Loading… until the GET resolves.

2026-06-02 23:00 - [MODIFIED] - Dashboard "Complete your profile" CTA now routes to `/patient/settings` instead of popping an inline modal. Before: `CompleteProfileCard` wrapped its button in a `<DialogTrigger>` that opened a generic "Aadhaar / ABHA / Home address / Emergency contact / Insurance / Allergies" form which did nothing but toast "Profile saved" after a 600ms simulated save — the form didn't write anything to the local store, let alone the DB, and silently hid itself with `done=true` after first click. With the now-DB-backed Profile / Family / Emergency / Insurance / Vaccinations surfaces all living on `/patient/settings`, that inline modal was both redundant and misleading. New: card is a static informational tile; the Complete-profile CTA is `<Button asChild><Link href="/patient/settings">Complete profile</Link></Button>` so the click takes the patient straight to the real settings page. Dialog imports, simulated-save state, and the `done`/`saving` flags all gone.

2026-06-02 22:45 - [ADDED] - Patient's Personal-information sub-tabs (Family / Emergency / Insurance / Vaccinations) are now DB-backed with full CRUD. Before: all four tabs read/wrote `usePatientStore` (localStorage only) — data was per-browser, didn't survive sign-out, and clinicians couldn't see it. New: four Prisma models (`FamilyMember`, `PatientEmergency`, `InsurancePlan`, `VaccinationRecord`) all db-pushed, plus four new role-gated API routes with `isDbUid` short-circuit for demo sessions: `GET/POST/PATCH/DELETE /api/patient/family` (CRUD by id, validates relationship enum), `GET/PUT /api/patient/emergency` (singleton upsert keyed by patientId — emergency contact, blood group, allergies/critical-meds/medicalIds as JSONB arrays, organ donor + preferred hospital), `GET/POST/PATCH/DELETE /api/patient/insurance` (CRUD, validates coverage + preauth enums, ensures exactly one primary plan, auto-promotes oldest plan to primary on primary-delete, supports `setPrimary` flag in PATCH), `GET/POST/PATCH/DELETE /api/patient/vaccinations` (CRUD, clamps doseNumber/totalDoses to 1–20, requires administered date). Each route hand-codes `$queryRaw` SQL since the generated Prisma client cache was stale on Windows. Managers rewritten: `family-manager.tsx`, `emergency-manager.tsx`, `insurance-manager.tsx`, `vaccinations-manager.tsx` now drop `usePatientStore` for the rows themselves, fetch on mount, push to the API on add/edit/delete/set-primary, and reload after each mutation. Submit buttons show "Saving…" while in flight; all primitives (the dialogs, the empty states, the stat tiles) stay visually identical. End-to-end: Aarav opens `/patient/settings → Personal information → Family → Add family member` → fills in "Anita Mehta, Spouse" → Save → row inserted in Postgres, the family tile counts and list update, the row persists across refresh and across browsers. Same for the other three tabs.

2026-06-02 22:00 - [MODIFIED] - Patient profile's "Clinic / Tenant" field now falls back to the assigned clinician's tenant. Before: self-registered patients (Aarav signed up directly, then Org Admin assigned Dr. Rahul to him) had `users.organizationId = NULL`, so `/api/patient/profile` returned `tenantName: null` and the new field on `/patient/settings` rendered as "—" despite the patient having an active care team at a real tenant. Fix in `app/api/patient/profile/route.ts`: `shape()` accepts a `fallbackTenantName` argument; on both GET and PATCH, when `users.organization?.name` is null, the route queries the patient's latest active `PatientAssignment` and uses `assignment.clinician.organization.name` as the fallback (GET reuses the careTeam query it already runs; PATCH adds a small `findFirst` join). Effect: Aarav's Profile tab now shows "city hospital" in the Clinic / Tenant field via Dr. Rahul's org, and the side-panel avatar card's tenant line populates too.

2026-06-02 21:45 - [MODIFIED] - Patient Settings → Profile tab now has Gender + Clinic/Tenant fields, and the Change-photo button actually persists. Before: the Personal-information section showed only First/Last/Email/Phone/DOB/MRN — no gender, no tenant — and the "Change photo" button at the top right of the side panel called `toast.success("Profile photo updated")` but threw the picked File away (never PATCHed `profilePhotoUrl`, never re-rendered the avatar). New: `<ChangePhotoButton>` (in `app/patient/settings/account-widgets.tsx`) accepts an `onPicked(dataUrl, name)` callback, reads the picked image via `FileReader.readAsDataURL`, rejects >2 MB, and forwards the data URL to the parent; while uploading the button shows "Uploading…" disabled. The Profile tab provides `handlePickPhoto` which PATCHes `/api/patient/profile` with `{ profilePhotoUrl: dataUrl }`; on success the local `photoUrl` state and `api` snapshot update so the avatar swaps to the real photo immediately. Added two new fields to the personal-info grid: a **Gender** `<select>` (Male / Female / Other / Prefer not to say — included in the PATCH body alongside the other fields when "Save changes" is clicked) and a read-only **Clinic / Tenant** field showing `api.tenantName`. The right-side card avatar now renders the data-URL image with `object-cover` and falls back to initials when no photo is set, plus shows the tenant name as a third line. API hardening: `/api/patient/profile` PATCH now rejects `profilePhotoUrl` over ~2 MB or with a URL that isn't `data:image/` or `http(s)`, and the route as a whole gained the `isDbUid` guard.

2026-06-02 21:30 - [MODIFIED] - Conversation pane now has a fixed scroll area + autoscroll-to-bottom. Before: the messages middle div had `flex-1 overflow-y-auto` but its parent `<section>` had no height constraint, so the whole conversation grew with the message list, pushed the composer footer off-screen, and the page scrolled instead of the conversation. Fix in both `app/clinician/messages/clinician-messages-view.tsx › ThreadPane` and `app/patient/messages/messages-view.tsx › ThreadView`: section gets `h-full min-h-0 overflow-hidden` so it actually fills the grid cell, the inner scroll div gets `min-h-0` so flex children can shrink (Tailwind/CSS flex children default to `min-height: auto`, which breaks `overflow` inside them). Added a `scrollRef` + `useEffect` that sets `scrollTop = scrollHeight` whenever the active thread switches or message count changes — so new sends and inbound polls both land you at the bottom. Header (avatar/Encrypted/Pin) and footer (composer + attachments + Send) stay pinned; only the middle bubbles area scrolls.

2026-06-02 21:15 - [MODIFIED] - Logout now flips the user offline immediately. Before: signing out still left the user shown as "online · green dot" on the other party's messages page until the 2-minute heartbeat window naturally expired (since `lastActiveAt` was last bumped within the last 60s). Repro: Priya logs out → Dr. rahul refreshes `/clinician/messages` → Priya's thread still has the green dot + the conversation header still reads "Patient · primary · online" in green. Fix: `POST /api/auth/logout` now (inside a try/catch so it never blocks the logout itself) verifies the session cookie and runs `UPDATE users SET "lastActiveAt" = NULL` for that uid before clearing the cookies. With `lastActiveAt` null, the threads endpoint's `now - lastActiveAt < 2min` check returns false and `otherOnline: false`, so the next poll on the recipient's side immediately switches the dot off and the header reverts to "last active 5:57 PM". Tab-close (no logout) still falls back to the 2-minute auto-expire — acceptable.

2026-06-02 21:00 - [ADDED] - Presence (online status) + WhatsApp-style date dividers on the messaging surface. Before: the conversation header just said "last active 6:21 PM" derived from the most recent message timestamp regardless of whether the other party was actually logged in, and messages from different days were rendered as one flat stream below a single "END-TO-END ENCRYPTED" divider with no day grouping. New: `User.lastActiveAt DateTime?` column (db-pushed); new `POST /api/me/heartbeat` (`app/api/me/heartbeat/route.ts`) bumps that column to `NOW()` for the signed-in user; `components/shared/role-header.tsx` (all non-patient roles) + `components/patient/header.tsx` (patient role) now ping the heartbeat once on mount and every 60s while the tab is open. `/api/messages/threads` joins `users.lastActiveAt` and returns `otherLastActiveAt` + `otherOnline` (true when heartbeat < 2 minutes old) per thread. Both views now render: (a) a green dot overlay on the conversation header avatar AND on each thread-list row avatar when the other party is online; (b) the header status line reads "Specialty · online" in success-green when online, "Specialty · last active 5:54 PM / Yesterday / Mon, May 30, 2026" otherwise; (c) WhatsApp-style date pills ("Today" / "Yesterday" / "Mon, May 30, 2026") rendered before each day's first message so a multi-day conversation reads "[Today]\n[bubble]\n[bubble]\n[Yesterday]\n[bubble]" without dropping the "End-to-end encrypted" intro pill at the very top. Helpers `dayKey(iso)` + `dayLabel(iso)` use local Y-M-D math (not UTC slice) so the dividers match the user's calendar regardless of timezone. End-to-end: Aarav signs in → his patient header heartbeat fires every 60s. On Dr. rahul's `/clinician/messages` the Aarav Mehta thread now shows a green dot on the avatar + the conversation header reads "Patient · specialist · **online**" in green. Aarav closes the tab → within 2 minutes the green dot disappears and the header switches back to "last active 6:21 PM". Multi-day history now reads with crisp WhatsApp-style date headers between message groups instead of one undifferentiated wall.

2026-06-02 20:20 - [MODIFIED] - Message Send button now enables on attachment-only sends. Before: both views' Send button used `disabled={!draft.trim()}`, so even after attaching a file (e.g. Insurance.docx 1.5 MB) the button stayed greyed out until you also typed text — which contradicted the 20:00 API change that already accepts body-OR-attachments. Fix: condition is now `disabled={!draft.trim() && pendingAttachments.length === 0}` in both `app/clinician/messages/clinician-messages-view.tsx` and `app/patient/messages/messages-view.tsx`. Effect: pick a file → Send lights up immediately; same on image; no text needed.

2026-06-02 20:00 - [MODIFIED] - Messages now persist attachments (files + images) to the DB. Before: the Paperclip / Image attachment buttons added a chip to the input area but `noteAttachment` only stored `{name, size, kind}` — the actual file bytes were dropped on the floor, `send()` did not include them in the POST body, and incoming messages had no rendered attachment. New: `Message.attachments` Json column (`@default("[]")`, db-pushed) holds an array of `{name, mimeType, size, dataUrl}` per row. `POST /api/messages` now validates attachments (max 5 per message, max 5 MB each, MIME prefix must be `image/`, `application/pdf`, `application/`, or `text/`) and inserts them via `${attachmentsJson}::jsonb`; `GET /api/messages` returns attachments back through `readAttachments(r.attachments)` with a runtime type guard. Both views (`app/clinician/messages/clinician-messages-view.tsx`, `app/patient/messages/messages-view.tsx`) updated: `noteAttachment` now uses `FileReader.readAsDataURL` to capture the file, rejects >5 MB, stores `{name, size, kind, mimeType, dataUrl}` in `pendingAttachments`, toasts "Attachment ready · sends with your next message"; `send()` passes the attachment objects in the POST body (`atts.map(a => ({name, mimeType, size, dataUrl}))`); failures restore both the draft text AND the pending attachments so nothing is lost on a 4xx/network error. Bubble renderer rewritten: image attachments render as inline thumbnails (`max-h-56 max-w-[260px] object-contain` wrapped in a link to the data URL with `download=name` so click opens in new tab, click-and-save downloads); non-image attachments render as the existing pill but now wrapped in `<a href={dataUrl} download={name} target="_blank">` so the file actually downloads. Empty-body messages (attachment-only sends) are accepted by the API (`body OR attachments must be present`). End-to-end: Dr. Rahul on `/clinician/messages` clicks the Paperclip → picks `lab-report.pdf` → green "Attachment ready" toast → optional reply text → Send → row inserted in Postgres with the pdf as base64 dataUrl, optimistic bubble shows immediately. Aarav on `/patient/messages` (within 10s of polling) sees the same conversation with a clickable `📎 lab-report.pdf · 234 KB` pill — click downloads the real PDF. Image flow shows the picture inline as a thumbnail in the bubble.

2026-06-02 19:30 - [MODIFIED] - Reverted the messages-page visual design to the previous (inbox/pinned/all tabs, search input, filter dropdown, bubble thread with avatar header + Encrypted/Audited badges + Pin button, "End-to-end encrypted" divider, double-check marks, attachment chips, emoji picker) while keeping the 18:45 DB-backed data flow. Before this turn: I had stripped the visual to a minimalist version that lost all the chrome. Now: both `app/clinician/messages/clinician-messages-view.tsx` and `app/patient/messages/messages-view.tsx` carry the original JSX structure verbatim (~600 lines each) but their state hooks pull from the messaging API — `GET /api/messages/threads` for the inbox + `GET /api/messages?withUserId=X` for the active conversation + `POST /api/messages` on Send and on Compose-dialog Start. Local fixtures (`THREADS`, `PATIENT_PANEL`, `CARE_TEAM`, `FLAG_META`) and the `usePatientStore` thread hooks are gone from these files entirely. Pinning is preserved as a session-local `Set<otherUserId>` (no DB column, no PATCH); attachments are still UI-only with a toast that says "attachments are UI-only for now and aren't persisted on send" so nothing silently disappears. The clinician inbox enriches each row with the patient's MRN/role by joining the `/api/clinician/patients` panel response into a `Map<otherUserId, {mrn, role}>` so the inbox row matches the compose-dialog row for the same patient. Both views poll every 10s for the thread list + the active conversation so inbound replies appear without a manual refresh.

2026-06-02 18:45 - [ADDED] - Full DB-backed messaging between clinicians and patients. Before: clinician and patient `/messages` pages rendered hardcoded `THREADS` arrays (Aarav · "Doctor, I've been having mild chest tightness…" / Riya · "Lab report attached" / Tarun · "Thanks for the prescription" / Aanya / Kabir · "Consent pending" / Meera, etc.) — every signed-in user saw the same fake feed regardless of who they actually messaged with, and "Send" only pushed into local store. New `Message` Prisma model (`messages` table, db-pushed) with fields: id, clinicianId, patientId, organizationId, senderRole ("clinician"|"patient"), body, sentAt, readAt, plus three indexes (`(clinicianId, patientId, sentAt)`, `(patientId, readAt)`, `(clinicianId, readAt)`). Threads are implicit — DISTINCT ON the (clinicianId, patientId) pair yields one row per conversation. Three new endpoints (raw SQL, all role-gated, demo-uid short-circuit to empty payloads): `GET /api/messages/threads` returns the signed-in user's thread list with the most recent body + sender role + unread count for that pair, joined to the other party's identity (name, initials, designation, department); `GET /api/messages?withUserId=X` returns the chronological conversation between the signed-in user and X, marking inbound-unread messages as read in the same query; `POST /api/messages` accepts `{toUserId, body}`, validates the role pairing (Clinician→Patient or Patient→Clinician), resolves the conversation's clinicianId/patientId/orgId, inserts via `$queryRaw INSERT ... RETURNING id, sentAt`. Both message views (`app/clinician/messages/clinician-messages-view.tsx` and `app/patient/messages/messages-view.tsx`) were rewritten from scratch: bespoke hardcoded `THREADS` / `CARE_TEAM` / flag-meta / pin/unread filter UI gone; new shared shape fetches threads on mount + polls every 10s, fetches messages for the active conversation, renders bubbles (right-aligned + primary background for fromMe, left + muted for them), and Send POSTs to `/api/messages` then refetches both thread list and conversation for instant truth. Compose dialog stays — it now POSTs the first message via the same endpoint so a new thread row materializes immediately. Deep-link `/clinician/messages/[threadId]` and `/patient/messages/[threadId]` routes now no-op the param (the view auto-selects most recent thread; targeting a specific conversation is done via `?thread=<otherUserId>` query string). End-to-end: as Dr. rahul on `/clinician/messages` → New message → pick Aarav Mehta from your real panel → type "hello" → Send. As Aarav on `/patient/messages` (within 10s) → Dr. rahul sharma appears at the top of the thread list with the unread badge → click → "hello" message renders, server marks read → reply "got it" → Send. Refreshing either side preserves the conversation; persists across browser sessions; no localStorage involvement.

2026-06-02 17:55 - [MODIFIED] - Messaging compose-recipient dropdowns are now DB-sourced on both clinician and patient sides. Before: `app/clinician/messages/clinician-messages-view.tsx` rendered a hardcoded `PATIENT_PANEL` array (Aarav Mehta · MRN-44118, Riya Mehta · MRN-44119, Tarun Mehta · MRN-44120, Aanya Verma · MRN-44211, Kabir Joshi · MRN-44309, Meera Singh · MRN-44402, Ramesh Patel · CITY-001234, Sai Iyer · MRN-44501) regardless of who was actually on the signed-in clinician's panel; `app/patient/messages/messages-view.tsx` similarly hardcoded a `CARE_TEAM` array (Dr. Priya Shah / Dr. Rohan Iyer / Dr. Meera Nair / Care coordinator / Radiology Dept. / Pharmacy) regardless of the patient's actual care team. New: clinician view's `ClinicianMessagesView` adds a `useEffect` that fetches `/api/clinician/patients` on mount, maps each row to `{name, initials, mrn, role}`, and passes the live list + a loading flag down to `ComposeDialog` as props. `ComposeDialog`'s "Recipient" select renders the dynamic list with a graceful "Loading your panel…" or "No patients on your panel yet. Ask your Org Admin to assign patients to you." empty state when the clinician hasn't been assigned anyone yet, and the Start-conversation button auto-disables on an empty roster. Patient view's `ComposeDialog` does the symmetric thing against `/api/patient/clinicians` — fetches on first open, maps each row to `{name: "Dr. F L", initials, role: designation ?? department ?? "Care team"}`, and shows "No clinicians in your care team yet. Book an appointment to get started." if the patient has nothing assigned yet. Demo-user safety: `/api/patient/clinicians` gained the `isDbUid` guard (returns `{ok: true, clinicians: []}` for non-UUID sessions) so demo patients see the empty-state copy instead of a 500. Thread storage + message persistence still localStorage-only — that's a larger scope (Thread/Message DB tables, send/list endpoints, attachments) tracked for a follow-up.

2026-06-02 17:15 - [ADDED] - Clinician can now create + run an appointment from the patient chart. Before: the chart's EncounterCard with "Arrived / In-progress / Completed / No-show" lifecycle buttons only rendered when `state.appointments` (local-store) had a matching row for today — for any DB-assigned patient with no booking yet, the section between the patient banner and the tab strip was a blank empty band (matches the user's screenshot for Neha Bansal, who was just assigned by Org Admin and has no appointment yet). Now: `POST /api/clinician/appointments` accepts `{patientId, date, time, durationMinutes, mode, notes}`, validates the patient is on the clinician's panel (`patientAssignment` row exists, not ended), looks up the clinician's tenant, rejects double-booking the same slot (status NOT IN cancelled/no_show), and creates an `appointments` row with status=confirmed. `PATCH /api/clinician/appointments/[id]` accepts `{status}` (one of confirmed | completed | no_show | cancelled | blocked) and updates ownership-checked; UI-only states "arrived"/"in-progress" return `ok: true, persisted: false` so the front-end keeps a local visual hint without forcing an enum migration. On the chart page: new "Add appointment" button on the patient banner (next to SOAP note + Message) opens a new `AddAppointmentDialog` (date / 24h time / 5–240 min duration / In-person · Telehealth toggle / optional notes) — Submit POSTs to the new endpoint, toasts, closes, and refetches. New `dbAppts` state polled from `/api/clinician/appointments` and filtered to this patient by `patientEmail` match (against `state.assignedPatients` OR `apiPatient`). If a row falls on local-today and is in a non-terminal status, it becomes `todayAppointmentDb` and the existing EncounterCard renders with that — every status click now hits PATCH if the appointment is DB-backed (refetches on success), or falls back to the local-store path for legacy demo flows. End-to-end: as Dr. Rahul on Neha Bansal's chart → click "Add appointment" → fill in 10:00 / 30 min / in-person → Book → row created in Postgres; the EncounterCard appears with the four lifecycle buttons; clicking Completed PATCHes status=completed and shows "audit-logged" in the toast. Patient sees the same appointment under their /patient/appointments page (same row, same email).

2026-06-02 16:30 - [MODIFIED] - Patient dashboard's last two static cards are now DB-driven. Before: `RecentRecords()` rendered a hardcoded 4-row array (Lipid panel · Dr. Priya Shah / Atorvastatin · Dr. Priya Shah / Chest X-ray · Radiology / Discharge · Dr. Rohan Iyer) and `PendingConsents()` rendered a single hardcoded "Dr. Neha Kapoor · Imaging" banner regardless of who had actually requested anything from the signed-in patient. New: `RecentRecords()` `useEffect`-fetches `/api/patient/records` (already DB-backed — finalized prescriptions ∪ medical_records joined to clinician + tenant), keeps the top 4 most recent, and renders them with the same icon+color treatment derived from `category` (Lab Report / Prescription / Imaging / Discharge / Clinical Note). The PDF-export helper now stamps the real clinician name + date + record id pulled from the DB row. Empty state ("No records yet — your clinicians' finalized notes and prescriptions will appear here") added so newly-registered patients don't see fabricated history. `PendingConsents()` fetches `/api/patient/consent-requests` (DB-backed, filters client-side to status=pending), shows the real requesting clinician's initials/name + the actual scopes formatted via the same SCOPE_LABEL map used in notifications, and the Approve/Decline buttons PATCH the API directly (no more local-store mirror — the API row is the source of truth). Loading + empty states added. Also: removed unused `ConsentRequestDialog` and `ActionButton` imports (the local-store grant path they wired into is gone). Bonus guard hardening: `/api/patient/records` and `/api/patient/consent-requests` now both short-circuit to `{ ok: true, records|requests: [] }` for demo-user sessions, matching the pattern added at 14:45. Fixed adjacent bug exposed in the dev-server log: `/api/notifications › adminFeed` was filtering `pa.organizationId` but `patient_assignments` has no such column; switched to `p."organizationId" = :orgId` via the joined patient. Dev server restarted with `NODE_OPTIONS=--max-old-space-size=4096` after the prior process OOM'd (Next.js dev memory threshold was hit after ~30min of hot-reloads).

2026-06-02 15:30 - [ADDED] - Cross-role dynamic notifications feed. Before: each role's `/notifications` page rendered a hardcoded `SimpleNotification[]` array (admin / clinician / patient had their own bespoke pages with localStorage-backed read-state; compliance / auditor / super passed a static fixture into the shared `NotificationsList`). New: single `GET /api/notifications` (`app/api/notifications/route.ts`) reads `claims.role` + `claims.uid` and returns a role-appropriate feed built from real Postgres rows via `$queryRaw` — Patient: pending + decided consent_requests joined to requesting clinician (pending marked `critical`), upcoming appointments next 14d joined by patient email, finalized prescriptions last 14d. Clinician: consent_requests decided in last 14d joined to patient, appointments booked in last 14d, new patient_assignments. Org Admin: new users in tenant last 14d (patient vs staff phrased differently), new patient_assignments in tenant. Compliance Manager: pending consent_requests in tenant (critical) + recent decisions. Auditor: read-only audit feed of consent decisions + appointment bookings in tenant. Super Admin: recently provisioned tenants + new org-admin onboardings across the platform. Demo-user sessions (non-UUID uid from `lib/demo-users.ts`) and any DB error fall through to `items: []` so the page renders the empty state instead of 500'ing. New `components/shared/dynamic-notifications.tsx` wrapper handles the fetch + loading + handoff to the existing `NotificationsList` UI. All 6 role pages (`app/{admin,auditor,clinician,compliance,patient,super}/notifications/page.tsx`) collapsed to ~13 lines each: PageHeader + `<DynamicNotifications />` — the old static `NOTIFICATIONS = [...]` arrays and the admin/clinician/patient bespoke render/read-state UIs are gone (read-state now lives in `NotificationsList`'s local component state, same as the compliance/auditor/super path already did). Effect: signing in as Dr. Rahul → submitting a consent request to Priya → signing in as Priya makes the patient see a "Dr. rahul sharma requested access · Pending your review · Lab Report" critical row on `/patient/notifications`; once Priya approves, switching back to Dr. Rahul makes "priya Mehta approved your request · Lab Report" appear on `/clinician/notifications`. Org Admin sees "New patient: priya Mehta" the moment Priya self-registers. Compliance + Auditor see the same consent decision feed scoped to tenant.

2026-06-02 14:45 - [MODIFIED] - Demo-user sessions no longer 500 the clinician + patient API routes. Repro: signing in as any seeded demo user (Dr. Priya Shah, Aarav Mehta, etc. — anything from `lib/demo-users.ts` whose `uid` looks like `"u_clinician_priya"`) made `/clinician/patients` and `/clinician/dashboard` blow up with `PrismaClientKnownRequestError P2023: Inconsistent column data: Error creating UUID, invalid character: ... found 'u' at 1` because the demo `uid` was passed straight into a `prisma.user.findUnique({ where: { id: claims.uid } })` (and a `${uid}::uuid` raw cast in availability/consent-requests/slots) against a Postgres `uuid` column. UI showed "Network error — could not load your panel." Fix: new `isDbUid(uid)` helper in `lib/auth.ts` (regex check for the canonical UUID v4 shape); when it returns false, every demo-user-reachable role-gated route short-circuits to a sensible empty payload before touching Prisma. Routes guarded: `/api/clinician/dashboard` (returns me-stub + empty today/upcoming/pendingTasks), `/api/clinician/patients` (empty roster), `/api/clinician/patients/[id]` (404 → the chart page falls through to its existing local-store lookup), `/api/clinician/appointments` (empty list), `/api/clinician/availability` (empty template + 15-min interval), `/api/clinician/consent-requests` (empty list — the 15s chart poll now no-ops cleanly for demo clinicians), `/api/clinician/notes` (empty), `/api/clinician/prescriptions` (empty), `/api/patient/dashboard` (me-stub + empty appointments/prescriptions/consents). Real DB users (`Dr. Rahul Sharma`, the Aarav/Tarun/Priya patients created via Org-Admin invite, anyone who self-registered) are unaffected — the regex passes and the existing Prisma queries run as before.

2026-06-02 14:00 - [MODIFIED] - Patient booking slot grid now subtracts already-booked appointments. Before: `/api/patient/clinicians/[id]/slots` generated slots purely from the clinician's `scheduleTemplate`, so the booking grid would still show `Tue Jun 2 · 9:30 AM` even after another patient (Tarun Mehta) had already booked it — risking double-booking until the DB unique-slot check failed at POST. New: route now also `$queryRaw`s the clinician's appointments from `appointments` where `clinicianId = :id AND deletedAt IS NULL AND status NOT IN ('cancelled','no_show') AND startsAt ∈ [now, now+11d)`, buckets them into a `Map<localYYYY-MM-DD, Set<H:MM AM/PM>>` keyed by local Y-M-D + the same `fmt12()` formatter the grid uses, and `.filter()`s those out of each day's `allTimes` list before pushing it to the response. Days whose entire slot list ends up consumed by bookings are skipped (the loop still tries up to 10 calendar days to find 4 working days with capacity). Effect: as soon as Tarun books `9:30 AM` on Jun 2 and Priya books `3:30 PM` on Jun 2, the next patient hitting Step 2 of /patient/appointments/new sees `Tue, Jun 2 · 30-min slots` with `9:30 AM` and `3:30 PM` absent from the grid — the remaining 9:00 / 10:00 / 10:30 / 11:00 / 11:30 / 12:00 / 12:30 / 2:00 / 2:30 / 3:00 / 4:00 / 4:30 PM still selectable. No client change required — `/patient/appointments/new` already renders whatever the API returns.

2026-06-02 13:30 - [MODIFIED] - Patient appointment booking date no longer slides one day west for east-of-UTC users. Repro: Aarav Mehta booked a `Tue, Jun 2 · 10:00 AM` slot from a browser in IST (UTC+5:30); the appointment showed on Dr. Sharma's `/clinician/schedule` on Mon, Jun 1 instead. Cause: `app/patient/appointments/new/page.tsx › slotDayToIso` parsed `"Tue, Jun 2, 2026"` as local midnight (`2026-06-02 00:00 IST` = `2026-06-01 18:30 UTC`) and then called `parsed.toISOString().slice(0, 10)` → `"2026-06-01"` — silently dropping the booking on the wrong day. Fix: derive YYYY-MM-DD from `getFullYear/getMonth/getDate` (browser-local) via a `toLocalIso` helper instead of round-tripping through UTC. Same anti-pattern fixed in two more spots that bucket appointments by day: `app/clinician/schedule/page.tsx` (week-view tile grouping and "today" highlight — both were using `d.toISOString().slice(0, 10)`, so the week view would have mis-assigned IST appointments to the previous day's column), and `app/clinician/patients/[id]/page.tsx › todayAppointment` (chart's today's-encounter card). Other `toISOString().slice(0, 10)` usages left in place: date-only DB fields like `dateOfBirth` / `joiningDate` (stored as midnight UTC, no TZ drift), and `/admin/appointments/page.tsx › todayDateInput` (already does the TZ-offset trick correctly).

2026-06-02 12:30 - [MODIFIED] - Demo "(demo) Simulate approval" button removed from the clinician chart's pending-request banner; pending→approved transition is now driven by the patient's real decision on `/patient/consents` and a 15s poll. Before: clicking the demo button on the yellow banner ran `simulateApprovalDecision` locally — a holdover from when the consent-request flow wasn't DB-backed. With the 06-02 09:00 DB-backed flow live, that button bypassed the real approval path. New: button + the `onSimulateApprove` prop gone. Banner copy updated to "Awaiting patient's consent decision" (clearer than "Request pending Sai's review", which named the wrong reviewer for the real flow). New `GET /api/clinician/consent-requests?patientId=…` (Clinician-gated, raw SQL) returns the signed-in clinician's requests for the given patient with current `status`, `decidedAt`, `expiresAt`. New `reconcileRemoteRequest` action on `lib/clinician-store.tsx` maps server status (approved/declined/expired/pending) → local AccessRequest enum, upserts the row in `accessRequests` with server-stamped `expiresAt`, and on approve mirrors `scopes` into the patient's standing `consentScopes` (same merge logic the 11:15 fix added). New `useEffect` on `/clinician/patients/[id]` polls the GET endpoint on mount and every 15s; on each tick reconciles every returned row through the store action. Effect: clinician submits → yellow "Awaiting patient's consent decision" banner shows. Patient signs in elsewhere, clicks Review & approve on `/patient/consents` → PATCH stamps status=approved + expiresAt. Within 15s the clinician's chart polls, reconciles, the yellow banner disappears, the green "Temporary access · expires in Nh Nm · Approved" banner takes its place, and every scope in the request flips its Consents-tab card from red "No access" to green "Active" simultaneously.

2026-06-02 11:45 - [MODIFIED] - Patient-chart Consents tab scope cards now honor approved access requests, not just standing consent. Before: the 6 cards (Insurance / ID Proof / Lab Report / Imaging / Prescription / Other) read `patient.consentScopes.includes(scope)` only — so if a scope had been approved via the request flow but never written to `consentScopes` (the pre-fix demo path, or any pre-existing approved request persisted in localStorage from before the 11:15 fix), the green "Approved · expires in Nh Nm" banner showed up top while that same scope's card below stayed dashed red "No access". New: `PatientConsents` lost its `patient.consentScopes`-based check and now receives an `isGranted(scope)` callback computed by the parent as `hasEffectiveConsent(state, patient.id, scope)` — that helper already returned the union of (standing consent ∩ active) ∪ (status=approved access requests not yet expired). Effect: any scope covered by a not-yet-expired approved request flips its card to the solid green "Active" badge + success-soft background + "Patient granted access" copy + "Simulate revoke" CTA, in lockstep with the green top banner. The button's onToggle path now decides "revoke vs request access" off the same union so toggling a scope that's granted via a request opens the revoke-info toast instead of opening the request dialog again. Combined with the 11:15 store fix, this covers both newly-approved scopes (which now write into `consentScopes`) AND legacy/persisted approved-request rows (which the cards now read directly from `accessRequests`).

2026-06-02 11:15 - [MODIFIED] - Simulated approval on the clinician chart now propagates to every scope card. Before: clicking "(demo) Simulate approval" on the yellow pending banner flipped the request's status to approved (so the green `Temporary access · expires in Nh Nm` banner replaced the yellow one), but the 6 cards in the Consents tab below stayed red "No access" because `consentScopes` was never updated — so the chart showed "Approved · Imaging" up top while the Imaging card still read "Patient has not granted access". New: `lib/clinician-store.tsx › simulateApprovalDecision` now reads the request being approved, merges its `scopes` array into the target patient's `consentScopes` (deduped via `Set`), and flips `consentStatus` to `"active"`. Effect: when the user simulates approval of any request (single or multi-scope — Insurance / ID Proof / Lab Report / Imaging / Prescription / Other), every scope in that request flips simultaneously from the dashed red "No access" card to the solid green "Active" card with the success-soft background. The yellow→green top-banner transition was already wired (pending banner disappears, ApprovedAccessBanner renders in its place once status flips), so the visual transformation now reads end-to-end: pending alert turns green AND all involved scope cards turn green. Reject path unchanged.

2026-06-02 09:00 - [ADDED] - Cross-portal consent-request flow is DB-backed end-to-end. Before: clinician's RequestAccessDialog only wrote to localStorage; the patient's `/patient/consents` page showed a hardcoded "Dr. Neha Kapoor requested access to Imaging" banner regardless of who actually requested anything. New `ConsentRequest` Prisma model (`consent_requests` table, db-pushed on both schemas): id, patientId, clinicianId, organizationId, scopes (JSONB array of scope keys), durationHours, reason, status (pending|approved|declined|expired), requestedAt, decidedAt, decisionNote, expiresAt. New `POST /api/clinician/consent-requests` (Clinician-gated, raw SQL): validates patient + clinician + scopes (against the union of patient-store and clinician-store ConsentScope enums) + reason (≥10 chars) + duration (1–168h); rejects duplicate-pending for the same (patient, clinician); inserts a row with status=pending. New `GET /api/patient/consent-requests` returns the signed-in patient's requests joined to clinician identity (name + department); `PATCH` flips status to approved or declined and (on approve) stamps `expiresAt = NOW() + durationHours hours`. `RequestAccessDialog` Submit button rewired: POSTs to the new endpoint, mirrors into the local store on success for instant chart-banner reflection, toasts "Request submitted · audit-logged · <patient> will see this on their consents page", disabled while in flight. `/patient/consents` page: hardcoded Dr. Neha Kapoor banner removed; replaced with a real loop over `pending` (state populated from GET) — each pending row shows the actual requesting clinician's name + department + the scope labels + duration + reason. Decline button PATCHes status=declined; Review & approve PATCHes status=approved AND mirrors the grant into the local store so the new consent appears in the Active tab immediately + a "Consent granted" notification is added.

2026-06-01 23:15 - [MODIFIED] - Clinician's patient-chart Consents tab now mirrors the patient's grant flow. Before: 5 fixed cards (Lab Reports / Prescriptions / Clinical Notes / Imaging / Mental Health) and the "Request access" CTA only flipped a local-store toggle. New: 6 cards in the same order the patient sees on `/patient/consents/grant` — **Insurance · ID Proof · Lab Report · Imaging · Prescription · Other**. Card UI unchanged (no-access/Active badges, consent-bound description). Clicking **Request access** on a non-granted scope now opens the existing `RequestAccessDialog` pre-scoped to that category instead of running the demo toggle. Existing granted scope still flips via the local-store revoke toggle (demo path — real revoke is patient-initiated). `lib/clinician-store.tsx` `ConsentScope` enum extended with `"insurance"`, `"id_proof"`, `"other"` and `CONSENT_SCOPE_LABEL` updated to match the patient store, so the dialog labels and the consent-status banner badges stay consistent across clinician + patient surfaces. `RequestAccessDialog`'s `ALL_SCOPES` list updated to the same 6-entry order — the request dialog now offers the same six categories as checkboxes when the clinician hits Request access from any tab.

2026-06-01 22:45 - [MODIFIED] - Consent grant Scope section rewritten to use document-category checkboxes (matches the /patient/documents Tags sidebar) instead of the previous PHI-access toggles. `ConsentScope` enum extended with three new values (`"insurance"`, `"id_proof"`, `"other"`) on top of existing lab/prescriptions/imaging/notes/mental_health; `CONSENT_SCOPE_LABEL` updated to match. `/patient/consents/grant` SCOPES list rewritten to six rows in the order Insurance / ID Proof / Lab Report / Imaging / Prescription / Other with sensible icon + description per row. Each row now shows a live document-count badge derived from `state.documents.filter(d => d.category === ...).length`, so the patient sees exactly how many vault items each scope exposes (e.g. "Lab Report · 1 · Lab results & panels"). UI swapped from the toggle `<Switch>` to a labelled `<input type="checkbox">` styled like the recipient radios; checked rows get a primary-tinted background. State logic + `addConsent` payload unchanged — scopes still go into the consent record as a `ConsentScope[]`.

2026-06-01 22:15 - [REMOVED] - Hardcoded "Dr. Priya Shah / Dr. Rohan Iyer / Dr. Neha Kapoor" recipient list on `/patient/consents/grant`. Page now fetches `/api/patient/clinicians` (the same endpoint the booking page uses), populates the recipient radio list with the patient's real care team first (active `PatientAssignment` rows, marked with a green **Your care team** badge) followed by other active clinicians in the tenant. Loading state ("Loading clinicians…") replaces the stale list while the fetch resolves; empty state ("No clinicians available in your tenant yet — once a clinician is assigned or you book an appointment they'll appear here") shows when the API returns 0 rows. `canSubmit` now also gates on `!!selected` so the Grant button stays disabled until a real clinician is picked; `submit()` re-checks `selected` defensively. `selectedName` is auto-set to the first returned clinician once the fetch resolves.

2026-06-01 22:00 - [ADDED] - Auto-tag uploaded documents by category. Before: every patient upload defaulted to category "Other" (or whatever the dropdown had pre-selected), so the Documents sidebar inevitably showed "Other · 4" with every other tag at 0. The category dropdown worked but the user had to manually pick it every time. New `guessCategory(file)` in `app/patient/documents/upload/page.tsx`: inspects `file.type` (MIME) + `file.name` (lowercase) against regex patterns — `/insur/` → Insurance, `/aadhaar|aadhar|passport|driving|licen[cs]e/` → ID Proof, `/prescription|^rx[-_ ]/` → Prescription, `/lab|report|panel|blood|cbc|lipid|hba1c/` → Lab Report, `/ecg|ekg|x[-_]?ray|mri|ct[-_ ]?scan|ultrasound/` → Imaging, any image MIME → Imaging, else Other. `addFiles()` auto-sets the dropdown to the guess for the first added file (with a "Tagged as Imaging · Auto-detected from filename" toast). The user can override via the Category dropdown — a `categoryEdited` flag tracks that and stops auto-detect from clobbering their pick. Upload loop now uses per-file `guessCategory()` when the user hasn't overridden, so dropping an insurance scan + a lab PDF together tags each correctly. Result: uploading `lab-cbc.pdf` lands in Lab Report (tag count ticks 0→1); uploading `ecg-2026.png` lands in Imaging. The existing Tags filter pipeline was already correct — these uploads now populate the right tag.

2026-06-01 21:30 - [ADDED] - Clinical notes are now DB-backed end-to-end. Before: `/clinician/notes/new` Save & Finalize only persisted to localStorage `clinician-store`; `/clinician/notes` list pulled from the same store and labelled every row "Unknown patient" because `state.assignedPatients` was empty for the freshly-signed-in clinician; the patient's `/patient/records` Clinical Notes + Discharge categories stayed at 0 because no rows ever reached the `medical_records` table. New `POST + GET /api/clinician/notes` (clinician-gated, raw SQL — runtime Prisma client cache is still stale on the new model) — POST validates patient + clinician + template (SOAP/Progress/Discharge/Consult), accepts S/O/A/P + body fields, optional `status:"finalized"` to lock immediately; GET lists this clinician's notes with optional `?patientId=` scope and joins `users` for the patient name (`patientName` field on response, no more "Unknown"). New `GET/PATCH/DELETE /api/clinician/notes/[id]` with the same immutability rules as prescriptions (finalized notes refuse PATCH + DELETE). `/clinician/notes/new` page rewritten: `persistDraft()` replaced with `persistToDb(finalize)` that POSTs the first save and PATCHes subsequent saves; local store is mirrored on success so the list reflects the change without a refetch but the DB row is canonical. `/clinician/notes` list page replaces `state.notes` with a `dbNotes` state populated from `GET /api/clinician/notes` on mount; `patientName(id, fromApi)` helper now prefers the joined name from the API row over the local-store lookup so "Unknown patient" rows disappear. Because the existing `/api/patient/records` already aggregates `medical_records` joined to author + tenant, Priya now sees Discharge + Clinical Notes counts tick up the moment Dr. Rahul finalizes them.

2026-06-01 21:00 - [ADDED] - `/patient/records` clinical history is now DB-backed. New `GET /api/patient/records` (Patient-gated) aggregates the signed-in patient's finalized **prescriptions** (joined to prescriber + tenant) AND finalized **medical_records** (joined to author + tenant) into a single `MedicalRecord[]` shape — prescription rows get `category: "Prescription"` + a populated `.prescription` block; SOAP/Progress notes get `category: "Clinical Note"`; `Discharge`-template notes get `category: "Discharge"`. Sorted by date desc. `app/patient/records/page.tsx` rewritten: removed the static `RECORDS` import + replaced every reference (filter pipeline, category counts, search, PDF export, "X records" footer) with a `records` state populated from the API on mount. Category counts in the sidebar now reflect REAL data — "Prescriptions · 1" appears as soon as Dr. Rahul finalizes one for this patient, "Clinical Notes · 2" once notes land, etc. Lab Report + Imaging categories stay at `0` until those tables get APIs (still on the roadmap).

2026-06-01 20:45 - [REMOVED] - Hardcoded clinician filter on `/patient/records`. Was a static `CLINICIANS = ["Dr. Priya Shah", "Dr. Rohan Iyer", "Dr. Neha Kapoor", "Radiology Dept."]` array — every patient saw the same four entries even after the seed data was stripped. Page now fetches `/api/patient/dashboard` on mount, extracts unique `careTeam[].clinician.name`, and populates the Clinician filter card from that. Falls back to an italic "No clinicians on your care team yet." placeholder when the patient has zero active `PatientAssignment` rows. Filter checkbox logic + `clinicians` filter state unchanged — the existing `r.clinician` match in the records filter pipeline keeps working untouched, ready for the records table to land DB-backed in a follow-up.

2026-06-01 20:30 - [ADDED] - Patient prescriptions page (`/patient/prescriptions`) is now DB-backed. Was reading from the (empty) `RECORDS` array in `app/patient/records/records-data.ts`, so every patient saw "0 active / 0 past / No active prescriptions" regardless of what clinicians had prescribed. New `GET /api/patient/prescriptions` (Patient-gated) uses `$queryRaw` (Prisma runtime cache is still stale on the new model — same pattern as the clinician routes) to read `prescriptions` joined to `users` (clinician identity) + `organizations` (tenant name) filtered to `patientId = session.uid AND deletedAt IS NULL AND status = 'finalized'` — drafts are deliberately excluded so the patient doesn't see in-progress prescriptions before the clinician finalises. Page rewritten as a `"use client"` component with a fetch on mount, loading + error states, Active/Past tabs (all finalized treated as Active until lifecycle tracking lands), stat cards bound to real counts, and a "Refill credits left" sum across active rxs. Each card displays drug + strength, route/frequency/duration, prescribing clinician + tenant + finalised date, refills count, and instructions (rendered only when set). Refill button hidden when `refills === 0`.

2026-06-01 20:00 - [ADDED] - Real document preview + download on `/patient/documents`. Until now `downloadDoc()` always built a synthetic text-blob placeholder ("HealthSecure Portal — secure document · demo placeholder export") regardless of what was uploaded, because the patient-store `PatientDocument` only kept metadata (`name, category, sizeBytes, …`) and discarded the file bytes. Extended the type with optional `dataUrl?: string` (base64 data URL captured at upload time) + `mimeType?: string`. `/patient/documents/upload/page.tsx` now reads each File via `FileReader.readAsDataURL` and persists the result alongside name/category/size. `/patient/documents/page.tsx`: new `dataUrlToBlob()` helper that strips the `data:<mime>;base64,...` prefix and produces a real Blob; `previewDoc()` wraps the Blob in `URL.createObjectURL` and calls `window.open(url, "_blank", "noopener")` so PDFs/PNGs/JPEGs render natively inline (Object URLs are more browser-compatible than long `data:` URLs in new tabs); `downloadDoc()` uses the same Blob with `a.download = d.name` so the file lands with its original name + type. Document rows are now click-to-preview (icon + name are both `<button>`s wired to `previewDoc`); the meta row gained a "click to preview" hint. Both actions are blocked when `scanStatus !== "clean"` ("Blocked — quarantined by the virus scanner"). Both toast "audit-logged". Legacy docs uploaded before this change (no `dataUrl`) gracefully fall back to the prior placeholder so the page never breaks.

2026-06-01 19:30 - [FIXED] - "Patient not on your panel" empty state on three sub-routes of the chart: `/clinician/patients/[id]/timeline`, `.../records`, and `.../documents`. Each page did its own `state.assignedPatients.find((p) => p.id === id)` lookup against the local store — which is empty for fresh clinicians after the v4 reset — and showed the NotFound card without ever attempting the API. Applied the same DB-fallback pattern that was already on the main chart page: on mount, when the store misses the id, fetch `/api/clinician/patients/[id]` (which validates an active `PatientAssignment` for this clinician), and synthesize a minimal `AssignedPatient` from the response so the rest of the page renders. Added a "Loading patient…" placeholder during the API roundtrip so the alarming "not on your panel" copy no longer flashes for half a second before the data arrives. All three pages also gained the `AssignedPatient` type import.

2026-06-01 19:00 - [FIXED] - Two issues from the patient chart Prescriptions flow + clinical note compose. (1) `GET/POST /api/clinician/prescriptions` and `GET/PATCH/DELETE /api/clinician/prescriptions/[id]` returned 500 — same cause as the schedule_template fix: the cached Prisma client engine in `node_modules/.prisma/client/schema.prisma` doesn't know about the new `Prescription` model (the DLL is locked by the dev server so `prisma generate` fails), and `prisma.prescription.*` calls throw `Unknown model`. Rewrote BOTH routes to use `$queryRaw` / `$executeRaw` against the live Postgres table. Postgres column names are camelCase (verified via `information_schema.columns`): `id, patientId, clinicianId, organizationId, drugName, strength, route, frequency, duration, refills, patientInstructions, status, createdAt, updatedAt, finalizedAt, deletedAt`. UPDATE uses `Prisma.sql` for safe param binding and a conditional `finalizedAt = NOW()` only when finalizing. (2) `/clinician/notes/new` clinical note compose page showed "— No patients assigned —" for every clinician because it read `state.assignedPatients` from the local store (empty after v4 reset). Page now also fetches `/api/clinician/patients` on mount, unions with the store (store-first dedup), and the patient `<select>` lists everyone — when API patients land after first render, the first one is auto-selected if no patient was specified via `?patient=`.

2026-06-01 18:30 - [ADDED] - DB-backed Prescriptions end-to-end (compose, save draft, finalize, list on patient chart). Three new Prisma models added (both frontend + backend schemas, `prisma db push`ed): `Prescription` (id, patientId, clinicianId, organizationId, drugName, strength, route, frequency, duration, refills, patientInstructions, status[draft|finalized], createdAt/updatedAt/finalizedAt/deletedAt + indexes on patient/clinician+status), `MedicalRecord` (id, patient/clinician/org, template[SOAP|Progress|Discharge|Consult], subjective/objective/assessment/plan/body, status, version, lifecycle timestamps), `PatientDocument` (id, patient/uploader/org, name/category/mimeType/sizeBytes/dataUrl/scanStatus/sharedWithPatient/uploadedAt). New Prescription routes (all Clinician-gated, same-clinician check): `GET /api/clinician/prescriptions?patientId=` lists this clinician's prescriptions for the given patient; `POST` creates a draft (placeholder drug "New prescription"); `GET/PATCH/DELETE /api/clinician/prescriptions/[id]` reads, updates, soft-deletes (finalized prescriptions refuse PATCH + DELETE; PATCH with `status: "finalized"` validates required fields + locks). Patient chart `/clinician/patients/[id]/page.tsx` rewired: removed `addPrescription/finalizePrescription` from the local store destructure, added a `dbRxs` state + `reloadRxs()` that hits `GET /api/clinician/prescriptions?patientId=`, Prescriptions tab's `onNew` POSTs and navigates to the compose page, `onFinalize` PATCHes with `status:finalized`. Timeline component now reads `dbRxs` instead of the local store. Compose page `/clinician/prescriptions/[id]/page.tsx` rewired: removed `useClinicianStore`, added GET `/api/clinician/prescriptions/[id]` on mount, "Save draft" PATCHes the fields, "Finalize & lock" PATCHes with `status: "finalized"` (server validates `drugName`, `strength`, `frequency`, `duration` are non-empty before locking), redirects to the chart Prescriptions tab on finalize. **Records + Documents tabs: schemas added + db-pushed but APIs and wire-up deferred to next turn** — they follow the same exact pattern (3-4 routes per resource + a small fetch+set on the chart page) and can be added without further schema changes.
2026-06-01 17:45 - [MODIFIED] - `POST /api/patient/appointments` now auto-establishes a `PatientAssignment` row when a patient self-books with a clinician for the first time. Before this, Priya could book Dr. Rahul's 9:30 slot and Dr. Rahul's Schedule Day/Week tabs would correctly show the appointment, but his Patient panel stayed empty ("No patients are assigned to you yet") because that page reads from `PatientAssignment`, not `Appointment`. Now: after the Appointment INSERT succeeds, the route checks for an existing active assignment for `(patientId=session.uid, clinicianId, endedAt=null)` and creates one tagged `role: "self-booked"` if missing. Org Admin can still see + remove these from `/admin/patients/[id]` → Assigned clinicians (the role tag distinguishes patient-initiated assignments from admin-created ones). Idempotent — repeated bookings by the same patient with the same clinician don't create duplicate assignments.

2026-06-01 17:30 - [ADDED] - Patient → Clinician booking round-trip is now DB-backed end-to-end. (1) New `POST /api/patient/appointments` (Patient-gated) — accepts `{clinicianId, date (YYYY-MM-DD), time ("9:30 AM"), durationMinutes, mode, reason}`, parses date+time into a local-time `startsAt`, looks up the clinician (must be active + have an organizationId), pulls patient name + email from the session, rejects double-booking the same `(clinicianId, startsAt)` slot, then INSERTs an `Appointment` row with `status: confirmed`, `room: "Telehealth"` for mode=telehealth, and `notes: "<reason>"`. (2) New `GET /api/clinician/appointments` (Clinician-gated) — returns this clinician's appointments with optional `?from=&to=` (local-day window), pre-formatted `date` + `time` fields for direct UI binding. (3) `app/patient/appointments/new/page.tsx` `confirm()` rewired: shows a "Booking…" disabled state, POSTs to the new endpoint, on 201 mirrors the booking into `patient-store` so the patient list reflects it immediately, then redirects to `/patient/appointments`. The button uses the slot's actual `slotMinutes` as duration. (4) `app/clinician/schedule/page.tsx` rewired: new `apiAppointments` state fetched whenever `selectedDate` changes (7-day window so Day + Week tabs share one dataset and don't refetch on view toggle); Day tab `dayAppointments` filters by local-date; Week tab gets a minimal shape (`id, date, time, reason`) derived from API rows (`notes ?? patientName ?? "Booked"` as the reason label). Unused `ClinicianAppointment` import dropped. End result: when Priya hits Confirm on `/patient/appointments/new`, Dr. Sharma's `/clinician/schedule` Day + Week tabs show the new booking on next render — no localStorage syncing across tabs, no demo seed in the loop.
2026-06-01 17:00 - [FIXED] - Schedule availability silently round-tripping to nowhere. Symptoms: clinician saves Mon–Sat availability with mixed 15/30-min cadences (and a Wed split shift); their own Availability templates tab keeps showing it (local store retained the values), but the patient booking page reports "Dr. Rahul Sharma hasn't published availability yet" — and the slots endpoint returns `hasTemplate: false`. Root cause: the `$queryRaw` / `$executeRaw` calls in both `/api/clinician/availability` AND `/api/patient/clinicians/[id]/slots` were using **snake_case** column names (`schedule_template`, `role_kind`, `deleted_at`, `updated_at`) but Prisma generated **camelCase** column names verbatim from the schema fields (`"scheduleTemplate"`, `"roleKind"`, `"deletedAt"`, `"updatedAt"`). Postgres returns no rows / no error from a `SELECT "schedule_template"` against a `scheduleTemplate` column when wrapped in our defensive try paths, so the reads returned `{}` and writes UPDATEd zero rows — silently. Fix: rewrote every raw-SQL identifier in both routes to the actual camelCase column names. Added an inline comment on the queries flagging the Prisma naming convention so the next raw-SQL hit doesn't repeat the mistake. Verified by querying `information_schema.columns` — actual names confirmed: `scheduleTemplate`, `roleKind`, `deletedAt`, `mfaEnrolledAt`, `mfaSecret`, `mfaRequired`, `permissions`.
2026-06-01 16:45 - [REMOVED] - Synthetic `slotsFor()` fallback in the patient booking page. The fallback (a deterministic hash of the clinician UUID → 15-or-30-min cadence × 9–5 grid) was kicking in whenever the API returned 0 slots — which happens for any clinician who hasn't yet saved a `schedule_template` row. That's why the screenshot showed every day uniformly 30-min "9:00 AM, 9:30 AM, …, 4:30 PM" even though the clinician's own availability tab displayed mixed cadences (the local-store display was stale from an old localStorage seed; the DB was empty). The page now renders only what the API returns. When the API returns `slots: []`, the empty-state copy adapts based on `hasTemplate`: "<Clinician> hasn't published availability yet — please pick another clinician" vs "No open slots in the next 7 days". Dead `toAmPm` + `slotsFor` functions deleted.
2026-06-01 16:30 - [FIXED] - Patient booking slot grid showed a uniform cadence (e.g. all 30-min) even when the clinician availability tab clearly displayed mixed cadences and split shifts per day. Root cause: the per-day Edit dialog collected `morning + afternoon` ranges (e.g. Wed = 9–12 + 2–5 with 30-min slots) but the `PATCH /api/clinician/availability` request only sent the morning window — the afternoon was silently dropped on the DB write, so on next reload the patient slots API saw only the morning range (and sometimes a wider one because Add availability had previously written `9–17` for that day). Fix extends the `schedule_template` JSON shape to optionally carry an `afternoon: { start, end }` alongside the existing first window. PATCH validates `afternoon.start < afternoon.end && afternoon.start >= morning.end` before persisting. `/api/patient/clinicians/[id]/slots` `buildSlotsForDay()` now generates from BOTH windows (refactored into a `buildWindow(start, end, slotMinutes)` helper and concatenated chronologically). `app/clinician/schedule/page.tsx` PATCH handler conditionally appends `afternoon` to the body when the local edit form set it; the on-mount hydration GET now also reads the optional `afternoon` field back into the store so the Availability templates display reflects what's actually persisted. End-to-end: Wed 9–12 + 2–5 30-min on the clinician page now produces exactly `9:00, 9:30, 10:00, 10:30, 11:00, 11:30, 2:00, 2:30, 3:00, 3:30, 4:00, 4:30` in the patient booking grid — not a single 9–5 range.
2026-06-01 16:00 - [ADDED] - Per-day slot-cadence display in the patient booking grid. When Dr. Sharma saves Mon 15-min / Wed 30-min / Thu 15-min via Schedule → Add availability, Aarav now sees the EXACT grid: "MON, JUN 1 · 15-min slots" → 9:00 AM, 9:15 AM, 9:30 AM, ..., 4:45 PM (32 entries) and "WED, JUN 3 · 30-min slots" → 9:00 AM, 9:30 AM, ..., 4:30 PM (16 entries). `/api/patient/clinicians/[id]/slots` response shape extended: each `{ day, times }` is now `{ day, times, slotMinutes }` (per-day cadence carried from `schedule_template`). `app/patient/appointments/new/page.tsx`: new `SlotDay` type with `slotMinutes`; the synthetic `slotsFor()` fallback was rewritten to build a full 9 AM–5 PM grid at the clinician's hashed cadence (15 or 30 min) instead of picking 3–6 random pool entries (which is what made the screenshot show only "10:00 AM, 1:30 PM, 2:00 PM, 4:30 PM"); `SlotStep` header hint now adapts to mixed cadences ("15-min slots" when uniform, "Slot lengths vary by day" when mixed) and each day's `<p>` gained an inline `<Badge>15-min slots</Badge>` next to its label.
2026-06-01 15:30 - [REMOVED] - All clinician-role static / seed data from `lib/clinician-store.tsx`. A freshly-signed-in clinician (Dr. Ayushi, Dr. Sharma, etc.) no longer sees Dr. Vikram Mehta's seeded patient panel (Ramesh / Suresh / Kavita), 10 cross-day appointments, today's Lunch / tomorrow's Admin-time / day-after Focus-block, the SOAP draft, the Chest-Xray review task, the access-request, or the assignment notification on Day/Week/Availability views. `makeSeed()` now returns empty arrays for every slice and blank strings on the `profile` object (identity already comes from `/api/clinician/dashboard`). `scheduleTemplate` defaults to **all 7 days off** (`{slotMinutes: 15, off: true}` per day) so the patient booking page's per-clinician slots fetch correctly reports "this clinician hasn't published availability yet — try another clinician" until the clinician saves via Schedule → Add availability. Storage key bumped `v3 → v4` so returning browsers reseed. Unused seed helpers `iso(days)` + `dateOnly(days)` deleted. Day-tab schedule already renders an empty-state row added earlier; Week-tab's "No bookings" placeholder + "Open day →" CTA take effect immediately.
2026-06-01 15:00 - [ADDED] - Patient booking Step 2 slot grid now reflects the **selected clinician's saved availability** instead of a hash-based synthetic schedule. New `GET /api/patient/clinicians/[id]/slots` (Patient-gated) reads the clinician's `schedule_template` JSONB column via `$queryRaw` (matches the existing /api/clinician/availability pattern to dodge the stale Prisma client cache), validates the user is an active clinician, then walks up to the next 10 calendar days and emits the first 4 working days. For each working day it expands `{start, end, slotMinutes}` into 12-hour-formatted time labels ("8:00 AM" / "8:15 AM" / ...). `app/patient/appointments/new/page.tsx`: removed direct `slotsFor()` call; added a `useEffect` that re-fetches slots whenever `data.clinicianId` changes. If the clinician has no saved template (or saved every day as `off`), the page falls back to the deterministic `slotsFor()` synthetic generator so the demo still has something to book against. `SlotStep` got a `loading` prop and a friendlier empty-state copy ("hasn't published availability yet — try another clinician"). When Dr. Ayushi Diyora saves Mon–Fri 09:00–17:00 / 15-min via her schedule page, Aarav sees those exact slots when he picks her in Step 1.
2026-06-01 14:30 - [REMOVED] - All patient-role static / seed data so a freshly-signed-in patient (Tarun, Ayushi, etc.) no longer sees Aarav Mehta's content on Records, Prescriptions, Appointments, Documents, Consents, Messages, Notifications, Family, Emergency, Insurance, Vaccinations. `lib/patient-store.tsx` `makeSeed()` rewritten to return empty arrays for every slice and blank-string defaults for `profile` + `emergencyContact`; the unused `isoDaysFromNow` + `dateOnly` seed helpers deleted. Storage key bumped `hs_patient_store_v2 → v3` so returning browsers reseed with the empty shape (instead of keeping their cached Aarav data). `app/patient/records/records-data.ts` `RECORDS` array emptied — drives `/patient/records`, `/patient/records/[id]`, and `/patient/prescriptions`; pages render their existing empty-state copy ("No appointments yet — Book your first appointment to get started", etc.). `components/patient/sidebar.tsx` + `components/patient/header.tsx` mobile-nav: hardcoded `count: 24` (Medical Records) / `badge: "1"` (Prescriptions, Consents) / `badge: "2"` (Appointments) / `badge: "3"` (Messages) all removed — sidebar rows render label-only until those slices are wired to real APIs. Sidebar JSX badge/count rendering removed since the data source is gone. Profile + dashboard surfaces remain DB-backed (unaffected — already use `/api/patient/profile` and `/api/patient/dashboard`).
2026-06-01 14:00 - [FIXED] - Patient login OTP not arriving for temp-mail / sandboxed recipients. Dev-server logs showed Resend rejecting the patient's address `yoyef20013@4nly.com` with `403 You can only send testing emails to your own email address` (Resend sandbox mode); SendGrid then "accepted" the message (202) but the temp-mail provider silently drops incoming mail. Login route used to gate `devOtp` in the response on `mailSent === false`, so once SendGrid said 202 the OTP was withheld and the patient was stuck. Now in any non-production NODE_ENV the response includes `devOtp` regardless of transport result, so the MFA-challenge page's existing "Dev mode · your code is …" banner renders as a reliable fallback. `app/api/auth/resend-otp/route.ts` had two bugs fixed at the same time: (1) used `findDemoUserByUid` only, so DB-backed self-registered patients got NO email at all on Resend tap — switched to `lookupUserByUid` which checks demo + Postgres; (2) same `mailSent` gating as login — same `NODE_ENV` fix applied. In production both routes still hide the OTP and rely on the inbox.
2026-06-01 13:40 - [FIXED] - Two issues on `/clinician/schedule`. (1) `GET /api/clinician/availability` returned 500 because the Prisma client's cached `schema.prisma` (inside `node_modules/.prisma/client/`) did not contain the new `scheduleTemplate` column — `prisma db push` had succeeded but `prisma generate` was blocked by the locked DLL. Rewrote the route to use `prisma.$queryRaw` / `prisma.$executeRaw` with `WHERE "id" = ${uid}::uuid` parameter binding, so it reads/writes the `schedule_template` JSONB column directly without going through the stale Prisma model. The route is now robust against future generate-blocked moments too. (2) Removed the hardcoded `slots` array (Rohan Jain ECG review / Saanvi Sen / Aarav Mehta / Neha Bansal / Vikram Rao / Ananya Joshi etc.) from `app/clinician/schedule/page.tsx`. The Day-tab table is now `useMemo`-derived from `state.appointments` + `state.blockedSlots` filtered to the selected date: each row maps appointment.status → completed/next/upcoming, blocks render as "blocked", patient name is joined from `state.assignedPatients`. Empty-state row added — "No appointments or blocks scheduled for <date>" with a CTA hint pointing at the Add availability + Block slot buttons. New `parseTime` helper near `fmtTime12` converts "9:00 AM"/"13:30"/"09:00 PM" → minutes-since-midnight for chronological sort.
2026-06-01 13:15 - [ADDED] - Clinician "Add availability" + per-day edit dialogs now persist to Postgres. New `scheduleTemplate Json @default("{}")` column on `User` (both frontend + backend Prisma schemas, db-pushed). New `/api/clinician/availability` route with three verbs: GET returns all 7 weekdays (filling missing days with `{start: "09:00", end: "17:00", slotMinutes: 15, off: true}` defaults); POST takes the existing dialog body shape `{days: Weekday[], start, end, slotMinutes}` and additively merges into `scheduleTemplate` (other days untouched); PATCH `{day, template}` updates a single day (used by the per-day Edit dialog). All three validate `HH:MM` 24h time format, start < end, and slotMinutes in [5, 240]. `app/clinician/schedule/page.tsx` keeps the existing dialog design/fields unchanged — only the `onSave` handlers were rewired: AddAvailabilityDialog now POSTs after the optimistic store update; EditTemplateDialog PATCHes; a new on-mount `useEffect` GETs the saved template and seeds the clinician store so day rows reflect persisted values across refresh and devices.
2026-06-01 12:30 - [REMOVED] - Hardcoded Dr. Priya Shah / Dr. Rohan Iyer / Dr. Neha Kapoor from the patient appointment booking flow (`/patient/appointments/new`). New `GET /api/patient/clinicians` (Patient-gated) returns the signed-in patient's clinicians: care-team members first (active `PatientAssignment`, marked `isAssigned: true`), then other active clinicians in the patient's tenant. Self-registered patients with no organizationId yet get all active clinicians across tenants. Each row includes name, initials, designation, department, workLocation, profilePhotoUrl, derived `experienceYears` (from `joiningDate`), tenantName, and `isAssigned`. `app/patient/appointments/new/page.tsx` rewritten to fetch this on mount. The `CLINICIANS` constant + `SLOTS_BY_CLINICIAN` map were deleted. Slot availability is now generated by `slotsFor(clinicianId)` — a deterministic hash of the clinician UUID picks 3–6 weekday slots over the next 5 business days (stable across loads but different per clinician) since the Schedule module isn't DB-backed yet. Mode (`office`/`telehealth`) is derived from `workLocation` (matches /tele|video|virtual|remote/). Specialty label = `designation ?? department ?? "Care team"`. Booking-summary sidebar + ClinicianStep card updated to use the new shape; care-team members get a "Your care team" badge.
2026-06-01 11:45 - [MODIFIED] - Fixed TOTP enrollment failing with "Code didn't match" on first try. Two bugs in `frontend/lib/mfa.ts`: (1) `generateURI` passed `type: "totp"` instead of `strategy: "totp"` — wrong key, silently ignored, so the QR URI was built with otplib v13's defaults; (2) `verifySync` passed `step` (should be `period`) and `now` (doesn't exist on `OTPVerifyOptions` — should be `epochTolerance`). Both options were silently dropped, so the manual `[now - STEP, now, now + STEP]` loop ran 3× against the same current time with no drift tolerance — any phone clock more than a few seconds off failed. Replaced with a single `verifySync` call using `strategy: "totp"`, `period: 30`, `digits: 6`, `epochTolerance: 1` (±1 30-s window = matches Google Authenticator UX). Also fixed `app/api/auth/mfa/verify/route.ts` returning HTTP 401 for "Code didn't match" — that bounces the browser to /login mid-enrollment; changed to 400 since the user IS authenticated, only the body is wrong. 401 reserved for genuinely missing session.
2026-05-29 20:00 - [ADDED] - Tenant-wide theme application — when Org Admin changes Primary/Secondary colors in Branding, the whole portal repaints instantly and survives refresh. New `GET /api/branding` (any signed-in role + public default fallback) returns `{ logoUrl, primaryColor, secondaryColor }` for the session's tenant from `Organization.settings.branding`. New `components/shared/branding-theme.tsx` exports `<BrandingThemeProvider>` + `useBranding()` — the provider mounts in `app/layout.tsx` and on mount fetches `/api/branding`, then writes CSS variables to `document.documentElement.style`: `--color-primary`, `--color-primary-500/600/700` (= the hex), and `--color-primary-50/100/200` derived via `rgba(r,g,b, 0.08/0.14/0.22)` from the hex so soft surfaces stay subtle. Admin settings ColorRow now calls `applyBranding({primaryColor})` on every change (instant local re-paint across every page in the SPA) AND debounces a PATCH at 350ms (server persistence). Logo upload + clear also call `applyBranding` so the chosen file URL is available via `useBranding()` for any header that wants to render it. Default DEFAULT_PRIMARY `#0E7490` matches the original teal in `globals.css` — unbranded tenants look unchanged.
2026-05-29 19:30 - [ADDED] - Branding tab (Logo + Color tokens) now persists to Postgres. `/api/admin/tenant` extended: GET returns `branding: { logoUrl, primaryColor, secondaryColor }` from `Organization.settings.branding` JSON with sensible defaults (`#0E7490` primary, `#FFFFFF` secondary, empty logo); PATCH accepts a `branding` patch that validates `logoUrl` is a `data:image/(svg+xml|png|jpeg);base64,…` URL (or empty to clear), enforces ~1 MB size cap, and validates `primaryColor`/`secondaryColor` against `#rrggbb` regex. Branding writes merge into `settings` alongside the existing `contact` block — neither stomps the other. `app/admin/settings/page.tsx` Branding tab rewritten: Logo card renders the persisted logo (or "No logo uploaded yet" placeholder) and has Upload + Remove buttons; Upload reads the file via `FileReader.readAsDataURL`, validates client-side (≤1 MB), and PATCHes the base64 data URL. Color rows use the inline native picker with optimistic local preview and a 350ms debounced PATCH so dragging doesn't spam the API. The decorative `EditColorButton` dialog from `org-widgets.tsx` is no longer rendered (it was a no-op toast, never persisted).
2026-05-29 18:50 - [MODIFIED] - `GET /api/admin/tenant` now falls back to the first Org Admin's email + phone when `Organization.settings.contact.{email,phone}` is empty. Freshly-provisioned tenants ("city hospital" etc.) had no contact saved, so the Profile tab showed an empty Primary contact email field. The fallback only kicks in when no explicit override exists — once Save sends `contact.email` via PATCH, that value wins on subsequent loads. Address has no User-row analogue so it stays empty until manually saved.
2026-05-29 18:30 - [REMOVED] - Hardcoded Organization Settings → Profile tab data ("City General Hospital · info@citygeneral.health · +91 79 4567 8910 · Plot 14, Sky Road, Ahmedabad 380015"). `app/api/admin/tenant/route.ts` extended: GET response now includes `contact: { email, phone, address }` read from `Organization.settings.contact` JSON, plus `typeKind` (raw enum value). New `PATCH /api/admin/tenant` (Org Admin gated, same-tenant) accepts `name` (validated non-empty) + `type` (label → `TenantType` enum) + `contact` (merged into `settings.contact`, email format-validated). Refactored shared helpers `shapeTenant` + `deriveCounts` + `loadOrgWithCounts` so GET and PATCH return the same envelope. `app/admin/settings/page.tsx` ProfileTab now fetches `/api/admin/tenant` on mount to populate name/type/email/phone/address from Postgres; Save sends PATCH and refreshes both the form state and the local admin-store (so the dashboard widgets stay in sync); Discard reverts to last server snapshot. Loading + saving spinners added.
2026-05-29 18:00 - [REMOVED] - Hardcoded Patient Settings → Profile tab data + Care Team panel. New `GET /api/patient/profile` returns the signed-in patient's DB row (firstName/lastName/email/phone/gender/dateOfBirth/profilePhotoUrl/tenantName/mfaEnrolled/mfaRequired/createdAt) + synthetic MRN + active `PatientAssignment` care team joined to clinician (name/initials/designation/department/photo). New `PATCH /api/patient/profile` accepts `firstName/lastName/phone/dateOfBirth/gender/profilePhotoUrl`, validates non-empty names + date parse, writes via `prisma.user.update`, returns the new shape (email is intentionally read-only — verification flow not wired). `ProfileTab` in `app/patient/settings/page.tsx` rewritten: fetches the GET on mount to populate the form draft + sidebar identity + Care Team list; "Save changes" sends PATCH and syncs the local store on success; "Discard" reverts to the last server snapshot. MRN field now shows the real synthetic MRN (was hardcoded `CG-2026-0481`). "Patient since Feb 2026" is now derived from `createdAt`. Care Team panel replaces the hardcoded Priya Shah + Rohan Iyer pair with whatever Org Admin has assigned via `PatientAssignment` — empty-state copy when none. Address/city/state/postalCode stay in localStorage since there are no DB columns.
2026-05-29 17:35 - [REMOVED] - Hardcoded patient header + dashboard greeting/stats/next-appointment. New `GET /api/patient/dashboard` (Patient-gated) returns `{ profile, stats, next, upcoming, careTeam }` from real DB rows: profile from session-resolved User (firstName/lastName/email/phone/gender/dob/photo/tenantName + synthetic MRN `CG-<year>-<uuidPrefix>`), `upcoming` from `appointments` where `patientEmail === me.email && startsAt >= today && status NOT IN (cancelled, no_show)` joined to clinician, `careTeam` from active `PatientAssignment` rows, `stats.upcomingCount/careTeamSize` from those queries (documents/activeConsents/unreadMessages stay derived since no DB tables yet). `components/patient/header.tsx` now fetches the dashboard endpoint to populate the avatar initials, signed-in name ("Aarav Mehta" → real name), tenant subtitle, dropdown email, and MRN — was hardcoded "Aarav Mehta · aarav.mehta@example.com · MRN · CG-2026-0481". `app/patient/dashboard/page.tsx` keeps the local `usePatientStore` for record-detail rendering but overlays the greeting first name, hero next-appointment card, and four quick-stat counts with API values when present. Falls back to store state when the API hasn't responded yet (preserves dev demo with seed data).
2026-05-29 17:10 - [ADDED] - Real patient chart lookup for DB-assigned patients on `/clinician/patients/[uuid]`. The chart page was using `state.assignedPatients.find` against the localStorage seed, so opening a real DB-backed assignment (UUID) showed "Patient not on your panel". New `GET /api/clinician/patients/[id]` (Clinician-gated) — looks up the patient by UUID, requires an active `PatientAssignment` (`endedAt IS NULL`) for the signed-in clinician, returns identity + age + assignment role. Page now: tries the store first, and if the seed doesn't have this id, fetches the API in `useEffect`, synthesizes a minimal `AssignedPatient` (empty conditions/allergies/consentScopes, consentStatus="active") so the chart banner + identity render. Clinical lists (notes/Rx/docs/appointments) stay empty for DB patients until those modules are DB-backed too — the chart still navigates and the "Request access" / consent flows continue to work against the (initially empty) consentScopes.
2026-05-29 16:45 - [REMOVED] - Hardcoded clinician Patient Panel page (`/clinician/patients`). Was reading the seed `assignedPatients` array (Ramesh/Suresh/Kavita with CITY-001234 MRNs) from `useClinicianStore`. New `GET /api/clinician/patients` (Clinician-gated) returns the full active panel — every `PatientAssignment` where `clinicianId === me && endedAt === null` — joined to the patient User with `firstName/lastName/email/phone/gender/dateOfBirth/profilePhotoUrl/status/mfaEnrolledAt`, the patient's `lastContact` (their last appointment with this clinician), `age` derived from DOB, a synthetic MRN (`CG-<year>-<index>`), and the assignment `role`/`notes`/`startedAt`. `app/clinician/patients/page.tsx` rewritten to fetch this endpoint instead of reading the store. Stats: `Total assigned` + `+N this month` derived from `startedAt`; `2FA enrolled` shows `<mfaCount> of <total> patients`. Open prescriptions + Unread threads remain `0` (no DB tables yet) with "—" subtitle. Search now matches name/MRN/email. Each row links to `/clinician/patients/[uuid]` (patient User UUID). When Dr. Rajesh Patel signs in, the panel now shows the two real patients Org Admin assigned him (Ayushi Diyora + Tarun Mehta) instead of the seed mock list.
2026-05-29 16:00 - [REMOVED] - Hardcoded clinician dashboard data. New `GET /api/clinician/dashboard` (Clinician-gated) returns `{ profile, stats, todayAppointments, panelPatients, nextUp }` — profile from session-resolved User row, `stats.todayCount`/`completedToday` from Postgres `appointments` filtered to today's local-day window for this clinician, `stats.panelSize` from `PatientAssignment.count({ clinicianId, endedAt: null })`, `panelPatients` from the latest 5 active `PatientAssignment` rows joined to the patient User. `app/clinician/dashboard/page.tsx` rewritten: fetches on mount, replaces `useClinicianStore` reads (greeting, hero next-up card, today's queue, assigned panel section) with live data. Tasks + prescriptions remain `0` since no DB tables yet — labelled "All caught up". `app/clinician/layout.tsx` rewritten to mirror the admin layout pattern: fetches `/api/auth/me` for header (name/email/initials/role) and refetches `/api/clinician/dashboard` on every pathname change for the sidebar `Patient panel` count + `Schedule` today badge. Subtitle assembles dynamically as `<designation> · <department> · <tenantName>`. Drops the hardcoded "Dr. Priya Shah · Cardiology · City General".
2026-05-29 15:30 - [ADDED] - Patient ↔ clinician panel assignments. New `PatientAssignment` Prisma model (`patient_assignments` table) with `patientId`, `clinicianId`, optional `role` (primary/specialist/consulting/covering), optional `notes`, `startedAt`, nullable `endedAt` (null = active, stamped to "unassign"), indexes on `(patientId, endedAt)` and `(clinicianId, endedAt)`. Inverse relations added to `User`: `asPatientPanel` + `asClinicianPanel`. Mirrored to both `frontend/prisma/schema.prisma` and `backend/prisma/schema.prisma`, applied via `prisma db push --skip-generate`. New routes (Org Admin gated, same-tenant check): `GET /api/admin/patients/[id]/clinicians` returns active assignments + pool of available clinicians for the assign dropdown; `POST` creates an assignment with duplicate-active guard + clinician-belongs-to-tenant check + 201 response; `DELETE /api/admin/patients/[id]/clinicians/[assignmentId]` sets `endedAt = now()` (soft-unassign so history is preserved) with 404/409/403 guards. UI: `app/admin/patients/[id]/page.tsx` rewritten to add a new "B · Assigned clinicians" section between Personal info and Account — lists assigned clinicians with avatar/designation/department, role badge, "Since <date>", and a Remove button per row. New `AssignDialog` opens from the section header, with clinician select (filtered to unassigned-yet pool), role select, optional notes textarea. Both assign and remove toast "audit-logged" and refetch the list. Section letters cascade (B Personal → B Assigned → C Account → D Security).
2026-05-29 11:30 - [ADDED] - Extended `User` schema with HR / employment fields and wired the full-profile invite form to persist every field. New nullable columns on `users` (synced in both `frontend/prisma/schema.prisma` and `backend/prisma/schema.prisma`, applied to Postgres via `prisma db push`): `employeeId String?`, `dateOfBirth Date?`, `gender String?`, `profilePhotoUrl String?`, `department String?` (indexed), `designation String?`, `accessLevel String?`, `reportingTo String?`, `joiningDate Date?`, `employmentType String?`, `shift String?`, `workLocation String?`, `permissions Json @default("{}")`, `mfaRequired Boolean @default(true)`. `app/api/admin/users` POST accepts every field and persists them; GET + POST response now include the full extended profile. `app/admin/users/invite/page.tsx` restored to its 5-section layout (Basic info / Role & access / Employment / Security / Permissions) — every input controlled, submits to the same endpoint, dropped fields are back. Verified round-trip: every field POSTed echoes back unchanged from Postgres.
2026-05-29 11:00 - [ADDED] - Functional staff management for Org Admins. New `GET/POST /api/admin/users` and `PATCH /api/admin/users/[id]` (all guarded to `role === "Org Admin"` + same-tenant check). POST creates a Postgres `users` row with bcrypt-hashed temp password and `status=invited`, mints a single-use onboarding JWT, then sends the welcome email via the multi-transport `sendMail` (Resend → SendGrid → SMTP). PATCH supports `status` changes (`active`/`suspended`/`deactivated`) and `forcePasswordReset` (re-sets status to `invited` so next login routes through reset). UI role labels (`Clinician`/`Care Team`/`Org Admin`/`Compliance Manager`/`Auditor`) map to Prisma `RoleKind`. `app/admin/users/page.tsx` rewritten — replaced `useAdminStore` with a live `/api/admin/users` fetch + a built-in `InviteStaffDialog`. Row menu's Force Reset / Deactivate / Reactivate now call PATCH and refetch the list. Empty/loading/error states added. Stats and tabs (`All`/`Clinicians`/`Admins`/`Invited`) compute from real DB data.
2026-05-29 10:00 - [REMOVED] - Hardcoded admin-portal data (Maya Iyer header, "Enterprise · Multi-AZ · SMS enabled · 99.9% SLA · 612 GB / 2 TB" subscription card, sidebar counts `Staff 64 / Patients 4128 / Clinicians 18 / Departments 7`). New `GET /api/auth/me` returns the signed-in user's session claims + initials. New `GET /api/admin/tenant` returns the org admin's tenant from Postgres (name, type, tier, region, multiAz, status, createdAt) plus live per-role user counts (`staff`, `clinicians`, `patients`, `admins`, `pendingInvites`, `departments=0` placeholder). `app/admin/layout.tsx` fetches both on mount and feeds them into `RoleHeader` (name, subtitle = "Org Admin · <tenant name>", initials, email) and `RoleSidebar` count badges. `app/admin/dashboard/page.tsx` Hero subscription card now renders the real tier ("Basic"/"Pro"/"Enterprise") and multi-AZ flag instead of static "Enterprise · 612 GB / 2 TB". Local `useAdminStore` still drives onboarding wizard / recent activity / invitations because those don't have DB-backed flows yet.
2026-05-29 09:15 - [MODIFIED] - Forgot-password flow is now DB-aware for staff org admins. `/api/auth/forgot` switched from `findDemoUser` to `lookupUserByEmail`, so Postgres-stored org admins are found and emailed a reset link (previously the route silently no-op'd because the demo lookup didn't see them). `/api/auth/reset-password` GET + POST swapped to `lookupUserByUid`. POST now branches by `user.source`: `"database"` users get a bcrypt hash written to Postgres `users.password_hash` + status promoted from `invited` to `active` + failedLoginCount/lockedUntil cleared; `"demo"` users still use the in-memory `setPassword` override. End-to-end test confirms: forgot → email → reset link → POST → sign-in with new password all green for a DB-backed Org Admin.
2026-05-28 19:00 - [ADDED] - Per-transport sender override in `lib/mail.ts`. New `RESEND_FROM` env (defaults to `onboarding@resend.dev`) and `SENDGRID_FROM` env (defaults to `EMAIL_FROM`). Resolves the conflict where free-tier providers each require a different verified sender. Resend sandbox uses `onboarding@resend.dev` and SendGrid uses the Single-Sender-verified `ayushi.diyora@sensussoft.com` — neither blocks the other. Default fallback chain reordered to `resend → sendgrid → smtp`: super-admin OTP (recipient = signup email) lands via Resend; welcome emails to other recipients fall through to SendGrid automatically after Resend's sandbox 403.
2026-05-28 18:30 - [ADDED] - `mailAttempts` array in `sendMail` result and `/api/super/tenants` response — each entry is `{via, outcome: "skipped"|"failed"|"ok", error?}`. Toast on provision now lists every transport tried, so a SendGrid failure isn't silently hidden behind a Resend fallback failure. EMAIL_FROM switched to verified SendGrid sender `HealthSecure Portal <ayushi.diyora@sensussoft.com>`. With the per-tenant `sendgrid: true` integration toggle ON, welcome emails now deliver to ANY recipient via SendGrid (no domain verification needed — Single Sender Verification suffices).
2026-05-28 17:45 - [ADDED] - Per-tenant integration toggles on the Provision Tenant form (`SendGrid`, `Twilio SMS`, `ClamAV virus scan`, `Custom SMTP`) now drive welcome-email delivery. Form keeps toggle state in a single `integrations: {sendgrid, twilioSms, clamav, customSmtp}` object and POSTs it to `/api/super/tenants`. The route persists this map under `Organization.settings.integrations` (Postgres JSON column) and translates the flags into a preferred-transport list via `transportsFromIntegrations()` in `lib/mail.ts`. New transports: `sendgrid` (SendGrid v3 REST), `twilio` (Twilio Programmable SMS REST — SMS-only path). `sendMail` now accepts `prefer?: Transport[]` and tries each preferred transport in order, falling back to Resend → SMTP. Toggles with missing env credentials are gracefully skipped (logged but not error-returned unless every transport is unconfigured). New env slots in `.env.local`: `SENDGRID_API_KEY`, `TWILIO_ACCOUNT_SID`, `TWILIO_AUTH_TOKEN`, `TWILIO_FROM`. Response now includes `mailVia` so the toast can say "Welcome email sent via sendgrid" etc. Bugfix in `sendMail`: missing-credential transports no longer clobber the actual error from an attempted transport.
2026-05-28 17:15 - [ADDED] - Tenant row menu actions are now wired to real DB mutations. New `GET/PATCH/DELETE /api/super/tenants/[id]` route (super-admin gated) that loads/updates/archives an `Organization` by slug. `tenant-row-menu.tsx`: **Configure** dialog now controlled (`useState` for name/tier/region) and submits via `fetch(PATCH)`; **Suspend / Reactivate** toggles `status` between `active` and `suspended` via the same endpoint. New `onChanged` callback prop refetches the list after every successful mutation. Submit buttons show a spinner + are disabled during the request; failures toast the server error. Fixed `next.config.ts` rewrite that proxied every `/api/*` to the NestJS backend on port 3001 — it now uses `fallback` shape so frontend filesystem AND dynamic routes (e.g. `/api/super/tenants/[id]`) take precedence, with the backend proxy only firing on true 404s. Without this fix the new dynamic route was unreachable.
2026-05-28 16:45 - [REMOVED] - Hardcoded `seededTenants` array (6 fake rows: City General, Riverside, Northpoint, GreenLeaf, Bluepine, Sunset) from `app/super/tenants/page.tsx`. Tenants list is now driven 100% from `GET /api/super/tenants` (Postgres). Active/Trial/Suspended/Total-users stat cards compute from real data (`useMemo` over the fetched rows). Sidebar "Tenants" badge count (was hardcoded `18` in `app/super/layout.tsx`) now reflects actual DB count, fetched once on layout mount; the badge is omitted while loading. `TenantRow` type lost the unused `patients` field; `tenant-row-menu.tsx` adjusted to drop the Patients detail and accept `status: ... | "archived"`. `GET /api/super/tenants` and `POST` now include `usersCount` per tenant (from Prisma `_count`). Empty / error / loading states added to the list view.
2026-05-28 16:30 - [MODIFIED] - Tenant provisioning now persists to **real Postgres** via Prisma (was in-memory only). Frontend installs `@prisma/client@5.22.0` + `bcrypt@6`, schema copied from `backend/prisma/schema.prisma` to `frontend/prisma/schema.prisma`, generated client output to frontend's `node_modules`. New `frontend/lib/prisma.ts` (HMR-safe singleton via globalThis) and `frontend/lib/user-lookup.ts` (unified `lookupUserByEmail`/`lookupUserByUid` checking seeded demo users → in-memory tenant store → Postgres `users` table). `POST /api/super/tenants` rewritten to use `prisma.$transaction` creating Organization + Role + User (bcrypt-hashed temp password, status=invited), with proper enum mapping (UI "Clinic" → schema `clinic` etc.). `GET /api/super/tenants` lists from Postgres including the first org admin per tenant. Frontend login route uses `bcrypt.compare()` when `lookupUserByEmail` returns `source: "database"`, falls back to `password-store` override + `DEMO_PASSWORD` for seeded demo users. `verify-otp` + `onboard` routes use `lookupUserByUid` so DB-backed admins flow through OTP and onboarding-link prefill identically to demo users. `DATABASE_URL` added to `frontend/.env.local` pointing at the same `helath_secure` instance the NestJS backend uses. Tenants now survive frontend dev restarts.
2026-05-28 15:00 - [ADDED] - Org-admin onboarding link: the welcome email now lands on `/staff-login?onboard=<jwt>` with email + temp password pre-filled, single click to sign in. New `signOnboardToken`/`verifyOnboardToken` in `lib/auth.ts` (HS256, 7-day TTL, jti-tagged). New `stashOnboardCred`/`consumeOnboardCred` in `lib/password-store.ts` (jti -> plaintext temp password Map, one-time retrieval, pinned on globalThis). `POST /api/super/tenants` now mints the onboarding JWT and stashes the plaintext password keyed by jti (password is NOT in the URL — only the signed JWT is). New `POST /api/auth/onboard` route exchanges the token for `{email, name, password, consumed}` and deletes the stash on first hit. `CredentialsForm` now reads `?onboard=` via `useSearchParams` (wrapped in Suspense), exchanges the token on mount, prefills both fields, and renders a "Welcome, <name> — credentials pre-filled" banner. Replay returns `consumed:true` and shows a "link already used → use Forgot password" warning instead.
2026-05-28 14:30 - [ADDED] - Functional tenant provisioning on the Super Admin portal. New `lib/tenant-store.ts` (globalThis-pinned dynamic tenants + org-admin users, same isolation pattern as `password-store.ts`), new `welcomeEmail()` template in `lib/mail.ts` (teal-header design, sign-in details box with email + temp password, "Open sign-in page" button), new `POST /api/super/tenants` route (super-admin only via `verifySession` + role check, generates 16-char crypto-random password meeting policy, creates Tenant + DemoUser, registers password override, sends welcome mail via Resend, falls back to `devCredentials` in the API response when mail send fails or no transport). `GET /api/super/tenants` returns dynamic tenants for the list page. `findDemoUser`/`findDemoUserByUid` in `lib/demo-users.ts` now also check the tenant-store so newly-provisioned org admins can log in. `app/super/tenants/new/page.tsx` rewritten as a client component with controlled form fields + submit; `RegionPicker` made controlled (`value`/`onChange` props). `app/super/tenants/page.tsx` fetches `/api/super/tenants` and merges dynamic rows above the seeded list (de-duped by id).
2026-05-28 13:35 - [REMOVED] - "Demo accounts · password Demo!Pass1234" hint block from all three login pages (patient, staff, super). `CredentialsForm` no longer accepts a `demoAccounts` prop and `DEMO_PASSWORD` constant is removed from the component (still defined in `lib/demo-users.ts` for the API to validate against). Login pages now look production-clean.
2026-05-28 13:20 - [ADDED] - Functional "Forgot password" flow on the frontend demo auth. New `lib/password-store.ts` (in-memory password override + used-jti tracker pinned on `globalThis` so Next.js HMR + per-route module instances share state), new `lib/auth.ts` reset-token helpers (`signResetToken`/`verifyResetToken`, 30 min TTL, jti-tagged for single-use), new `lib/mail.ts` `resetEmail()` template (teal-header design matching the OTP email), new `POST /api/auth/forgot` (sends reset link via Resend, returns same shape for known/unknown emails to defeat enumeration), new `GET/POST /api/auth/reset-password` (validate then consume token, enforce length+upper+lower+digit+symbol policy, revoke jti on success), and interactive `/forgot-password` + `/reset-password` pages. `POST /api/auth/login` now checks the override before falling back to `DEMO_PASSWORD`, so once a user resets they must use the new password. Token signature uses the same `AUTH_SECRET` (or dev fallback) as the session JWT.
2026-05-28 13:00 - [MODIFIED] - OTP email template redesigned to match user spec: teal header bar with "HealthSecure Portal", "Verify it's you" heading, dashed teal box around the 6-digit code, expiry note, audit-logged footer. Template lives in `frontend/lib/mail.ts` `otpEmail()`.
2026-05-28 12:45 - [ADDED] - Resend transport on the frontend demo auth. New `frontend/lib/mail.ts` (Resend-via-fetch, no extra deps), wired into `/api/auth/login` and `/api/auth/resend-otp`. With `RESEND_API_KEY` set in `.env.local`, OTPs are sent to the user's real inbox and the "Dev mode · your code is …" hint disappears from the MFA page (devOtp only returned when no transport is configured).
2026-05-28 12:20 - [MODIFIED] - Backend `MailService` (`backend/src/mail/mail.service.ts`) now prefers Resend (via REST + `RESEND_API_KEY`) over SMTP. Resolution order: Resend → Gmail SMTP → console fallback. New `RESEND_API_KEY` + `EMAIL_FROM` slots in `backend/.env.example`.
2026-05-28 11:45 - [ADDED] - Email OTP MFA for staff roles. New `MfaChallenge` table (6-digit code SHA-256-hashed, 5-min TTL, single-use, 5 attempts). New `MailService` (nodemailer with Gmail SMTP via env + console fallback when SMTP not configured). New `MfaService` (issueChallenge / verifyChallenge) and `/api/v1/auth/mfa/verify` endpoint. Login flow now returns `{ mfaRequired, challengeId, emailHint, expiresAt }` instead of tokens for roles in `MFA_REQUIRED_ROLES` (default: super_admin, org_admin, compliance_manager, auditor, clinician). Patient login stays single-step. Configurable via `SMTP_HOST/USER/PASS/FROM`, `MFA_REQUIRED_ROLES`, `OTP_LENGTH`, `OTP_TTL_SECONDS` in `.env`.
2026-05-28 11:30 - [MODIFIED] - Super admin user updated in DB: email `ayushi.diyora@sensussoft.com`, name "Ayushi Diyora". Seed script env (`SEED_SUPER_ADMIN_EMAIL`) updated to match so re-seed is idempotent.
2026-05-27 16:00 - [ADDED] - Backend scaffold at `backend/` — NestJS 10 + Prisma 5 + PostgreSQL. Initial schema covers `organizations`, `users`, `roles`, `permissions`, `role_permissions`, `sessions` (Sec 7). Modules: `prisma/`, `auth/` (login + refresh + logout + /me with JwtAuthGuard + RoleGuard), `super/tenants/` (GET/POST/PATCH/DELETE under `/api/v1/super/tenants`, locked to `super_admin` role). Seed script provisions a Super Admin user from env + the system permission catalog. Tokens: JWT HS256 12h TTL, refresh tokens SHA-256-hashed in DB and rotated on use. Lockout: 5 failed logins → 15-min lockout. Database name: `helath_secure` (matches project name typo). Setup instructions in [backend/README.md](backend/README.md).
2026-05-27 14:15 - [MODIFIED] - Patient sidebar entries removed: Family, Emergency info, Insurance, Vaccinations. The 4 module pages were refactored — body extracted into reusable `*-manager.tsx` files (`family/family-manager.tsx`, `emergency/emergency-manager.tsx`, `insurance/insurance-manager.tsx`, `vaccinations/vaccinations-manager.tsx`), and `page.tsx` files reduced to thin PageHeader wrappers. Settings → Personal information now renders the 4 managers in a nested Tabs UI (Family / Emergency / Insurance / Vaccinations) instead of summary cards. Dedicated `/patient/{family,emergency,insurance,vaccinations}` routes remain for direct URL access. Why: consolidate "personal info" navigation into Settings per user request, freeing up sidebar real estate.
2026-05-27 13:45 - [ADDED] - `app/patient/settings/page.tsx` gained a new **"Personal information"** tab (between Profile and Security) that consolidates Family / Emergency / Insurance / Vaccinations into 4 summary cards. Each card shows live stat counts + the first 3 records + a "Manage in full view" link to the dedicated page. Settings is now the discovery surface; the 4 dedicated pages remain for deep edits.
2026-05-27 13:30 - [ADDED] - Patient sidebar gained a new "Personal" section grouping the 3 account-personal items (family, emergency, insurance); "Vaccinations" added under Workspace. Both desktop sidebar (`components/patient/sidebar.tsx`) and mobile/search header (`components/patient/header.tsx`) updated in sync.
2026-05-27 13:25 - [ADDED] - `app/patient/vaccinations/page.tsx` — vaccination history with stats (total, doses this year, travel count, next due), filter tabs (All / Routine / Travel / Upcoming), search, add/edit dialog with common-Indian-vaccine combobox, certificate-download mock, upcoming-dose reminder banner (<30 days). Per requirement #11.
2026-05-27 13:20 - [ADDED] - `app/patient/insurance/page.tsx` — multi-plan management with stats (active plans, primary, claims, next renewal), policy-number auto-masking on save, primary-plan flag with auto-promotion on remove, pre-authorization status chips, optional card image upload (filename only for demo), renewal warning banner. Per requirement #10.
2026-05-27 13:15 - [ADDED] - `app/patient/emergency/page.tsx` — ER-visible emergency information panel: primary + secondary contacts, blood group, organ-donor flag, preferred hospital, allergy + critical-medication chip lists, dynamic medical-IDs list. Read-only by default with Edit/Save toggle. Per requirement #9.
2026-05-27 13:10 - [ADDED] - `app/patient/family/page.tsx` — manage family members and authorised caregivers with stats (total, minors, caregivers, last added), relationship/minor/caregiver badges, add/edit dialog, remove with confirm. Per requirement #8.
2026-05-27 13:05 - [MODIFIED] - `lib/patient-store.tsx` — extended with 4 new slices (`family`, `emergencyContact`, `insurance`, `vaccinations`) + 11 new actions + seed data. Storage key bumped to `hs_patient_store_v2`.
2026-05-27 12:30 - [ADDED] - Created CLAUDE.md + KNOWLEDGE.md at project root per session-rule setup. KNOWLEDGE.md scanned and documented all current modules, stores, shared components, known gaps, and conventions.
2026-05-27 11:55 - [MODIFIED] - `components/shared/role-header.tsx` notification bell converted from `<button>` no-op to `<Link href={notificationsHref}>`; added required `notificationsHref` prop and wired it from all 5 role layouts (admin, clinician, compliance, auditor, super) so the bell now navigates to each role's `/notifications` page.
2026-05-27 11:40 - [ADDED] - `app/(auth)/account-locked/page.tsx` — lockout screen with live MM:SS countdown, reason chip, reset-password CTA, audit-event explainer. Honors `?email=`, `?unlocksAt=`, `?reason=` query params.
2026-05-27 11:25 - [ADDED] - `app/clinician/patients/[id]/timeline/page.tsx` — dedicated chronological clinical-events view with kind filter, range tabs, day grouping; pulls assignments, appointments, notes, prescriptions, documents, and access-request events.
2026-05-27 11:00 - [ADDED] - `app/clinician/appointments/page.tsx` — encounter queue index (today/upcoming/past/all tabs, status filter, search, sortable rows linking to detail).
2026-05-27 10:45 - [ADDED] - `app/clinician/appointments/[id]/page.tsx` — single-encounter detail with lifecycle stepper, related notes/Rx blocks, patient identity card.
2026-05-27 10:30 - [ADDED] - `app/clinician/patients/[id]/documents/page.tsx` — patient-scoped documents page (upload, virus-scan badge, category filter, signed-download toast). Discovery link added in chart's Documents tab.
2026-05-27 10:15 - [ADDED] - `app/clinician/patients/[id]/records/new/page.tsx` — thin redirect to `/clinician/notes/new?patient=<id>` (keeps single source of truth for the note editor).
2026-05-27 10:00 - [ADDED] - `app/clinician/patients/[id]/records/page.tsx` — dedicated records view with template/status filters, stats, consent gating + request-access dialog reuse. Discovery link added in chart's Records tab.
2026-05-27 09:45 - [MODIFIED] - Fixed admin patient roster 404 — admin-store seed IDs changed from `pt_aarav`/`pt_neha`/`pt_vikram` to slug IDs (`aarav-mehta` etc.) so they match the static `PATIENTS` array used by the `[id]` detail page. Storage key bumped to `hs_admin_store_v2`.
2026-05-27 09:30 - [MODIFIED] - 10 pages wrapped in `<Suspense>` around `useSearchParams` so `next build` passes static prerender (clinician/patient messages views, mfa-prompt, mfa-recovery-codes, checkout, audit-logs, clinician/notes/new, patient/records).
2026-05-27 09:00 - [ADDED] - Sensitive-access approval walkthrough: extended clinician store with `AccessRequest` slice, `requestAccess` / `simulateApprovalDecision` / `revokeExpiredAccess` actions, `hasEffectiveConsent` helper. Added `RequestAccessDialog` component and wired pending/approved banners + countdown into the patient chart page.
2026-05-27 08:45 - [MODIFIED] - Security pass — bumped `postcss` to 8.5.15 via `overrides`, drove `npm audit` to 0 vulnerabilities, scaffolded `.eslintrc.json`, fixed 5 rules-of-hooks/unescaped-entities lint errors, converted Google Fonts to `next/font/google` (self-hosted Inter + JetBrains Mono).
