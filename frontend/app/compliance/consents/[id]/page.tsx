// Compliance Manager opens the same consent detail page as the patient
// (read-only view of the consent record). Mirrors the auditor re-export
// pattern at app/auditor/audit-logs/page.tsx.
export { default } from "../../../patient/consents/[id]/page";
